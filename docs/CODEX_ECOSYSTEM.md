# AFTERLIGHT — Codex Ecosystem (AF-088)

**Authority:** Produced output of AF-088. Extends AF-000 → AF-087 — above all AF-043's unchanged Codex engine and AF-087's unchanged profile/progression/journal layer. Every future discovery, expansion and civilisation extends this architecture; it is extended, never replaced.
**Binding rule of the whole document:** knowledge is a network, not a store — the eight-tier ladder contains AF-087's six-stage lattice exactly, the Player Notebook is completed rather than duplicated, every automatic feed is genuinely automatic, and Community Support stays honestly deferred per the Constitution's own offline-first rule.

---

## 1. Composition, not modification

AF-043 and AF-087's entire engine — entry shapes, categories, eras, the discovery lattice, the player journal — stands untouched. AF-088 adds exactly one wrapping runtime (`CodexArchiveRuntime`), two new permanent ledgers, one notebook extension, and a family of pure query functions. Every one of them depends on AF-087's public API only.

## 2. The Knowledge Web reaches two categories deeper

Seventeen web nodes: fifteen realise directly onto AF-087's own primary-category register, and two — Timeline Events and Historical Events — reach into AF-043's real `"timeline"`/`"events"` codex shelves, ground AF-087's seventeen categories never touched. `knowledgeWebFor` turns the one flat `relatedEntryIds` mechanism into a genuine seventeen-bucket graph view, verified against the real forty-entry roster.

## 3. Eight tiers, composed around six

`CodexArchiveRuntime` wraps a `CodexDiscoveryRuntime` instance. Detected sits before Observed; Archived sits after Mastered and requires it — a permanent parallel mark, the AF-077 "evolved" pattern once more. The six shared tiers preserve AF-087's own order exactly; "Analysed" is simply this layer's label for the identical "studied" ordinal slot, and the wrapped runtime's own vocabulary stays fully readable through its own unmodified API.

## 4. Two more permanent ledgers, genuinely automatic

`ScientificArchiveRuntime` and `ExpeditionJournalRuntime` are sequence-monotone, no-removal, and — the point that matters — **automatically fed**: the Expedition Journal records at the real unlock-check seam, the real `endRun`, and the real research-unlock callback. Nothing here waits for a player click; that is exactly the spec's own distinction between the Expedition Journal (§automatic) and the Player Notebook (§curated).

## 5. The Notebook, completed rather than duplicated

AF-087's locked journal already ships four of the spec's seven Player Notebook surfaces. AF-088 adds precisely the three it doesn't have — Comparison Notes, Research Goals, Expedition Plans — in its own small extension runtime.

## 6. Search, Collection Tracking, Community Support

Eight Search System kinds are real functions run against the real roster and runtimes, not registered names. Collection Tracking's overall figure is asserted to equal AF-043's own `discoveryPercent` exactly — no drift between the aggregate and the engine. Community Support's six items are a registered list and nothing else: no networking code exists anywhere in this module, matching the Master Constitution's own rule that online features are an optional later layer.

## 7. Live in the game

The Discoveries and Scientific Papers feeds fire on the same per-frame seam AF-087 introduced; Mission History and Bosses feed at the real end-of-run; Recovered Research feeds at the real research unlock; the Statistics screen gained a real "Set/Clear Research Goal" button beside AF-087's Pin — browser-verified end to end.

---

## Internal review loop (AF-088, recorded)

- **Zero engine changes** — one wrapping runtime, two ledgers, one notebook extension, pure functions; AF-043/087 untouched. ✔
- **The web reaches further, invents nothing** — two categories realised through AF-043's own timeline/events shelves. ✔
- **The ladder is structurally contained** — the six shared tiers preserve AF-087's order exactly, proven. ✔
- **Automatic vs curated, honoured** — the Expedition Journal fires at three real seams; the Notebook only on player action. ✔
- **The Notebook is completed, not duplicated** — the three new surfaces registered, the four existing ones verified untouched. ✔
- **No drift** — the completion aggregate matches the engine's own percentage exactly. ✔
- **Community Support stays deferred** — a registered list, zero networking code. ✔
- **Reachable, live** — both ledgers non-empty from the first frame, the goal button works end to end, zero page errors. ✔

**Internal quality score: 9.5/10 — approved and locked; the web realisation map, tier containment proof, ledger discipline, notebook completion and search functions bind at every future discovery module.**
