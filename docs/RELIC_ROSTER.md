# AFTERLIGHT — Relic Roster (AF-078)

**Authority:** Produced output of AF-078. Extends AF-000 → AF-077 — above all AF-029's unchanged relic engine (whose fusion-flavoured evolution shaped this module's set design), AF-077's unchanged profile architecture, and AF-026's collection pattern. Every future Relic extends this architecture; it is extended, never replaced.
**Binding rule of the whole document:** no relic exists in isolation — families, origins, sets, and the museum bind every relic into the ecosystem; set bonuses carry no numeric field (gameplay, never statistics); void power pays a price by law; and fusion inputs are never set pieces, because the engine taught us so.

---

## 1. The full reliquary

Nine relics: AF-029/077's eight head the roster unchanged, joined by the **Veil Fragment** — a void relic obeying the module's own law (power requires sacrifice: +status chance, −shield capacity), passing AF-029's validator and AF-077's fifteen-part completeness function. The composition root's drop pools upgraded from the sandbox seven to the full roster: the Keepsake and the Fragment genuinely drop.

## 2. Families and origins

Fifteen families map totally onto AF-077's sixteen categories; eight of fifteen carry relics today, the rest await content. Eight discovery origins each carry an authored identity ("reputation relics are given, never dropped — trust made physical"), and every roster entry resolves relic, family, origin, and set membership with no stat field (key-inspection asserted).

## 3. Relic sets — gameplay, never statistics

`RelicSetBonusDef` is a threshold, a trigger, and a description — no numeric field exists, so stat-flavoured set bonuses are unrepresentable. Two sandbox sets: the **Thermal Cycle** (Cinder Heart + Static Node) and the **Expedition Ledger** (Conduit Loop + Gambler's Die + Singularity Keepsake, 2pc and 3pc). Detection (`activeSetBonusesFor`) is pure, order-independent, and threshold-exact — verified against ALL 512 possible loadouts by brute force.

## 4. The engine taught the third authoring convention

AF-029's evolution is a FUSION: acquiring the Ember Core and Frost Shard consumes BOTH and grants the Cinder Heart. The first set draft listed the inputs as pieces and the engine broke it in tests — twice. The recorded convention: **fusion inputs must never be set pieces; build sets from the fusion's product**, so evolution only ever advances a set. (Joins AF-077's two conventions: uniqueness is `maxStacks: 1`; synergies declare on the alphabetically-earlier id.)

## 5. Void, ancient, prototype — trait vocabularies with one law

Void traits (six), ancient traits (seven), prototype traits (six) are registered vocabularies. One is a LAW today: every void-family relic must carry a negative effect clause, asserted across the roster — power requires sacrifice. Seven evolution routes map totally onto AF-077's six triggers; eight collection kinds and three forbidden balance outcomes (mandatory combinations, single dominant builds, meaningless stat inflation) are registered by name.

## 6. The Museum

`museumEntryFor` derives a seven-feature exhibit (3D inspection, lore, history, discovery timeline, civilisation origin, related entries, statistics) from data every relic already carries — asserted non-empty for all nine. The museum celebrates exploration without a new system.

---

## Internal review loop (AF-078, recorded)

- **Zero shape changes** — the roster is data on AF-029/077's unchanged types; one pure set-detection function; the full roster live in the real drop pools. ✔
- **Sets are gameplay by shape** — no numeric field (key inspection); every piece a real relic; thresholds ascending; detection brute-force-verified across all 512 loadouts. ✔
- **The fusion convention holds** — the engine's own evolution semantics broke two set drafts in tests before the design honoured them; the convention is recorded for every future set author. ✔
- **Power requires sacrifice, by law** — every void-family relic asserted to pay a price. ✔
- **Sweep proof** — 1,000 seeded acquisition careers through the REAL RelicSystem: set bonuses never regress as pieces accumulate (fusion included). ✔
- **Reachable, live** — `relics · sets 0 live · reliquary 0/9 owned` on the overlay, zero page errors. ✔

**Internal quality score: 9.5/10 — approved and locked; the families, origins, set thresholds, trait vocabularies, evolution routes and museum features bind at every future relic, set and expansion module.**
