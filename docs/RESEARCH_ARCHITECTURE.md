# AFTERLIGHT — Research Architecture (AF-081)

**Authority:** Produced output of AF-081. Extends AF-000 → AF-080 — above all AF-024's unchanged research engine (categories, node types, the validated tree, hidden discoveries, reset-with-refund), AF-026's codex, and DR-005's owner-ratified instant-unlock ruling. Every future technology extends this architecture; it is extended, never replaced.
**Binding rule of the whole document:** research is DISCOVERY, never inflation — every project's gameplay unlock is a kind and a description with no numeric field; research time is instant by DR-005, permanently; and exactly the resources that exist are marked live.

---

## 1. The engine and the doctrine

AF-024 built the machine: fifteen categories, eleven node types, a `ResearchTree` that validates its own graph (cycles and dangling prerequisites fail loudly), hides discoveries until triggered, and refunds every point on reset. AF-081 adds the science on top as pure data — `ResearchProjectProfileDef` wraps each node BY ID, and `researchArchitectureFor` proves all **twelve architecture parts** (unique ID through future expansion hooks) as a function. Nothing in AF-024 changed.

## 2. Branches — two shelves, one binding

Fifteen **primary branches** (ship engineering through experimental science) and ten **secondary branches** (efficiency through research infrastructure) map totally onto AF-024's fifteen categories. The three-layer binding law holds per project: profile branch → mapped category → the node's own category, exact. Notable mappings are canon, not convenience: quantum physics lives at the Zone's edge (`voidResearch`), barrier lattices are materials science (`shieldTechnology`), and experiments too dangerous for a hull run on orbital platforms.

## 3. The law — never simply statistics

`ResearchGameplayUnlock` is a kind (on the eight-kind progression shelf: new weapons through new biomes) and a description — **no numeric field exists**, so a stat-flavoured breakthrough is unrepresentable at the profile layer. The tree's new project makes the law literal: **Lattice Attunement**, the first crystalResonance project (the category stood registered-empty since AF-024), carries `effect: null` — its entire meaning is the gameplay surface it opens.

## 4. Time, resources, laboratories

**Research time honours DR-005**: unlocks are instant, permanently (owner-ratified), so the "Research Time" architecture part is a compliance assertion — `completionTimeMs === 0` for every project, tested. **Resources are honest**: seven registered, exactly one live today (research points — AF-024's real economy), six awaiting their producing systems. **Eight laboratories** each carry an authored specialisation, from the Engineering Labs to the Ancient Archive; every project names its lab, and every project's codex entry resolves against AF-026's real codex.

## 5. Presentation as derivation

The dependency map, research efficiency (earned points converted to unlocks), and scientific progress (fraction of the tree unlocked) are pure functions over the tree and its snapshot — no new system, and the overlay's research line now reads them live. Seven discovery routes with authored identities, seven major-discovery kinds, eight synergy surfaces, six presentation features, and eight accessibility surfaces are registered shelves; mandatory grinding and numerical inflation are forbidden by name.

---

## Internal review loop (AF-081, recorded)

- **Zero engine changes** — profiles, four pure functions, one new node through the engine's own validation. ✔
- **The law is a shape** — no numeric field on any gameplay unlock (key inspection), and one project with no numeric effect at all. ✔
- **DR-005 honoured** — instant unlocks asserted across the whole tree. ✔
- **"Complete every research project," literally** — every node unlocked through the real engine (hidden discovery revealed, every dependency satisfied, every point spent, efficiency exactly 1), then reset refunds all and preserves the discovery. ✔
- **Sweep proof** — 1,000 seeded research careers: unlocks never regress, points never negative, the spent ledger never drifts. ✔
- **Reachable, live** — `eff 0% · sci 0%` on the overlay's research line at boot, zero page errors. ✔

**Internal quality score: 9.5/10 — approved and locked; the branch maps, architecture parts, unlock-kind shelf, laboratory register, resource honesty rule and derived metrics bind at every future research module.**
