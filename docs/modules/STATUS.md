# AFTERLIGHT — AF Module Status Registry

**Living instrument of AF-012 §9.** One row per AF module: the project's production state at a glance. Updated in the same commit as any status change.

Phase note: AF-000 → AF-012 are specification/framework modules — their build-applicable pipeline stages (Prototype, Performance/Accessibility/Balance Testing) bind onto the first implementing code modules and are tracked there (recorded per module as "standing obligations").

| Module | Title | Version | Stage | Review | QA | Approval | Lock |
|---|---|---|---|---|---|---|---|
| AF-000 | Foundation | v1 | Complete (spec) | Aligned | — (no build output) | Approved 9.5/10 | **LOCKED** |
| AF-001 | Technical Architecture | v1 | Complete (spec) | Aligned | Standing obligations → first code module | Approved 9.5/10 | **LOCKED** |
| AF-002 | Visual Language | v1 *(+2 authorised amendments: palette extension; display face)* | Complete (spec) | Aligned | Standing obligations → first visual build | Approved 9.5/10 | **LOCKED** |
| AF-003 | UX & HUD Architecture | v1 | Complete (spec) | Aligned | Playtest obligations → first playable | Approved 9.5/10 | **LOCKED** |
| AF-004 | Gameplay Readability | v1 *(colour reconciliation resolved by owner ruling)* | Complete (spec) | Aligned | Standing obligations → all content modules | Approved 9.5/10 | **LOCKED** |
| AF-005 | UI Component Library | v1 | Complete (spec) | Aligned | Live review → UI implementation module | Approved 9.5/10 | **LOCKED** |
| AF-006 | Asset Production | v1 | Complete (spec) | Aligned | Validation tooling → first code module | Approved 9.5/10 | **LOCKED** |
| AF-007 | Iconography & Symbols | v1 *(authorised supersession: nine-tier rarity ladder)* | Complete (spec) | Aligned | Standing obligations → all content modules | Approved 9.5/10 | **LOCKED** |
| AF-008 | Colour System & Hierarchy | v1 *(3 judgement calls ratified by owner)* | Complete (spec) | Aligned | Contrast validator → first code module | Approved 9.5/10 | **LOCKED** |
| AF-009 | Typography & Writing | v1 | Complete (spec) | Aligned | Text tooling → first UI build | Approved 9.5/10 | **LOCKED** |
| AF-010 | World Building & Canon | v1 | Complete (spec) | Aligned | Canon ledger begins → first content module | Approved 9.5/10 | **LOCKED** |
| AF-011 | Design Pillars | v1 | Complete (spec) | Aligned | Unified Feature Gate binds all future modules | Approved 9.5/10 | **LOCKED** |
| AF-012 | Production Governance | v1 | Complete (spec) | Aligned | Lifecycle binds all future modules | Approved 9.5/10 | **LOCKED** |
| AF-013 | Content Governance | v1 *(adds compact ID-set save rule; anti-FOMO live-content guarantee)* | Complete (spec) | Aligned | Validation binds all future additions | Approved 9.5/10 | **LOCKED** |
| AF-014 | Master Design Constitution | v1 *(authority chain recorded; priority-order floor rule; ADR practice instituted)* | Complete (spec) | Aligned | Governs all future modules beneath AF-000 | Approved 9.5/10 | **LOCKED** |
| AF-015 | Master Foundation Lock | v1 *(full AF-000→015 review executed; scheduled debts assigned)* | Complete — **Foundation Phase locked 2026-07-06** | Aligned | Production contract binds AF-016+ | Approved 9.5/10 | **LOCKED** |

**PHASE STATUS: Foundation Phase (AF-000 → AF-015) COMPLETE AND LOCKED. Core Gameplay phase in progress (AF-016 →).**

## Core Gameplay phase

| Module | Title | Version | Stage | Review | QA | Approval | Lock |
|---|---|---|---|---|---|---|---|
| AF-016 | Core Gameplay Framework | v1 | Complete — spec + first implementation increment (state machine, core runtime, tests, CI; old iteration archived) | Aligned | Typecheck + 26 tests green; full-session playtests bind as AF-017→026 land | Approved 9.5/10 | **LOCKED** |
| AF-017 | Adaptive Enemy Director | v1 *(canon: 7 environmental events, 9 wave identities, Mutators registered)* | Complete — spec + implementation (`src/game/director/`), 300-run headless simulation in CI | Aligned | 42 tests green incl. simulation invariants; live-feel tuning binds AF-021+ | Approved 9.5/10 | **LOCKED** |
| AF-018 | Camera Framework | v1 *(Photo Mode registered as future flag)* | Complete — spec + implementation (`src/engine/camera/`), deterministic headless tests | Aligned | 54 tests green; on-screen feel review binds AF-020+ | Approved 9.5/10 | **LOCKED** |
| AF-019 | Input Framework | v1 *(targeting law binds AF-021; QuickPing/PhotoMode/one-handed registered)* | Complete — spec + implementation (`src/engine/input/`): action core, kb/mouse + gamepad adapters, touch joystick; pause wired via actions | Aligned | 74 tests green; per-device hardware passes bind at platform QA | Approved 9.5/10 | **LOCKED** |
| AF-020 | Player Movement | v1 *(reserved handling attributes for ship profiles; broad-phase slot registered)* | Complete — spec + implementation (`src/game/movement/`) + playable canvas sandbox (input→movement→camera), browser-verified via Playwright | Aligned | 88 tests green; per-ship/biome feel passes bind at content QA | Approved 9.5/10 | **LOCKED** |
| AF-021 | Combat Framework | v1 *(chain reactions registered as weapon-module extension points; self-damage future)* | Complete — spec + implementation (`src/game/combat/`): 9-stage pipeline w/ breakdown, status engine w/ AF-020 bridge, layered defence, selectors; live sandbox combat (drones, cannon, death), browser-verified | Aligned | 107 tests green incl. 200-fight simulation; weapon/enemy/boss passes bind at content QA | Approved 9.5/10 | **LOCKED** |
| AF-022 | XP & Level Progression | v1 *(12 upgrade categories registered; reroll/lock/wider offers await content buyers; narrated upgrades future)* | Complete — spec + implementation (`src/game/progression/`): anti-grind curve, 7-tier pooled pickups w/ magnetism + coalescing, weighted seeded upgrade pool; live in sandbox (gems, bar, level-up overlay w/ real choices), browser-verified | Aligned | 121 tests green; pacing/balance passes bind at content QA | Approved 9.5/10 | **LOCKED** |
| AF-023 | Loot Framework | v1 *(13 categories + 8 special kinds registered; placeholder affixes; pity timers rejected)* | Complete — spec + implementation (`src/game/loot/`): 8-roll deterministic generator w/ per-drop seeds, AF-007 rarity ladder, clamped smart loot, bank-don't-delete ground cap (Legendary+ exempt); live beams + notices in sandbox, browser-verified | Aligned | 131 tests green incl. 500k-drop distribution; item content passes bind at equipment/weapon/relic QA | Approved 9.5/10 | **LOCKED** |
| AF-024 | Research Framework | v1 *(save system implemented — AF-001 §8 debt paid; 15 categories + 11 node types registered; DR-005: instant unlocks, owner-ratified)* | Complete — spec + implementation (`src/core/save/`, `src/game/research/`): versioned slices w/ checksum/migration/quarantine/backup, tree engine w/ graph validation + hidden discoveries + refund reset; persistent research live in sandbox, reload-survival browser-verified | Aligned | 148 tests green; tree content + research screen bind at their modules | Approved 9.5/10 | **LOCKED** |
| AF-025 | Crafting Framework | v1 *(12 categories + 10 resources + 6 stations registered; evolution hook awaits content inputs; instant crafts per DR-005 reasoning)* | Complete — spec + implementation (`src/game/crafting/`): persistent materials/blueprints/hangar, gated deterministic crafting producing AF-023-shaped items, floor-guaranteed salvage w/ tested anti-exploit invariant, escalating-cost reforge; Lightforge live at Galaxy Command, craft→reload→salvage browser-verified | Aligned | 157 tests green; recipe/evolution content binds at equipment modules | Approved 9.5/10 | **LOCKED** |
| AF-026 | Meta Progression | v1 *(reward union is cosmetic/knowledge only — no stat field exists; account level reuses AF-022 engine; collections use AF-013 compact ID sets)* | Complete — spec + implementation (`src/game/meta/`): account level, generic mastery tracks, idempotent collections, permanent statistics, once-only challenge engine; third save slice; Statistics screen is a live profile; defeat-pays + reload-survival browser-verified | Aligned | 167 tests green incl. 300-expedition career sim; reward pacing binds at cosmetic/content modules | Approved 9.5/10 | **LOCKED** |

**AF-016 §8 integration list COMPLETE: all ten planned systems (AF-017 → AF-026) implemented on the locked foundation with zero foundational additions.**

## Post-integration extension modules

| Module | Title | Version | Stage | Review | QA | Approval | Lock |
|---|---|---|---|---|---|---|---|
| AF-027 | Inventory Framework | v1 | Complete — spec + implementation (`src/game/inventory/`): stacking, cached sort, filters, search, loadouts, protection; live in sandbox (Tab opens real inventory), fifth save slice, 20k-item scale-tested | Aligned | 177 tests green incl. 20k-item scale suite; inventory screen binds at UI module | Approved 9.5/10 | **LOCKED** |
| AF-028 | Equipment Framework | v1 *(resolves AF-027's deferred slot vocabulary; droneEffectiveness/orbitalPower and onBossPresent/onMissionModifier registered — no consuming system yet)* | Complete — spec + implementation (`src/game/equipment/`): slot vocabulary, additive aggregation, additive-only set bonuses, named-reason validation; bonuses route into existing AF-020/AF-021 fields, live in sandbox | Aligned | 188 tests green incl. 5000-combination fuzz suite; build-diversity/balance passes bind at equipment content modules | Approved 9.5/10 | **LOCKED** |
| AF-029 | Relic Framework | v1 *(relic rarity = restricted 7-tier view of AF-007's ladder; only Evolution implemented of 7 relic events; onStatusApplied/onBossMechanic registered future)* | Complete — spec + implementation (`src/game/relics/`): non-numeric-clause schema law, symmetric order-independent synergy detection, exclusion-group validation, evolution (consumes inputs, fires once); two real bugs found+fixed by the self-review's own fuzz tests before lock; live in sandbox (elite kills grant relics, feed the real damage pipeline) | Aligned | 201 tests green incl. 3000-combination fuzz suite | Approved 9.5/10 | **LOCKED** |
| AF-030 | Commander Framework | v1 *(catalogued retroactively — arrived out of turn order, processed before AF-031; ten archetypes registered)* | Complete — spec + implementation (`src/game/commanders/`): four-hook signature (passive/active/ultimate/signature mechanic) reusing AF-028's vocabulary, charge-gated ultimate (new mechanic), code-checked no-overlap fingerprint; mastery/progression reuse AF-026 exactly, zero new persistence; a sandbox Commander governs the run end-to-end (ability + ultimate wired to input) | Aligned | 209 tests green | Approved 9.5/10 | **LOCKED** |
| AF-031 | Ship Framework | v1 *(eleven classes registered Scout → Mythic; Energy introduced as the first new numeric resource, scoped to ability cost)* | Complete — spec + implementation (`src/game/ships/`): `ShipDef` IS an AF-020 `MovementProfile` producer (first non-instant turn rate live) + AF-021 `DefenceState` seed, energy-and-cooldown-gated ability reusing AF-028's `ActiveModule`, code-checked no-overlap fingerprint (`findShipOverlap`, mirroring AF-030); mastery reuses AF-026's `ship:<id>` tracks, zero new persistence; a sandbox Ship governs the run end-to-end (movement profile, hull/shield, energy, passive, ability wired to new `ShipAbility` input), browser-verified | Aligned | 220 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-032 | Weapon Framework | v1 *(fifteen categories registered Ballistic → Singularity; status application is an exact zero-new-kind reuse of AF-021's ten `StatusKind`s; Energy Cost widens AF-031's Energy scope, not a second resource)* | Complete — spec + implementation (`src/game/weapons/`): `WeaponDef` feeds AF-021's existing `DamagePacket`/`OffensiveModifiers` "weapon" stage and `StatusEngine` directly, deterministic fire-pattern spawn geometry (`computeShotAngles`) and per-behaviour projectile motion (`stepProjectile`) for all twelve behaviours, cooldown-and-energy-gated `WeaponRuntime`, evolution evaluator generalising AF-029's pattern across six heterogeneous triggers, code-checked no-overlap fingerprint (`findWeaponOverlap`, mirroring AF-030/031); mastery reuses AF-026's `weapon:<id>` tracks, zero new acquisition systems; a sandbox weapon (Coil Ripper) replaces the placeholder cannon end-to-end (fire timing, pierce, status-on-hit through drones' own `StatusEngine`, kills confirmed feeding XP/commander/loot), browser-verified | Aligned | 249 tests green (29 new) | Approved 9.5/10 | **LOCKED** |
| AF-033 | Enemy Framework | v1 *(fifteen families, twelve roles, ten-state AI chain, ten movement behaviours, eleven attack-type labels, ten special abilities, eight death-event kinds registered; ranged attacks ARE AF-032 WeaponDefs — zero new combat model)* | Complete — spec + implementation (`src/game/enemies/`): AI states are a transition table over AF-016's existing `StateMachine`, deterministic per-behaviour motion (`stepEnemyMovement`, the one new mechanical surface), telegraph-and-cooldown-gated `EnemyRuntime` whose special-ability bonus is condition-gated (not always-active) via AF-028's exact trigger/bonus shape, `EliteModifier` producing the same `elite:boolean` flag every locked system already consumes, code-checked no-overlap fingerprint (`findEnemyOverlap`, mirroring AF-030/031/032); Death Events configure AF-021's existing unconditional `EnemyKilled` fact plus two new mechanics (Status Explosion, Spawn Event); two sandbox enemies (melee Chaser, ranged Flanker firing a real weapon) plus Elite variants govern live encounters end-to-end (AI state progression, telegraphed ranged attack, Enrage at low health, player-side status via a new player `StatusEngine`), browser-verified | Aligned | 278 tests green (29 new) | Approved 9.5/10 | **LOCKED** |
| AF-034 | Elite Enemy Framework | v1 *(seven tiers Veteran → Mythic mapped onto AF-029's restricted 7-tier rarity view; fifteen mutations registered, nine mechanically live; Elite Codex reuses AF-026's `"enemies"` collection category exactly)* | Complete — spec + implementation (`src/game/enemies/eliteData.ts`, `EliteGenerator.ts`): deterministic Base→Tier→Mutations→Stat/Behaviour/Reward Package→Identity pipeline (`generateElite`) superseding AF-033's single fixed `EliteModifier` for generated instances without editing its locked schema; mutation compatibility reuses AF-029's `exclusionGroup`, stacking reuses AF-021's `StackingRule`; elite-only numerics (regen, on-death explosion, a status a melee attacker couldn't otherwise carry) live in a new `MutationEffects` structure layered alongside `EnemyDef`, never merged into it; Retreat Logic is the first live use of AF-033's `retreat`/`recover` AI states; generated Elites spawn through AF-033's existing wave path end-to-end, browser-verified with zero errors | Aligned | 292 tests green (14 new) incl. a 1,400-combination self-review sweep | Approved 9.5/10 | **LOCKED** |
| AF-035 | Boss Framework | v1 *(seven-item phase-system vocabulary, twelve boss mechanics, five enrage triggers, six mastery-challenge kinds registered; nearly every Boss attribute — `"boss"` DamageSourceKind/XpTier/CollectionCategory, `bossPriority` selector, `MiniBoss`/`BossWave` Director phase — was already reserved by an earlier module awaiting its first producer)* | Complete — spec + implementation (`src/game/bosses/`): phase structure is a transition table over AF-016's `StateMachine` (a third consumer, after AF-016 itself and AF-033); Health/Shield/Armour reuse AF-021's `DefenceState` directly; attack telegraph/cooldown gating reuses AF-033's `EnemyRuntime` by synthesizing the minimal `EnemyDef` view it reads; ranged Boss mechanics ARE AF-032 `WeaponDef`s; Weak Points (sub-hitbox damage tracking) and deterministic arena hazard-zone ticking are the two genuinely new mechanical surfaces; rewards reuse AF-022/23/25/26/29's existing acquisition systems end-to-end; a sandbox Boss (The Hollow Sentinel) spawns on the Director's existing `MiniBoss` phase and runs a full two-phase/Enrage/hazard-zone/reward-ceremony encounter, finally giving AF-021's long-idle `bossPriority` selector a live consumer, browser-verified | Aligned | 308 tests green (16 new) incl. a 5,000-tick full-fight simulation | Approved 9.5/10 | **LOCKED** |
| AF-036 | Biome Framework | v1 *(eleven core biomes, ten environmental conditions, ten hazard kinds, ten biome events, seven weather kinds, seven interaction kinds registered; nearly every mechanism routes through a hook an earlier module had already reserved — AF-023's `smartLoot.categoryWeights`, AF-020's `"force"` MovementModifier, AF-017's `EnvironmentalEventTriggered`/`ThreatInputs.biomeModifier`/`RunSessionRecord.biomeId`, AF-019's unconsumed "Interact" action)* | Complete — spec + implementation (`src/game/biomes/`): weather rotation feeds AF-020's reserved wind/gravity `MovementModifier` kind directly; hazards reuse AF-035's exact `HazardZoneDef`/`stepHazardZone` engine; Biome Events extend AF-017's existing bus fact rather than opening a second channel; enemy buffs/immunities reuse AF-028's `EquipmentBonus` shape and AF-021's `StatusEngine.setImmunity`; discovery reuses AF-026's already-registered `"biomes"`/`"lore"` collection categories; Environmental Interaction finally consumes AF-019's long-idle "Interact" action; a sandbox biome (Crystal Fields) governs a live run end-to-end (weather, a ticking hazard, a fired weighted event, the Director's threat responding, a working interactable), browser-verified with zero errors | Aligned | 321 tests green (13 new) incl. a 5,000-tick full-session simulation | Approved 9.5/10 | **LOCKED** |
| AF-037 | Mission Framework | v1 *(sixteen mission categories, fourteen objective types, eight optional-objective kinds, ten mission modifiers, ten dynamic events registered; Mission Structure is AF-016's existing `RunPhase` lifecycle finally driven automatically; Dynamic Events are a third naming layer over AF-017/036's existing event vocabularies through the shared `EnvironmentalEventTriggered` fact)* | Complete — spec + implementation (`src/game/missions/`): deterministic modifier rolling (`generateMission`) makes the same seed guarantee every generator already makes; run-scoped objective progress (a local counter map, never routed through `MetaProgression`) reuses AF-026/035's counterKey/target pattern, with a new zero-target "stay under" semantic for No Damage-style objectives; modifiers feed AF-017's `ThreatInputs.mutatorModifier` and AF-023's `DropContext.mutatorBonus`, both hardcoded baselines since their own modules, plus a per-run `DirectorTuning` clone for Elite Squad size; `advanceRunPhaseTo()` replaces the placeholder manual "Advance Run Phase" button with real triggers (Director phase changes, boss defeat, objective completion, an extraction countdown); a sandbox mission (Crystal Fields Incursion) governs a live run end-to-end, browser-verified with zero errors | Aligned | 337 tests green (16 new) incl. a 2,000-seed self-review sweep | Approved 9.5/10 | **LOCKED** |
| AF-038 | Galaxy Framework | v1 *(ten regions, ten point-of-interest kinds, nine galaxy events, seven long-term goal kinds registered; Galaxy Regions reuse AF-010's canon factions by name — zero new lore layer; Points of Interest/Discovery reuse AF-026's `CollectionCategory` wholesale; Fast Travel gives AF-024's dormant `galaxyNavigation`/`galaxyUnlock` vocabulary its first producer; Galaxy Events are a fourth naming layer over the shared `EnvironmentalEventTriggered` fact)* | Complete — spec + implementation (`src/game/galaxy/`): route traversal + adjacency/Fast-Travel gating (`GalaxyRuntime.canTravelTo`/`travelTo`), the same weighted-pick-on-a-timer event algorithm reused a third time, and a pure `clampedDelta` helper so Sector Stability/Exploration% persist through AF-026's existing `recordStat` under namespaced `galaxy:<systemId>:*` keys with zero new save slice; one new `warp-charting` research node (`unlockFlag`, no new effect kind); a sandbox galaxy (three Crystal Dominion systems) governs the Galaxy Command hub end-to-end (real travel/discovery buttons, a Fast-Travel-gated system correctly refusing an offered transition), browser-verified with zero errors | Aligned | 350 tests green (13 new) incl. a 5,000-tick full-session simulation | Approved 9.5/10 | **LOCKED** |
| AF-039 | Faction Framework | v1 *(ten core factions registered (three fully profiled), ten faction-attribute kinds, eight reputation levels, eight conflict states, ten faction-mission kinds, eight faction events, five player-choice kinds registered; Core Factions pay off AF-010's nine-attribute content-debt note directly; Faction Rewards are a discriminated union over AF-024/025/026/028/030/031/032's existing acquisition id-spaces; Faction Events are a fifth naming layer over the shared `EnvironmentalEventTriggered` fact)* | Complete — spec + implementation (`src/game/factions/`): full `FactionDef` profiles for Crystal Dominion/Machine Collective/Human Alliance (History/Technology/Military/Culture/Economy/Territory/Unique Units/Unique Resources/Lore); `FactionRuntime` tracks pairwise Conflict-System relationships (session-scoped, mirroring `GalaxyRuntime`'s exact precedent) and the same weighted-pick-on-a-timer event algorithm reused a fourth time; Reputation persists through AF-026's `recordStat` under namespaced `faction:<id>:reputation` keys, clamped by AF-038's exact `GalaxyRuntime.clampedDelta` imported directly (zero reimplementation, zero new save slice); Faction Missions reference real AF-037 mission ids; a sandbox roster governs Galaxy Command's diplomacy layer end-to-end (Support/Oppose/Negotiate buttons moving a real persisted reputation number and level, a real inter-faction relationship reading from authored defaults, a Faction Mission queuing through the existing legal Mission Selection path and paying out reputation + a real faction reward on victory, an abandoned run correctly releasing the queued offer), browser-verified with zero errors — a mid-verification bug (a button transitioning `GalaxyCommand → Loading` directly, illegal under AF-016's locked transition table) was caught live and fixed by routing through the existing legal path instead of touching the locked table | Aligned | 369 tests green (19 new) incl. a 5,000-cycle self-review sweep | Approved 9.5/10 | **LOCKED** |
| AF-040 | Galaxy Economy Framework | v1 *(six currency types registered (one genuinely new — Credits; five are views over AF-024/025's existing values), ten merchant types, five trade actions, seven special-economic-event kinds registered; Resource Tiers reuse AF-007/023's nine-tier Rarity ladder directly; Merchant offer rewards are a discriminated union over AF-024/025/026's existing acquisition id-spaces; Special Economic Events are a sixth naming layer over the shared `EnvironmentalEventTriggered` fact)* | Complete — spec + implementation (`src/game/economy/`): `MarketRuntime` rotates a merchant's offer window via the same deterministic Fisher-Yates shuffle AF-037's `MissionGenerator` already uses for modifiers, fires the same weighted-pick-on-a-timer event algorithm a sixth time, and exposes a pure `price()` static combining AF-023 Rarity value × AF-039 Faction Reputation discount × the active Special Economic Event multiplier; Credits persist through AF-026's `recordStat` under `economy:credits` with zero new save slice, earned from six existing bus facts (elite/boss kills, research unlocks, mission/faction-mission victories, point-of-interest discovery) and spent at a live sandbox Galaxy Trader; the other five currencies are honestly read-only views today, since neither `ResearchTree` nor `CraftingSystem` expose a public decrement method to spend them without editing a locked class, browser-verified end-to-end (an unaffordable Buy correctly no-ops; an affordable Buy deducts the exact price and grants the real resource/research reward) with zero errors | Aligned | 385 tests green (16 new) incl. a 5,000-cycle self-review sweep | Approved 9.5/10 | **LOCKED** |
| AF-041 | Galaxy Event Framework | v1 *(ten Event Categories registered — four re-surface AF-017/036/038/039/040's own kinds, six are genuinely new (Sector/Ancient/Scientific Discovery/Emergency/Hidden/Legendary); ten World State keys, seven Player Participation kinds, eight Event Chain outcome kinds registered; World State clamped by AF-038's exact `GalaxyRuntime.clampedDelta` reused a third time; Dynamic Event Generation reuses the weighted-pick-on-a-timer algorithm a sixth time)* | Complete — spec + implementation (`src/game/worldEvents/`): `WorldEventRuntime` fires a deterministic weighted pick across a ten-event sandbox roster spanning all ten categories; each firing applies an ambient World State delta immediately regardless of player presence, persisted through AF-026's `recordStat` under `worldState:<key>` with zero new save slice; Player Participation (Investigate/Support/Prevent) mirrors AF-039's Player Choice discipline exactly — Ignore/Observe are zero-delta by data, never mandatory — and applies an additional delta on top, once per event instance; Event Chains are inert `{kind, contentId}` data references, with `researchOpportunity` wired to a real `researchTree.addPoints` producer; World Evolution reuses AF-026's `discover("lore", …)` wholesale; the Notification System adds one more Galaxy Command subtitle line (matching AF-038/039/040's own pattern) plus the existing `lootNotices` HUD toast; browser-verified across a real ~53-second wait for the seeded timer to elapse — a Trade Festival fired, `tradeNetworks` moved 0→10 ambiently, Investigate added a further 10→14, and the button correctly disappeared — zero errors | Aligned | 398 tests green (13 new) incl. a 5,000-cycle "galaxy years" self-review sweep | Approved 9.5/10 | **LOCKED** |
| AF-042 | Achievement and Collection Framework | v1 *(fifteen Achievement Categories, four difficulty tiers, two Completion Criteria kinds, four Challenge Cadence kinds, two new Collection categories registered; Prestige Rewards are exactly AF-026's existing nine-value `CosmeticRewardKind`; ten of fifteen Collections categories reuse AF-026's existing `CollectionCategory`; Achievement completion persists through AF-026's existing `discover("achievements", …)`)* | Complete — spec + implementation (`src/game/achievements/`): `AchievementRuntime.checkCompletions()` is a pure read over a small `AchievementProgressReader` interface `MetaProgression`'s already-public `stat()`/`snapshot.collectionCounts` satisfy without any class edit; the one deliberately new persistence surface, `CollectionLedger` (its own small save slice), tracks exactly the two Collection categories AF-026 has no bucket for (Resources, Ancient Artefacts) plus a 50-entry-capped Discovery Log with real mission/biome/sector/commander/ship context; a sandbox roster of eight achievements spans distinct categories including two Hidden Achievements ("???" until completed); the existing AF-026 Statistics screen gains two lines (Achievements, Resources/Ancient Artefacts/Recent discovery) rather than a new screen, browser-verified end-to-end — discovering the game's first Lore Entry immediately completed a hidden achievement and revealed its name, and a second Lore Entry then completed "Curator" — zero errors | Aligned | 414 tests green (16 new) incl. a 2,000-cycle "complete every achievement" self-review sweep | Approved 9.5/10 | **LOCKED** |
| AF-043 | Codex Framework | v1 *(twenty Codex categories, nine Timeline eras registered; every entry's unlock is a reference to an existing AF-026/042 discovery — zero new unlock mechanism; Discovery Rewards reuse AF-026's existing `CosmeticRewardKind`)* | Complete — spec + implementation (`src/game/codex/`): `CodexRuntime` is a pure reader providing unlock checking, Timeline ordering, validated Related-Entry resolution, Missing-Link counting, Discovery %, a real tested Search function, and Section Completion — none of it a new persistence layer; a sandbox roster of thirty entries across all twenty categories includes real content for the three sandbox factions, the sandbox boss, and several ancient/event/achievement cross-links; this module's own code contribution was finally wiring four already-registered-but-dormant collection buckets (`"ships"`/`"commanders"`/`"weapons"` at run start, `"relics"` on acquisition) and AF-039's dormant `FactionDef.loreId` (on Faction Mission victory), giving every AF-026 `CollectionCategory` a real producer for the first time; browser-verified end-to-end — discovering one Ancient Vault cascaded through the Codex and AF-042's Achievements simultaneously, unlocking two entries from a single action (Discovery % 43%→50%) with zero Missing Links and zero errors | Aligned | 435 tests green (21 new) incl. an "unlock every entry" self-review sweep reaching exactly 100% Discovery | Approved 9.5/10 | **LOCKED** |
| AF-044 | Save Framework | v1 *(Save Architecture/Account/Run/Data-Validation/Migration/Security were already substantially complete since AF-024, whose own README explicitly deferred "the full save module" to this point; Save Slots, Autosave-status bookkeeping, Major Milestone Backups, and Settings Save are the genuinely new pieces; Cloud Synchronisation needs zero new interface — `SaveStorage` already named IndexedDB/Steam Cloud/account sync as its purpose)* | Complete — spec + implementation (`src/core/save/`): `SaveProfileManager` isolates Save Profiles by prefixing the exact same `SaveStorage` interface every existing `SaveSlice` already uses (zero slice rewrites, proven by a test round-tripping a real `SaveSlice` per profile with zero cross-contamination); `SaveCoordinator` adds Autosave-status bookkeeping across six independent slices without changing any of their save triggers, plus checksum-protected Major Milestone Backups (reusing AF-024's exact `checksumOf`) taken on Account Level Up; a new, deliberately profile-independent Settings slice gives AF-041's dormant Reduced Notification Mode a real toggle and behavioural effect; browser-verified end-to-end — toggling a setting and reloading the page fully preserved both the setting and the auto-created Primary Profile, the concrete guarantee this module exists to make, with zero errors | Aligned | 454 tests green (19 new) incl. a 2,000-cycle multi-profile self-review sweep | Approved 9.5/10 | **LOCKED** |
| AF-045 | Audio Framework | v1 *(nineteen Audio Categories, seven Mixer Channels, twelve Adaptive Music states registered; no audio asset pipeline exists yet, so the engine runs behind a pluggable `AudioBackend` — the exact pattern AF-024's `SaveStorage` established; Mixing seeds from AF-044's existing three-slider audio settings; every sandbox cue answers a real, already-existing bus fact or call site — zero new gameplay signaling)* | Complete — spec + implementation (`src/game/audio/`): `AudioMixer` (seven independent channels, master-scaling, mute propagation, `fromSettings`/`syncFromSettings`) and `VoicePool` (per-category simultaneous-voice cap with priority-based eviction, satisfying both Positional Audio's Priority Mixing and Performance's "limit simultaneous voices") back a swappable-backend `AudioEngine`; `resolveMusicState()` is a pure function over AF-016/017/035's existing game/Director/boss state, and `AudioEngine.setMusicState()` is idempotent so transitions are structurally seamless; all six §Player Feedback examples (Critical Hit/Shield Break/Level Up/Legendary Drop/Boss Spawn/Mission Complete) plus Research/Craft/Achievement cues are wired to real facts, three of which (`ShieldBroken`/`CommanderLevelUp`/`LootDropped`) had zero subscribers anywhere in the codebase until now; browser-verified — entering Gameplay correctly moved Adaptive Music from `galaxyCommand` to `exploration`, and the Debug overlay's live master mixer level matched AF-044's own default settings — zero errors | Aligned | 484 tests green (30 new) incl. a 5,000-cycle Voice Pool pressure sweep | Approved 9.5/10 | **LOCKED** |
| AF-046 | Human Outlaw Enemy Framework | v1 *(fourteen Core Unit kinds, eight tactics, eight special mechanics, six command orders, six mini-boss kinds, seven elite perks registered; every unit is a plain AF-033 `EnemyDef` passing the no-overlap law, with real AF-032 `WeaponDef` ranged attacks; the faction IS AF-039's registered-but-unprofiled Mercenary Guild, now the fourth full profile; Formation Flying gives AF-033's reserved-and-never-used `formationAnchor`/`formationOffset` movement context its first producer)* | Complete — spec + implementation (`src/game/enemies/outlawData.ts`, `OutlawSquad.ts`): five sandbox defs (Raider missile swarms, Sniper rail lance, Shield Carrier, Mine Layer, Captain with suppressive fire); `OutlawSquadRuntime` — the one genuinely new surface — runs forming→coordinated→scattered→eliminated Command Structure: Focus Fire only while the Captain lives, wedge Formation Flying on the Captain, and captain-death scattering the squad into AF-033's existing `retreat` state; Elite Captains ARE AF-034 Elites via the unchanged spawn path, with callsign presentation; Deployable Mines reuse AF-035's exact hazard engine with orange warning-ring rendering; squads enter via the Director's existing `AmbushEvent` wave (no Director changes); squad elimination improves Sector Stability (AF-038 `clampedDelta` + AF-026 `recordStat`); first Captain kill unlocks the Guild's additive AF-043 Codex entry; browser-verified — a real HeavyCombat `AmbushEvent` spawned the squad, forming→coordinated ran live on the debug overlay, and the squad killed the piloted player (dangerous through tactics, as specified) with zero errors; browser verification caught one real bug (mine-drop `phase` gate) fixed before lock; live mines subsequently observed (`mines 1`) via AF-047's dev spawn keys | Aligned | 501 tests green (17 new) incl. a 1,000-encounter randomised kill-order sweep | Approved 9.5/10 | **LOCKED** |
| AF-047 | Machine Collective Enemy Framework | v1 *(fourteen unit kinds, eight combat styles, eight special mechanics, seven Adaptive AI inputs, six network commands, six mini-boss kinds, seven elite gains registered; every unit is a plain AF-033 `EnemyDef` passing the no-overlap law against the base roster AND the Outlaws; the faction was already fully profiled by AF-039 — zero new lore; AF-033's dormant `healAllies` and `spawnReinforcements` ability kinds gain their first producers)* | Complete — spec + implementation (`src/game/enemies/machineData.ts`, `MachineNetwork.ts`): six sandbox defs (Combat Drone, Sniper Unit, Shield Generator, Repair Drone, Swarm Constructor, Elite Command Core); `MachineNetworkRuntime` — the one genuinely new surface and the designed mirror-opposite of AF-046 — DEGRADES instead of scattering: killing the Core drops Target Synchronisation, Shared Shields (×0.85 lattice), Self Repair, and the Drone Factory at once while the machines keep fighting, and killing an individual service unit drops exactly that service; Adaptive AI analyses incoming damage per school and builds resistance in 5% steps hard-capped at 25% ("adapts without becoming unfair" is a constant, not a promise), collapsing when the Core dies; the Factory manufactures Combat Drones mid-fight through the shared spawn path, enrolling them into the network and the Director's census; networks enter via the Director's existing `ReinforcementWave` (no Director changes); elimination banks Research Data through AF-024's exact `bankResearchSample` pattern; two live self-review tunings recorded in code (factory interval 7000→4000ms; Constructor kiting→retreat); browser-verified via new DEV-only spawn keys — all services live, adaptation counters rising under real fire (p3→p17), per-service degradation on unit deaths, and a real mid-fight drone built and enrolled (`6 units, built 1`), zero errors | Aligned | 519 tests green (18 new) incl. a 1,000-encounter randomised kill-order sweep | Approved 9.5/10 | **LOCKED** |
| AF-048 | Crystal Ascendancy Enemy Framework | v1 *(thirteen unit kinds, eight combat styles, ten special mechanics, six Resonance shared traits, seven Environmental Control actions, six mini-boss kinds, seven elite gains registered; every unit is a plain AF-033 `EnemyDef` passing the no-overlap law against the base roster, Outlaws, AND Machines; the faction IS AF-039's already-profiled Crystal Dominion — zero new lore; Crystal Growth extends AF-035's exact hazard-zone engine with a new pure growth-step function, its first mechanical extension)* | Complete — spec + implementation (`src/game/enemies/crystalData.ts`, `CrystalResonance.ts`): six sandbox defs (Crystal Drone, Shard Hunter, Resonance Node, Growth Seeder, Crystal Guardian, Elite Crystal Titan); `CrystalResonanceRuntime` — the one genuinely new surface, and the third distinct faction doctrine — WEAKENS CONTINUOUSLY instead of scattering (AF-046) or degrading in discrete steps (AF-047): no state machine at all, just a resonance-strength value proportional to living Resonance Node count, hard-capped at 40% for fairness exactly like AF-047's Adaptive AI pattern; three Resonance shared traits are mechanically live — healing, outgoing damage bonus, and movement speed all scale continuously with strength, composing into the same damage-multiplier point as Focus Fire/Target Synchronisation; "destroying resonance nodes weakens nearby organisms" recomputes on every node kill, verified live (8%→0% on a single node kill); Crystal Growth reuses AF-035's `HazardZoneDef`/`stepHazardZone` unchanged plus one new pure `growCrystalZone` step function that grows a zone's radius over its life, seeded by the Growth Seeder on its own cadence and capped for performance; ecosystems enter via the Director's existing `MixedEncounter` wave (no Director changes, extending the established AmbushEvent/ReinforcementWave precedent to a third wave identity); a Crystal Titan runs through AF-034's Elite pipeline unchanged; first Resonance Node kill unlocks the faction's additive AF-043 Codex entry; browser-verified via a new DEV-only spawn key ("7") — a live ecosystem spawned (6 organisms, 1 node, 8% strength), a Resonance Node kill collapsed strength to 0% immediately, and the Growth Seeder seeded two live, ticking, growing crystal hazard zones mid-fight, zero errors | Aligned | 535 tests green (16 new) incl. a 1,000-encounter randomised kill-order sweep | Approved 9.5/10 | **LOCKED** |
| AF-049 | Void Swarm Enemy Framework | v1 *(thirteen unit kinds, eight combat styles, ten special mechanics, six Void Network traits, seven Reality Distortion actions, six mini-boss kinds, seven elite gains registered; every unit is a plain AF-033 `EnemyDef` passing the no-overlap law against the base roster, Outlaws, Machines, AND Crystals, guaranteed by the dormant `voidEntity` family's first use since AF-033; the Swarm is explicitly "not a civilisation" per its own spec, so — unlike every prior faction — it gets no AF-039 `FactionDef` at all; AF-033's dormant `split`/`merge`/`cloak` special-ability kinds and `teleport` movement behaviour gain their first EnemyDef producers)* | Complete — spec + implementation (`src/game/enemies/voidData.ts`, `VoidCorruption.ts`): six sandbox defs (Void Wisp, Corruption Parasite, Shadow Hunter, Void Beacon, Rift Guardian, Elite Ancient Void Avatar); `VoidCorruptionRuntime` — the one genuinely new surface and the fourth distinct faction doctrine — CORRUPTS instead of scattering (AF-046), degrading in steps (AF-047), or continuously weakening from a count (AF-048): corruption climbs over time in proportion to live Void Beacon count, decays only once every Beacon is destroyed (containment, not a snap), and steps down immediately on any single Beacon kill; three Void Network traits are mechanically live — healing, damage amplification, and a corruption-scaled incoming-damage reduction ("Reality Stability"), composing into the same multiplier point as every prior faction's bonus; Corruption Zones reuse AF-035's exact hazard-zone engine a third time, the first enemy-faction hazard to carry a live status effect (the already-registered AF-021 "corruption" `StatusKind`) via `statusOnTick`; swarms enter through a new listener on AF-017's existing `EnvironmentalEventTriggered`/`VoidDistortion` fact — literally zero `EnemyDirector` changes, not even a wave-type reuse; a Void Avatar runs through AF-034's Elite pipeline unchanged; first Beacon kill unlocks the additive AF-043 Codex entry; browser-verified via a new DEV-only spawn key ("6") — a live swarm's corruption climbed continuously from 1% to 37%+ under real combat with zero page errors, tracking the tuned growth rate exactly | Aligned | 553 tests green (18 new) incl. a 1,000-encounter randomised kill-order sweep | Approved 9.5/10 | **LOCKED** |
| AF-050 | Ancient Custodian Enemy Framework | v1 *(fourteen unit kinds, eight combat styles, ten special mechanics, six Ancient Network traits, six mini-boss kinds, seven elite gains, five security stages registered; every unit is a plain AF-033 `EnemyDef` passing the no-overlap law against the base roster, Outlaws, Machines, Crystals, AND Void; the sixth of AF-039's ten registered `FactionId`s to be profiled — paying off `ancientCustodians`' content debt exactly as AF-046 paid off the Mercenary Guild's; AF-021's dormant `armourBreak` `StatusKind` and the dormant `ancient` `WeaponCategory` gain their first producers)* | Complete — spec + implementation (`src/game/enemies/ancientData.ts`, `AncientSecurity.ts`): six sandbox defs (Sentinel, Defence Drone, Guardian Sphere, Shield Architect, Custodian Walker, Elite Ancient Executor); `AncientSecurityRuntime` — the one genuinely new surface and the fifth distinct faction doctrine, and the first to run in the MIRROR-OPPOSITE direction of every prior one: a five-stage security ladder (Minor Trespass → Warning → Containment → Guardian Deployment → Maximum Response) that ESCALATES the longer the player trespasses near a site and continuously DE-ESCALATES the moment they leave — regardless of how many Custodians remain — while a permanent ceiling, derived from surviving Shield Architects, caps how far it can ever climb again once a network node falls; three Ancient Network traits are mechanically live — Repair Functions, Target Information, and Shield Capacity, each stepped discretely by stage and composing into the same multiplier point every prior faction's bonus already uses; Guardian Deployment (stage 3+) gets real teeth via a capped, cadence-gated reinforcement reusing AF-047's Drone Factory pattern; sites enter through a new listener on AF-017's existing `EnvironmentalEventTriggered`/`AncientSignal` fact — zero `EnemyDirector` changes, following AF-049's zero-wave-type precedent; an Ancient Executor runs through AF-034's Elite pipeline unchanged; a full `FactionDef` profile (territory `ancientCore`, resource `ancientComponents`) plus one additive `codex-ancient-security-doctrine` Codex entry (careful to avoid an id collision with a pre-existing, older `codex-ancient-custodians` Ancient-Vault-discovery entry) unlock on the first Shield Architect kill; browser-verified via a new DEV-only spawn key ("5") — a live site's alert ceiling visibly and immediately collapsed to 0% the instant its Shield Architect died, confirmed on two independent runs, zero page errors | Aligned | 571 tests green (18 new) incl. a 1,000-encounter randomised kill-order sweep | Approved 9.5/10 | **LOCKED** |
| AF-051 | Bio-Engineered Xenomorph Enemy Framework | v1 *(fourteen unit kinds, eight combat styles, ten special mechanics, six Hive Network traits, seven biological terrain kinds, six mini-boss kinds, seven elite gains, five evolution stages registered; every unit is a plain AF-033 `EnemyDef` passing the no-overlap law against the base roster and all five prior factions, guaranteed by the dormant `swarm` family's first use since AF-033; like AF-049's Void Swarm, the Hive is explicitly "not a civilisation" so it gets no AF-039 `FactionDef`; AF-021's dormant `poison` status and AF-033's dormant `burrow`/`wallCrawling` movement behaviours gain their first producers)* | Complete — spec + implementation (`src/game/enemies/xenoData.ts`, `HiveEvolution.ts`): six sandbox defs (Hive Drone, Spitter, Stalker, Evolution Node, Crusher, Elite Living Titan); `HiveEvolutionRuntime` — the one genuinely new surface and the sixth distinct faction doctrine, and the first whose core meter has NO way down at all: Biomass climbs from encounter duration and from every death — hive or otherwise — and never decreases, ever; Evolution Stage is a pure five-stage ratchet over it, hard-capped for fairness like every prior doctrine's number; the one thing that CAN be lost tactically is the Hive Node's network link (reusing AF-047's discrete on/off flag technique) — severing it immediately zeroes the three mechanically-live, stage-stepped Hive Network traits (Healing, Target Information, Aggression) for whatever survives the encounter, without ever touching Biomass itself; Rapid Reinforcement (stage 2+, linked) manufactures a real Hive Drone through the shared spawn path, a fifth reuse of AF-047's Drone Factory cadence pattern; Acid Pools reuse AF-035's exact hazard engine a fourth time, carrying the newly-live `poison` status; hives enter through the Director's existing `HunterPack` wave type (a hunting pack fits Swarming/Flanking/Overwhelming Numbers almost too literally); a Living Titan runs through AF-034's Elite pipeline unchanged; one additive `codex-xenomorph-hive` Codex entry (cross-referencing AF-049's Void Swarm as the galaxy's other non-civilisation threat) unlocks on the first Evolution Node kill; browser-verified via a new DEV-only spawn key ("4") — a live hive's biomass climbed from 0% through the "adapting" stage threshold at 20%, and the Node's death was reflected as an immediate link-severance without halting biomass growth, zero page errors | Aligned | 592 tests green (21 new) incl. a 1,000-encounter randomised kill-order sweep | Approved 9.5/10 | **LOCKED** |
| AF-052 | Stellar Nomad Enemy Framework | v1 *(fourteen unit kinds, eight combat styles, ten special mechanics, six Fleet Coordination actions, six mini-boss kinds, seven elite gains registered; every unit is a plain AF-033 `EnemyDef` passing the no-overlap law against the base roster and all six prior factions; the seventh of AF-039's ten registered `FactionId`s to be profiled — paying off `nomadFleet`'s content debt exactly as AF-046 did for the Mercenary Guild and AF-050 for the Ancient Custodians; AF-021's dormant `stasis` status and AF-032's dormant `returning` projectile behaviour gain their first producers)* | Complete — spec + implementation (`src/game/enemies/nomadData.ts`, `NomadFleet.ts`): six sandbox defs (Scout Skiff, Hunter, Escort Fighter, Junker Gunship, Repair Frigate, Elite Nomad Flagship); `NomadFleetRuntime` — the one genuinely new surface and the seventh distinct faction doctrine, and the first driven by an ACTIVELY-SPENT economy rather than a passive multiplier: Scrap is earned passively (Salvage Recovery) and spent on three discrete tactical actions — Deployable Turrets, Scrap Shields, Emergency Repairs — each gated on BOTH affordability and its own cooldown; destroying the Command Ship (the Flagship itself, doubling as both AF-034 Elite leader and fleet command) doesn't touch any banked value, it only throttles future income ("disrupts fleet cohesion"), a fresh mechanical shape none of AF-046 → AF-051 used; Target Priority (a continuous, Scrap-derived damage bonus) and Escort Protection (a continuous, PURELY headcount-derived incoming-damage reduction, fully independent of Scrap) are the two mechanically-live Fleet Coordination traits, composing into the established multiplier/reduction points; fleets enter through the Director's existing `AmbientPatrol` wave type; a full `FactionDef` profile (territory `darkNebula`, resource `commonMaterials`) plus one additive `codex-nomad-fleet` Codex entry unlock on the first Command Ship kill; browser-verified via a new DEV-only spawn key ("3") — a live fleet's Scrap climbed from 1 to 8 at the tuned rate while an Escort Fighter's death was tracked distinctly from a regular crew loss, zero page errors | Aligned | 610 tests green (18 new) incl. a 1,000-encounter randomised kill-order sweep | Approved 9.5/10 | **LOCKED** |
| AF-053 | Paragon Protocol Enemy Framework | v1 *(thirteen unit kinds, eight combat styles, ten special mechanics, seven Adaptive Technology inputs, six mini-boss kinds, seven elite gains registered; every unit is a plain AF-033 `EnemyDef` passing the no-overlap law against the base roster and all seven prior factions; like AF-049's Void Swarm and AF-051's Xenomorph Hive, the Protocol belongs to no civilisation so it gets no AF-039 `FactionDef`; AF-021's dormant `overload` status and AF-032's dormant `gravityAffected` projectile behaviour gain their first producers)* | Complete — spec + implementation (`src/game/enemies/paragonData.ts`, `ParagonInstability.ts`): six sandbox defs (Prototype Drone, Pulse Cannon, Adaptive Hunter, Containment Sentinel, Energy Construct, Elite Omega Prototype); `ParagonInstabilityRuntime` — the one genuinely new surface and the eighth distinct faction doctrine, and the first driven by a SINGLE IRREVERSIBLE THRESHOLD EVENT rather than a smooth curve: Reactor Stability depletes from incoming damage (Adaptive Technology's live input, the same `recordIncomingDamage` shape AF-047's Adaptive AI first established) and its own inherent instability, repaired only while a Containment Sentinel lives; the instant it reaches zero, Containment Collapse fires exactly once, permanently inverting the unit from defended-but-fragile (Adaptive Shields, degrading pre-collapse) to unshielded-but-far-more-dangerous (Energy Overload + Unstable Reactors, a large permanent damage/speed spike) — no prior doctrine reverses direction this way; a Sentinel's death removes future repair without touching Stability's current value, echoing AF-052's "kill changes a rate, not a value" shape on a depleting-toward-catastrophe meter instead of an accumulating currency; Containment Collapse seeds one real Singularity Charge hazard through AF-035's exact engine, a fifth reuse; protocols enter through a new listener on AF-017's existing `EnvironmentalEventTriggered`/`GravityFlux` fact — zero `EnemyDirector` changes; an Omega Prototype runs through AF-034's Elite pipeline unchanged; one additive `codex-paragon-protocol` Codex entry (cross-referencing AF-051's Xenomorph Hive as another non-civilisation threat) unlocks on the first Sentinel kill; browser-verified via a new DEV-only spawn key ("2") — a live protocol's Reactor Stability visibly dropped from 95% to 67% under real combat damage exactly tracking the tuned loss rate, zero page errors | Aligned | 628 tests green (18 new) incl. a 1,000-encounter randomised kill-order sweep | Approved 9.5/10 | **LOCKED** |
| AF-054 | Celestial Conclave Enemy Framework | v1 *(thirteen unit kinds, eight combat styles, ten special mechanics, six Celestial Network traits, eight Cosmic Manipulation actions, six mini-boss kinds, six elite gains registered; every unit is a plain AF-033 `EnemyDef` passing the no-overlap law against the base roster and all eight prior factions; like AF-049/051/053, the Conclave is ancient cosmic consciousness, not a civilisation, so it gets no AF-039 `FactionDef`; AF-021's dormant `shieldBreak` status gains its first producer)* | Complete — spec + implementation (`src/game/enemies/celestialData.ts`, `CelestialConstellation.ts`): six sandbox defs (Solar Spark, Pulsar Hunter, Gravity Oracle, Constellation Avatar, Corona Guardian, Elite Living Supernova); `CelestialConstellationRuntime` — the one genuinely new surface and the ninth distinct faction doctrine, and the first whose bonus is LOCAL rather than global: a Constellation is a graph of specific pairwise links (not a hub-and-spoke network), and each living entity's Solar Energy (damage), Shield Strength (incoming-damage reduction), and Healing bonuses are computed from ITS OWN surviving link count — two members can carry different bonus magnitudes at the same instant, something no prior faction's single shared value could produce; "destroying anchor entities destabilises nearby formations" needed no special-cased anchor flag at all — the Constellation Avatar is simply the graph's highest-degree point by construction, so its death costs every neighbour a link simultaneously, a cascade emerging purely from the graph shape; Gravity Wells reuse AF-035's exact hazard engine a sixth time; formations enter through a new listener on AF-017's existing `EnvironmentalEventTriggered`/`SolarFlare` fact — zero `EnemyDirector` changes; a Living Supernova runs through AF-034's Elite pipeline unchanged; one additive `codex-celestial-conclave` Codex entry (cross-referencing AF-053's Paragon Protocol as another non-civilisation threat) unlocks on the first Avatar kill; browser-verified via a new DEV-only spawn key ("1") — a live formation's Avatar died to real combat within the first observation window, and its former neighbours' link counts visibly dropped to exactly what the graph model predicts (0 for pure spokes, 1 for the two units sharing an extra edge), a real cascade caught live, zero page errors | Aligned | 645 tests green (17 new) incl. a 1,000-encounter randomised kill-order sweep | Approved 9.5/10 | **LOCKED** |
| AF-055 | Eclipsed Enemy Framework | v1 *(thirteen unit kinds, eight combat styles, ten special mechanics, five corruption stages, five memory-echo kinds + six deterministic echo lines, six mini-boss kinds, six elite gains registered; every unit is a plain AF-033 `EnemyDef` passing the no-overlap law against the base roster and all nine prior factions; the fifth faction with no AF-039 `FactionDef` — but as former humans, not a cosmic force, its Codex entry points at the Human Alliance it fell from rather than at another threat; AF-033's dormant `ambush` movement behaviour and the `teleport` special-ability kind gain their first base-def producers)* | Complete — spec + implementation (`src/game/enemies/eclipsedData.ts`, `EclipsedCorruption.ts`): six sandbox defs (Lost Scout, Broken Pilot, Echo Drone, Memory Warden, Fallen Guardian, Elite Eclipsed Champion); `EclipsedCorruptionRuntime` — the one genuinely new surface and the tenth distinct faction doctrine, a MIRROR rather than a force, new in two independent ways: (1) corruption is a PERSONAL timeline — every member walks its own five-stage fall (Recently Lost → Corrupted → Broken → Consumed → Irrecoverable), staggered at spawn, monotonic and capped, slowed while the Memory Warden lives, and jumped forward by grief on every ally death, so killing the group faster pushes the survivors further from themselves and the kill-the-support reflex finally costs something; (2) Ability Mimicry is the first mechanic whose strength reads what the player has BECOME — the player's progression level feeds the mirror's damage share, never regressing, hard-capped for fairness (AF-050 read only the player's position); Memory Echoes reuse AF-053's one-shot consume-event shape as cadence-gated, lifetime-capped, deterministic world-building text; the Director's `EliteSquad` wave becomes their entrance, giving ElitePressure a face — every Eclipsed was once a Commander, and every AF-034 Elite in Director play now enters as a faction leader; browser-verified via a new DEV-only spawn key ("0", completing the digit row) across two live runs — staggered stages from the first frame, same-tick grief jumps on the Warden's death and a later member's, all five stage names reached up to `irrecoverable`, a Memory Echo at exactly the 7s tuned cadence, the mirror reading the player's real level, zero page errors | Aligned | 663 tests green (18 new) incl. a 1,000-encounter randomised kill-order sweep | Approved 9.5/10 | **LOCKED** |
| AF-056 | Enemy Director Framework | v1 *(nine responsibilities, ten encounter types, six pacing pressures, five recovery triggers, seven adaptive inputs, six faction-mix kinds registered; the unique alignment case of the series — AF-017's Director already exists and is LOCKED, so AF-056 is fulfilled as a CONDUCTOR layer over its `onDirective` seam with `EnemyDirector.ts`/`directorTuning.ts` byte-for-byte unmodified; the total `WaveType → EncounterType` mapping formalises AF-046 → AF-055's ten faction routings as the encounter catalogue)* | Complete — spec + implementation (`src/game/director/conductorData.ts`, `DirectorConductor.ts`): `DirectorConductor<TDirective>` — the one genuinely new surface, and the first whose subject is TIME rather than a faction: reactive Recovery Windows open after all five specified triggers (elite battles and boss phases via the existing `EnemyKilled` flags, major events via `EnvironmentalEventTriggered`, large waves via a new `notifyWaveLanded` call at both spawn sites, resource discoveries via the existing legendary+ `LootCollected` branch), and while one is open ordinary directives wait in a FIFO Spawn Queue — capped, with overflow flushing oldest, so pressure is deferred but never deleted and recovery windows cannot soft-lock the Director; boss directives bypass the gate (boss timing stays AF-017/035's); window duration adapts to a struggle score from the two live Adaptive Response inputs (damage taken + player health) between a 2s floor and a 9s hard cap — "adaptation adjusts pacing, not hidden difficulty" is structural, since no conductor code path can reach a stat or budget; the six pacing pressures are a pure derivation from the Director's own phase + window state; dual-faction moments emerge when the queue flushes two faction directives in one tick; held encounters are named on-screen (accessibility), and the conductor holds zero RNG; browser-verified across three live runs — a `largeEnemyWaves` window opening the moment the first real faction wave landed, BOTH adaptive extremes observed (2.0s window at struggle 0%, ~9s at 100%), struggle live-tracking real damage 0→100%, an `eliteBattles` window on a real elite kill, and a real Director directive held at `queue 1` inside it, zero page errors | Aligned | 678 tests green (15 new) incl. a real-EnemyDirector integration run and a 5,000-mission randomised sweep | Approved 9.5/10 | **LOCKED** |
| AF-057 | Boss Director Framework | v1 *(twelve encounter beats, nine phase alterations, seven arena features, seven summon kinds, seven cinematic beats, seven legendary moments, eight memory keys, seven multi-boss kinds registered; like AF-056, the subject already exists and is LOCKED — AF-035's `BossRuntime`/`BossArena`/`bossData` remain byte-for-byte unmodified, decorated via notifications and snapshot polling)* | Complete — spec + implementation (`src/game/bosses/bossDirectorData.ts`, `BossDirector.ts`): `BossDirectorRuntime` — the one genuinely new surface, the second consecutive orchestration layer: `beatFor` derives the twelve-beat flow purely from the runtime's own state (no second state machine); phase changes open breathing room during which `attacksHeld` gates the boss's own `tryAttack` (recovery through repositioning, never healing), evolve the arena (the existing AF-035 Collapse hazard grows by a pure per-phase multiplier — AF-048's `growCrystalZone` shape), and enqueue a data-driven summon plan that drains ONE spec per cadence through the shared spawn path (elite guards via AF-034's unchanged flag, counts into AF-017's census, capped, cleared on defeat); cinematic one-shots reuse AF-053's consume-event shape a third time; Boss Memory persists attempts/victories/fastest-kill through AF-026's stats via the pure improve-only `fastestKillStatDelta`; the Reward Ceremony gains paced memory lines additively over the untouched `grantBossRewards`; no director code path can inflate a stat, and the director holds zero RNG; verified end-to-end against a REAL Hollow Sentinel `BossRuntime` in an integration test (phase change detected → fire measurably held → both summons in plan order at cadence → victory cinematic); one honest AF-046-style gap recorded — the live in-browser fight wasn't reached this session (piloted player died at 14s/22s of the ~98s journey to the vault; wiring loads live with zero page errors), binding at the next session that reaches it | Aligned | 691 tests green (13 new) incl. a real-BossRuntime integration fight and a 5,000-encounter randomised sweep | Approved 9.5/10 | **LOCKED** |
| AF-058 | Human Frontier Biome | v1 *(ten locations, seven weather names, seven hazard kinds, eight mission types, eight resources, eight POIs, eight events, eight discoveries, five boss kinds registered — the weather and event names carry TOTAL mappings onto AF-036's locked shelves; a pure CONTENT module, the first full biome authored on the unchanged AF-036 engine)* | Complete — spec + implementation (`src/game/biomes/frontierBiome.ts`): `HUMAN_FRONTIER_BIOME` is a plain `BiomeDef` — two AF-035 hazard zones (Debris Belt, burning Reactor Leak), three weathers with real AF-020 wind forces, six weighted events, AF-023 smart-loot weights biased to early progression, nine enemy ids spanning Outlaws/Nomads/Machines/Eclipsed/Ancients (resolution-tested across all eleven rosters), three interactables including the civilian-archive lore discovery; deliberately gentle: threatModifier 0.9, `enemyBuff: null`, no hazard immunities — all asserted, not intended; **AF-038's `StarSystemDef.biomeId` gets its first consumer** — a `BIOME_REGISTRY` resolves the run's biome from the current system at `startRun`, and a new `humanFrontier` region + Meridian Rest system join the sandbox galaxy additively (threat 1, Human Alliance, reachable without Fast Travel); one additive `codex-biome-human-frontier` entry unlocks via the archive interactable; browser-verified end-to-end — real Galaxy Command travel to Meridian Rest, launch, and the frontier ran live (`Meridian Rest · weather ionClouds · hazards 2`), zero page errors, player survived the full window | Aligned | 703 tests green (12 new) incl. real GalaxyRuntime travel + a 1,000-mission seeded sweep | Approved 9.5/10 | **LOCKED** |
| AF-059 | Crystal Expanse Biome | v1 *(ten locations, seven weather names, eight hazard kinds, eight mission types, eight resources, eight POIs, eight events, eight discoveries, five boss kinds registered with TOTAL mappings onto AF-036's locked shelves — AF-058's exact content-module shape applied to the Ascendancy's birthplace)* | Complete — spec + implementation (`src/game/biomes/crystalExpanseBiome.ts`): `CRYSTAL_EXPANSE_BIOME` is a plain `BiomeDef` — a shocking Resonance Field + Shardfall Canyon (AF-035 zones), Crystal Rain/Prismatic Storms/Crystal Mist (AF-020 forces), six weighted events, advanced-progression loot weights; "peaceful while remaining highly dangerous" is numbers: threatModifier 1.15, resonance-hardened natives via AF-036's live `enemyBuff` hook (shieldCapacity +10), shock immunity — all asserted; enemy presence is the spec's ecosystem (six Ascendancy organisms primary, Custodians, occasional Void, rare Machines, all resolution-tested); "every environment constantly grows" is ALREADY mechanically live via AF-048's Growth Seeder + `growCrystalZone` — recorded as the owning link, deliberately not re-implemented; `bossId` resolves to the real Hollow Sentinel (an ancient guardian in a temple — the coexistence made playable); Prismheart joins the galaxy additively (crystalDominion, threat 3) as the second consumer of AF-038's `biomeId` through AF-058's registry; one additive `codex-biome-crystal-expanse` entry unlocks via the resonance well; browser-verified end-to-end — real travel to Prismheart, launch, the Expanse live on the overlay (`Prismheart · weather solarStorms · hazards 2`), zero page errors | Aligned | 714 tests green (11 new) incl. real GalaxyRuntime travel + a 1,000-mission seeded sweep | Approved 9.5/10 | **LOCKED** |
| AF-060 | Machine Expanse Biome | v1 *(ten locations, seven weather names, eight hazard kinds, eight mission types, eight resources, eight POIs, eight events, eight discoveries, five boss kinds registered with TOTAL mappings onto AF-036's locked shelves — the AF-058/059 content-module shape applied to the Collective's industrial heart)* | Complete — spec + implementation (`src/game/biomes/machineExpanseBiome.ts`): `MACHINE_EXPANSE_BIOME` is a plain `BiomeDef` — THREE AF-035 hazard zones (a shocking Laser Grid, a burning Molten Channel, a heavy Press Line: the factory itself is the danger, the most hazards of any authored biome), Electrical Storms/EMP Waves/Magnetic Winds (AF-020 forces incl. real wind + reduced visibility), four weighted events, engineering-progression loot weights; "nothing here exists naturally" is numbers: threatModifier 1.25 (asserted strictly above AF-059's 1.15 — the deepest authored biome), factory-maintained natives via AF-036's live `enemyBuff` hook (shieldCapacity +12), and dual shock+burn `hazardImmunities` — the foundry does not harm its own machines; enemy presence follows the spec (full six-def AF-047 Collective roster primary in its home, Paragon prototypes, an Ancient watcher, occasional Outlaws, rare Void, all resolution-tested); a security-node interactable uses the `disableHazard` interaction kind — infiltration made literal; `bossId: null` is honest (Adaptive War Platform already AF-047 mini-boss vocabulary; forge bosses bind as `BossDef`s when authored); Forge Primus joins the galaxy additively (new machineExpanse region, threat 4, reached through Hollow Drift — two real travel hops) as the third consumer of AF-038's `biomeId` through AF-058's registry; one additive `codex-biome-machine-expanse` entry unlocks via the AI archive; browser-verified end-to-end — two real travel hops, launch, the Forge live on the overlay (`Forge Primus · weather ionClouds · hazards 3`), zero page errors | Aligned | 725 tests green (11 new) incl. real GalaxyRuntime travel + a 1,000-mission seeded sweep ("the factory never stops working") | Approved 9.5/10 | **LOCKED** |
| AF-061 | Void Expanse Biome | v1 *(ten locations, seven weather names, eight hazard kinds, eight mission types, eight resources, eight POIs, eight events, eight discoveries, five boss kinds registered with TOTAL mappings onto AF-036's locked shelves — the AF-058/059/060 content-module shape applied to the edge of existence)* | Complete — spec + implementation (`src/game/biomes/voidExpanseBiome.ts`): `VOID_EXPANSE_BIOME` is a plain `BiomeDef` — THREE AF-035 hazard zones expressing unstable physics through owned engines (a statusless heavy Gravity Well, a Reality Tear leaking AF-021's corruption status — AF-049's signature — and a Temporal Field applying AF-052's stasis root: stolen seconds), Void Storms/Dark Matter Clouds/Gravitational Waves (AF-020 forces incl. the strongest wind + reduced visibility of any biome), four weighted events led by voidBreach, endgame loot weights (researchSample/relic/ancientArtifact highest of any biome); "the edge of existence" is numbers: threatModifier 1.35 (asserted strictly above AF-060's 1.25 — the deepest authored biome), natives hardened via AF-036's live `enemyBuff` hook (shieldCapacity +14) and corruption-immune — the Void does not corrupt what is already its own; enemy presence follows the spec (full six-def AF-049 Void Swarm roster primary, three Eclipsed, two Custodians, rare Celestial spark, occasional Machine expedition, all resolution-tested across five rosters); a Reality Anchor uses `triggerEvent` ("Stabilise Reality" made literal) and a Hidden Gateway gives `unlockSecret` its FIRST producer since AF-036 registered it; `bossId: null` is honest (Rift Guardian + Void Avatar already AF-049 vocabulary; void bosses bind as `BossDef`s when authored); Hollow Crown joins the galaxy additively (new voidExpanse region, threat 5 — the highest in the galaxy — past even Forge Primus: three real travel hops) as the fourth consumer of AF-038's `biomeId` through AF-058's registry; one additive `codex-biome-void-expanse` entry unlocks via the void archive; browser-verified end-to-end — three real travel hops, launch, the Void live on the overlay (`Hollow Crown · weather voidLightning · hazards 3`, `galaxy Hollow Crown (voidExpanse)`), zero page errors | Aligned | 736 tests green (11 new) incl. real GalaxyRuntime travel + a 1,000-mission seeded sweep ("reality never stops collapsing") | Approved 9.5/10 | **LOCKED** |
| AF-062 | Ancient Core Biome | v1 *(ten structures, seven weather names, eight hazard kinds, eight mission types, eight resources, eight POIs, eight events, eight discoveries, five boss kinds registered with TOTAL mappings onto AF-036's locked shelves — the AF-058→061 content-module shape applied to the precursors' preserved civilisation)* | Complete — spec + implementation (`src/game/biomes/ancientCoreBiome.ts`): `ANCIENT_CORE_BIOME` is a plain `BiomeDef` — THREE AF-035 hazard zones that feel INTENTIONAL rather than natural per the spec (a Security Field stripping shields via AF-054's shieldBreak status — its first biome-hazard use — a heavy statusless precision Defence Laser, a Temporal Lock applying AF-052's stasis root), Solar Streams/Energy Rain/Stellar Winds (AF-020 forces) with ZERO visibility reduction asserted per-def — technological perfection never obscures; four weighted events led by ancientVault (voidBreach carries "their relationship with the Void"), the richest endgame loot weights of any biome (ancientArtifact/relic/researchSample highest yet + loreObject — knowledge as loot); "civilisation at its absolute peak" is numbers: threatModifier 1.45 (asserted strictly above AF-061's 1.35 — completing the five-biome ladder 0.9→1.15→1.25→1.35→1.45), natives via AF-036's live `enemyBuff` hook (shieldCapacity +16), dual shieldBreak+stasis `hazardImmunities` — the security recognises its keepers; a REAL boss: `bossId` resolves to the authored Hollow Sentinel (the guardian class defends its makers' home, resolution-tested); enemy presence follows the spec (full six-def AF-050 Custodian roster primary, two Conclave entities, rare Void, a Paragon prototype, occasional Eclipsed, all resolution-tested across five rosters); a Collapsed Gate gives `destroyObstacle` its FIRST producer — all seven locked interaction kinds now have producers; First Light joins the galaxy additively (new ancientCore region, threat 6 — the highest — past even Hollow Crown: FOUR real travel hops) as the fifth consumer of AF-038's `biomeId` through AF-058's registry; one additive `codex-biome-ancient-core` entry unlocks via the knowledge vault; browser-verified end-to-end — four real travel hops, launch, the Core live on the overlay (`First Light · weather crystalRain · hazards 3`, `galaxy First Light (ancientCore)`), zero page errors | Aligned | 747 tests green (11 new) incl. real GalaxyRuntime travel + real-BossDef resolution + a 1,000-mission seeded sweep ("the vigil never lapses") | Approved 9.5/10 | **LOCKED** |
| AF-063 | Solar Wastes Biome | v1 *(ten locations, seven weather names, eight hazard kinds, eight mission types, eight resources, eight POIs, eight events, eight discoveries, five boss kinds registered with TOTAL mappings onto AF-036's locked shelves — the AF-058→062 content-module shape applied to star systems dying in public)* | Complete — spec + implementation (`src/game/biomes/solarWastesBiome.ts`): `SOLAR_WASTES_BIOME` is a plain `BiomeDef` whose defining statement IS the spec's thesis — "the environment itself is the greatest threat": the ONLY authored biome where every hazard zone carries a status effect, asserted per-hazard (a Radiation Field applying AF-021's poison, a heavy-burn Plasma Geyser, a Solar Shockwave applying AF-053's overload — its FIRST biome-hazard use); Solar Flares/visibility-reducing Radiation Storms/Heat Waves (AF-020 forces incl. the strongest single-axis wind yet), four weighted events with solarFlare structurally dominant (asserted ≥ every other weight), high-tier technology loot weights (resource/craftingMaterial/equipment); "the star is the enemy, not the factions": threatModifier 1.3 slots BETWEEN the Forge's 1.25 and the Void's 1.35 — asserted from both sides, the first mid-ladder biome, because the danger budget lives in the environment; natives via AF-036's live `enemyBuff` hook (shieldCapacity +12) with dual burn+poison `hazardImmunities` — forged under this sun; enemy presence follows the spec (Machines AND Outlaws dual-primary ≥3 defs each — the salvage wars, with factionConflict at weight 2 carrying the same story — Conclave incl. the LIVING SUPERNOVA, the spec's own boss name already playable as an AF-054 entity and asserted by id, a Paragon energy construct, rare Void, all resolution-tested across five rosters); an Orbital Mirror uses `disableHazard` (redirect the mirror, the beam goes out); Cinderfall joins the galaxy additively (new solarWastes region, threat 4, off Forge Primus — three real travel hops) as the sixth consumer of AF-038's `biomeId` through AF-058's registry; one additive `codex-biome-solar-wastes` entry unlocks via the ancient forge (cross-referencing the Conclave AND the Ancient Core — the star-shepherds and what the precursors needed stars for, one thread); browser-verified end-to-end — three real travel hops, launch, the Wastes live on the overlay (`Cinderfall · weather solarStorms · hazards 3`, `galaxy Cinderfall (solarWastes)`), zero page errors | Aligned | 758 tests green (11 new) incl. real GalaxyRuntime travel + a 1,000-mission seeded sweep ("the star never relents") | Approved 9.5/10 | **LOCKED** |
| AF-064 | Frozen Reach Biome | v1 *(ten locations, seven weather names, eight hazard kinds, eight mission types, eight resources, eight POIs, eight events, eight discoveries, five boss kinds registered with TOTAL mappings onto AF-036's locked shelves — the AF-058→063 content-module shape applied to the galaxy's frozen edge)* | Complete — spec + implementation (`src/game/biomes/frozenReachBiome.ts`): the first biome whose identity is RESTRAINT — "contrast every previous region through calm, restraint and overwhelming stillness" made MECHANICAL and asserted against all six prior authored biomes: the slowest hazard ticks of any biome (every zone ≥1300ms — planning, not reflexes), the sparsest event pool (three defs, ≤ every prior biome's), the LOWEST elite chance (0.06, strictly below all six — "encounters remain deliberate rather than overwhelming"), winds never above a drift (every axis ≤0.3, per-def); two AF-021 status kinds dormant since registration get their FIRST producers — the Cryogenic Field roots with `freeze` (the preservation system does not distinguish visitors from specimens) and the Frozen Gas Cloud drags with `slow` — beside a heavy statusless Ice Crack, with dual freeze+slow `hazardImmunities` (the natives froze long ago; the cold has nothing left to take); "quiet is not safe": threatModifier 1.2 mid-ladder between the Expanse's 1.15 and the Forge's 1.25, asserted from both sides; enemy presence follows the spec (Eclipsed primary ≥4 defs — frozen former humans above frozen former colonies — Custodians, Machines, CRYOGENIC WILDLIFE resolved as AF-051 organisms adapted to the ice, rare Void, all resolution-tested across five rosters); Crystal Hail gives `meteorActivity` real downward force, Ice Fog is the longest weather def yet (13s), `lostExpedition` leads a pool for the first time anywhere — the expeditions are still here, perfectly preserved; Winterline joins the galaxy additively (new frozenReach region, threat 3, THE ECLIPSED as dominant faction — the first system they hold — past the Human Frontier, two real travel hops) as the seventh consumer of AF-038's `biomeId` through AF-058's registry, with a frozen-fleet POI giving `abandonedFleets` its first authored use; one additive `codex-biome-frozen-reach` entry unlocks via the cryo vault (cross-referencing the Eclipsed AND the Human Alliance — the colonies that chose suspension were human); browser-verified end-to-end — two real travel hops, launch, the Reach live on the overlay (`Winterline · weather meteorActivity · hazards 3` — Crystal Hail falling, `galaxy Winterline (frozenReach)`), zero page errors | Aligned | 769 tests green (11 new) incl. real GalaxyRuntime travel + stillness assertions against all six prior biomes + a 1,000-mission seeded sweep ("even the stillness has teeth") | Approved 9.5/10 | **LOCKED** |
| AF-065 | Derelict Expanse Biome | v1 *(ten locations, seven weather names, eight hazard kinds, eight mission types, eight resources, eight POIs, eight events, eight discoveries, five boss kinds registered with TOTAL mappings onto AF-036's locked shelves — the AF-058→064 content-module shape applied to the galaxy's largest starship graveyard)* | Complete — spec + implementation (`src/game/biomes/derelictExpanseBiome.ts`): the first biome whose identity is EXPLORATION OVER COMBAT, made structural and asserted against all seven prior authored biomes — the MOST interactables of any biome (four: black-box lore archive, salvage cache, sealed hangar via openHiddenArea, blocking hulk via destroyObstacle — every wreck a story you can touch, all findable-in-range-tested) and the FIRST biome to weight `shipComponent` loot (2.4, its highest weight, asserted absent from every prior — salvage IS the reward); hazards emerge from destruction: a pressurised Hull Explosion (the heaviest single statusless tick of any biome hazard, 11 damage, asserted), a Reactor Leak burn, and Unstable Wreckage giving AF-021's `armourBreak` its FIRST biome-hazard producer, with dual armourBreak+burn `hazardImmunities` (the wrecks already took everything the Expanse could break); the first biome with TWO visibility-reducing weathers (Sensor Interference + Electromagnetic Clouds, asserted exactly two — radio interference you can't see through) beside drifting Debris Storms; a distressSignal-led event pool (asserted dominant — SOME SHIPS STILL TRANSMIT) with the Salvage Race as factionConflict and a lone Nomad wanderingMerchant; "quiet, not safe": threatModifier 1.1 asserted between the Frontier's 0.9 and the Expanse's 1.15 — the danger is what you wake up; enemy presence follows the spec (Eclipsed primary ≥3 — these may be their own fleets — Outlaws AND Nomads both fielding salvage crews ≥2 each, Machine reclamation, rare Void, all resolution-tested across five rosters) with scavenged plating (shieldCapacity +8, the lightest buff of any buffed biome); Gravewake joins the galaxy additively (new brokenSystems region, threat 2, THE ECLIPSED's second held system, off Meridian Rest — two real travel hops) as the eighth consumer of AF-038's `biomeId` through AF-058's registry, with a ghost-carrier POI on `abandonedFleets`; one additive `codex-biome-derelict-expanse` entry unlocks via the black box (cross-referencing the Eclipsed AND the Nomad fleet — the dead fleets and the living ones; archives in one crew's voice, honouring "countless personal stories rather than one grand narrative"); browser-verified end-to-end — two real travel hops, launch, the Expanse live on the overlay (`Gravewake · weather meteorActivity · hazards 3` — Debris Storms drifting, `galaxy Gravewake (brokenSystems)`), zero page errors | Aligned | 780 tests green (11 new) incl. real GalaxyRuntime travel + exploration-over-combat assertions against all seven prior biomes + a 1,000-mission seeded sweep ("the wrecks are still dying") | Approved 9.5/10 | **LOCKED** |
| AF-066 | Living Ecospheres Biome | v1 *(ten locations, seven weather names, eight hazard kinds, eight mission types, eight resources, eight POIs, eight events, eight discoveries, five boss kinds registered with TOTAL mappings onto AF-036's locked shelves — the AF-058→065 content-module shape applied to worlds that became single organisms)* | Complete — spec + implementation (`src/game/biomes/livingEcospheresBiome.ts`): the deliberate INVERSE of AF-064's stillness, asserted against all eight prior authored biomes — the RICHEST event pool of any biome (seven defs, strictly above every prior: a living planet is always doing something) and the FASTEST hazard tick of any biome (a Carnivorous Grove snapping at 600ms, strictly below every prior's fastest: the terrain is hungry) — the Reach and the Ecosphere bracket the pacing spectrum, both brackets in tests; hazards emerge from biology (the statusless Grove, Toxic Spores as AF-021 poison — the planet's immune response — Root Traps as slow) with dual poison+slow `hazardImmunities` (the ecosystem does not eat its own); "everything serves the planetary organism" includes loot: the highest researchSample weight of any biome (2.4, asserted — biology IS research), with Mass Bloom leading the pool through the SAME crystalBloom engine event AF-048's crystal ecology rides (two living doctrines, one bloom vocabulary); enemy presence follows the spec (the Bio-Engineered Hive primary as the FULL AF-051 roster, all six defs asserted — their home; the Ascendancy ≥2 as the other living doctrine; ENVIRONMENTAL WILDLIFE resolved as the same organisms — the fauna IS the faction; rare Void; occasional Custodians; all resolution-tested across four rosters) with symbiotic carapace (shieldCapacity +11); a Symbiosis Chamber uses `triggerEvent` — provoke the planet; threatModifier 1.28 asserted between the Forge's 1.25 and the Wastes' 1.3 — a living world defending itself; Verdance joins the galaxy additively (new darkNebula region — "something inside it drinks the starlight" — threat 4, the Hive's first held system, hidden past Prismheart: the living worlds cluster, two real travel hops) as the ninth consumer of AF-038's `biomeId` through AF-058's registry, with an ancient-bio-lab POI giving `researchStations` its first authored use; one additive `codex-biome-living-ecospheres` entry unlocks via the organic archive (cross-referencing the Hive AND AF-048's resonance doctrine; the ethics-of-creating-life lore carried as open questions, not answers); browser-verified end-to-end — two real travel hops, launch, the Ecosphere live on the overlay (`Verdance · weather energyWinds · hazards 3` — Seed Winds blowing, `galaxy Verdance (darkNebula)`), zero page errors | Aligned | 791 tests green (11 new) incl. real GalaxyRuntime travel + most-alive assertions against all eight prior biomes + a 1,000-mission seeded sweep ("the planet never sleeps") | Approved 9.5/10 | **LOCKED** |
| AF-067 | Singularity Zone Biome | v1 *(ten locations, seven weather names, eight hazard kinds, eight mission types, eight resources, eight POIs, eight events, eight discoveries, five boss kinds registered with TOTAL mappings onto AF-036's locked shelves — the AF-058→066 content-module shape applied to the rarest region in the galaxy)* | Complete — spec + implementation (`src/game/biomes/singularityZoneBiome.ts`): the ENDGAME APEX, a battery of strict inequalities asserted against all nine prior authored biomes — highest threatModifier (1.55, above even the Ancient Core's 1.45), highest eliteChance (0.2), strongest enemyBuff (shieldCapacity +18 — reality armours what it accepts), richest ancientArtifact weight (2.6 — the pinnacle of technological progression), strongest weather wind anywhere (Gravity Storms at 1.2, above every prior's maximum), and the MOST hazard zones of any biome (FOUR: a heavy statusless Micro Singularity, a stasis Time Dilation Field, shock Quantum Lightning, an overload Event Horizon Surge — physics itself is hostile), with stasis+overload `hazardImmunities` (the Zone's own do not object to its redrafting); "weather alters the battlefield while remaining fully readable" is the spec's own constraint and is asserted — zero visibility reduction per-def, the same authored statement as the Ancient Core, deliberately; enemy presence follows the spec (Void ≥3, Conclave ≥2, Custodians ≥2, Paragon ≥2 incl. the OMEGA PROTOTYPE in the endgame biome, and "rare Reality Constructs" resolved as the constellation avatar + energy construct by id — the spec's new name is the game's existing roster, the AF-064/066 resolution move); Axiom joins the galaxy additively AND completes it: the singularityZone region def is the LAST of AF-038's ten locked GALAXY_REGIONS to receive a definition (asserted: every region on the shelf resolves — the galaxy map is complete), with Axiom (threat 7, asserted the deepest system against every authored system, Void Swarm dominant, past even First Light — the Zone begins where the precursors' light ends, FIVE real travel hops) as the tenth consumer of AF-038's `biomeId` through AF-058's registry and a mathematical-engine POI giving `prototypeFacilities` its first authored use; one additive `codex-biome-singularity-zone` entry unlocks via the quantum archive (cross-referencing the Ancient Core AND AF-049's corruption doctrine — the precursors went in, the Void leaks out; their fate and the Network's purpose answered only with "We go to ask it in person"); browser-verified end-to-end — five real travel hops, launch, the Zone live on the overlay (`Axiom · weather crystalRain · hazards 4` — the four-zone biome running, `galaxy Axiom (singularityZone)`), zero page errors | Aligned | 803 tests green (12 new) incl. real GalaxyRuntime travel + the endgame-apex battery against all nine prior biomes + the galaxy-map-complete assertion + a 1,000-mission seeded sweep ("reality never stops redrafting") | Approved 9.5/10 | **LOCKED** |
| AF-068 | Campaign Framework | v1 *(ten campaign stages, eight chapter-content kinds, eight world-progression kinds, eight choice domains, eight story-delivery channels, eight major-event kinds, eight milestone-unlock kinds, seven post-campaign kinds registered — a ledger over locked engines in the AF-056/057 orchestration class)* | Complete — spec + implementation (`src/game/campaign/`): `CampaignRuntime` is pure, bus-free, RNG-free and clock-free — objectives reuse AF-026/035/037's exact counterKey/target pattern with exactly THREE composition-root feed keys (mission victories via endRun, system arrivals via real GalaxyRuntime travel, boss defeats via the AF-057 death sequence); "gameplay should never pause for the story" is structural — beats queue per chapter on the eight registered channels and drain one-per-cadence through the AF-055/057 consume seam into the existing notice class; "never permanently remove core content" holds by TYPE — no remove/clear/revoke/reset operation exists (asserted over the prototype), flags/unlocks/world-changes/choices all append-only; "every stage naturally unlocks the next" is the algorithm — ten chapters, one per stage in strict ladder order, counters persisting so banked progress chains chapters the moment a gate opens (tested both ways: the ladder halts at boss-gated Ancient Awakening regardless of surplus, then one feed chains straight to the end); post-campaign is a STATE, not an ending — the final chapter has zero objectives, the runtime accepts progress forever (1,000 post-completion feeds asserted), and the seven post-campaign pillars map TOTALLY onto AF-038's LongTermGoalKind shelf — dormant since AF-038, its FIRST producer; "every campaign feels personal" through determinism, not chance (mirrored-runtime assertion over 200 randomized feeds); the sandbox campaign's unlocks are resolution-tested against real content (hollow-sentinel, singularity-zone, humanFrontier/machineExpanse) and its beats thread the arc the ten biomes told — the Sentinel standing down, First Light's vacated seat, "We go to ask it in person"; browser-verified — real travel banked a system, launch, `campaign prologue · \"Afterlight\" (1/10) · obj 0/1` live on the overlay, zero page errors | Aligned | 817 tests green (14 new) incl. the no-removal-API prototype assertion + mirrored-runtime determinism + a 1,000-playthrough seeded sweep in arbitrary feed orders (every playthrough reaches the post-campaign galaxy with monotone stages and flags) | Approved 9.5/10 | **LOCKED** |
| AF-069 | Endgame Framework | v1 *(ten endgame phases incl. the Constitution-compliant communityEvents future slot, eight legendary-expedition kinds, six world-evolution kinds, five difficulty axes, six infinite-research branches, eight legendary-reward kinds, eight world events, eight AF-026 mastery-track IDs, six legacy-record kinds registered — AF-068's sibling ledger, gated on the campaign)* | Complete — spec + implementation (`src/game/endgame/`): `EndgameRuntime` is pure and constructs LOCKED — every progression operation no-ops until `notifyCampaignComplete()`, fired by the same composition-root seam that feeds AF-068 (the gate is a STATE, visible in-game: the overlay reads `locked — the endgame begins after the main campaign` until the ladder closes); "resets expedition progression, retains permanent progression" is the TWO-MAP SPLIT — `ascend()` clears exactly the per-ascension expedition map while lifetime totals, research nodes, the evolution log and the legacy log all survive (asserted together), and the permanent side has no clearing operation at all (no remove/delete/revoke/reset on the prototype, asserted); "not inflated health, not inflated damage" is UNREPRESENTABLE — `AscensionModifierDef` carries an axis (five registered levers) and a description with NO numeric field, asserted across fifty generated levels by inspecting the shape's own keys; "unlimited future expansion" is a formula — authored Ascensions I–III (The Second Dawn / The Long Watch / Past the Horizon, each with axis modifiers, real reward kinds and an unlocked mechanic) extend forever via `ascensionLevelFor` (ten-ascension career tested, modifiers compounding); "never artificial progression walls" — linear milestone scaling + geometric research costs, six branches unbounded, progression halts only when points run out and resumes on the next top-up (tested); world events are a seeded weighted pick (deterministic across mirrored seeds, always in-pool); mastery reuses AF-026's engine (track IDs, no second system); browser-verified — the endgame line live beside the campaign line, the locked gate rendering verbatim, zero page errors | Aligned | 828 tests green (11 new) incl. the fifty-level no-numeric-modifier assertion + the locked-gate battery + a 1,000-career seeded sweep of 200 mixed operations each (permanent progression and ascension level never regress; per-ascension progress resets every ascension; "there is always another goal", literally) | Approved 9.5/10 | **LOCKED** |
| AF-070 | Live Operations Framework | v1 *(ten content tiers, eight seasonal kinds, eight expansion kinds, seven galaxy-evolution sources, eight live events, six community objectives — a Constitution-compliant FUTURE slot — eight cosmetic/lore reward kinds, FIVE FORBIDDEN PRACTICES registered by name, five monetisation kinds, six mandatory QA gates — the third ledger module, and the one that guards all the others)* | Complete — spec + implementation (`src/game/liveops/`): `LiveOpsRegistry` is a validating CONTENT-PACK REGISTRY where the spec's promises are registration-time GATES — validation is all-or-nothing and a rejected pack mutates NOTHING (snapshot-equality asserted); "never invalidate previous content" is a REJECTION: an addition reusing a registered id is a replacement attempt and is refused — the gate protects the REAL game because `CORE_GAME_PACK` registers as pack zero with the actual shipped ids (all ten authored biomes resolution-tested, the Hollow Sentinel, AF-068's chapters), and the test shipping "Singularity Zone Remastered" over the real id is rejected with the exact reason; "never create power creep" is UNREPRESENTABLE (AF-069's discipline continued): an addition is a kind + id (+ challenge-only temporary flag), asserted by key inspection — no stat field exists, and all eight reward kinds are cosmetic/lore; "regression testing becomes mandatory" is a gauntlet — a pack failing ANY of six QA gates is rejected, all six failure modes tested individually; "avoid FOMO-exclusive gameplay / core progression never resets": only temporaryChallenges may be temporary (a limited-time biome is rejected BY NAME), ending a season retires exactly its challenges while discoveries/cosmetics/lore/the core game stay live, retired challenges remain as history (the Season Archive), and no remove/delete/revoke/reset operation exists (prototype-asserted); "Year 1 → Year 10 without architectural redesign" is the sweep run literally — a simulated DECADE (40 quarterly seasons + 10 annual expansions through the same unmodified class): monotone content growth every year, a year-one save compatible at year ten, all 40 challenges retired with nothing else ever removed, the core game intact, and `returningPlayerRecap` reconstructing the full decade ("players should never feel lost returning after months away"); browser-verified — `liveops v2 · packs 2 (19 additions) · season S1 \"Embers of the Frontier\" · compat ok` live beside the campaign and endgame lines, zero page errors | Aligned | 837 tests green (9 new) incl. the replacement-rejection with state-equality + the six-gate QA battery + the FOMO rejection + the ten-year decade sweep | Approved 9.5/10 | **LOCKED** |
| AF-071 | Commander Framework | v1 *(eight classes mapped TOTALLY onto AF-030's locked archetype shelf, seventeen architecture parts, seven ability stages, six talent-node kinds, six mission beats, seven progression kinds, six relationship subjects, seven cosmetic kinds, and the 12–16 ROSTER_TARGET registered — layered over AF-030's byte-for-byte unchanged commander system)* | Complete — spec + implementation (`src/game/commanders/commanderFrameworkData.ts` + `CommanderProgressionRuntime.ts`): `CommanderProfileDef` wraps each AF-030 `CommanderDef` BY ID (the AF-056/057 wrapping pattern) — "nothing is left undefined" is a FUNCTION (`architectureFor`, all seventeen parts asserted per commander); the seven-stage ability structure completes AF-030's four hooks with a secondary ability (plain AF-028 ActiveModule), a mastery passive, and the ASCENSION UPGRADE — AF-069's ascension level given its first cross-module consumer: the linked talent node unlocks FREE at the gate, exactly once; talent trees are AF-028 vocabulary (three branches × all six kinds, every node a nonzero EquipmentBonus), hybrid builds are the DEFAULT (any branch, any order) with specialisation EARNED (endgame nodes need three in-branch), and "cache talent calculations" is reference-equality tested; mastery reuses AF-026 (`commander:{id}` tracks — the composition root's commander:placeholder feed became the real track); "relationships influence dialogue, not gameplay balance" is UNREPRESENTABLE (subject+target+hint, no bonus field, key-inspected; every target resolution-tested against real codex entries/commanders — a nonexistent codex-the-collapse reference was caught by exactly this test and corrected); cosmetics gameplay-neutral by the same construction; progression append-only (no remove/respec on the prototype — respec is a future Project-Owner decision); the roster extends additively — AF-030's pair (asserted unchanged) + Dr. Sen Vael "Meridian" (scientist), all three passing AF-030's OWN findOverlap law with distinct fingerprints; three classes, three ascension gates, fifty-four authored talent nodes prove the architecture while ROSTER_TARGET registers the release goal honestly; browser-verified — `Longlight (assault) · talents 0/18 (0 pts) · mission originStory` live on the extended commander overlay line, zero page errors | Aligned | 850 tests green (13 new) incl. the seventeen-part completeness function + the extended-roster overlap law + a 1,000-career seeded sweep (the point ledger never drifts; every spent point is a live AF-028 bonus) | Approved 9.5/10 | **LOCKED** |
| AF-072 | Commander Roster | v1 *(fourteen launch philosophies mapped TOTALLY onto AF-030's archetypes, six team-synergy kinds + four AI contexts registered FUTURE, seven recruitment sources, eight customisation kinds, eight statistic kinds, five balance axes — with raw damage asserted ABSENT from the shelf)* | Complete — spec + implementation (`src/game/commanders/rosterData.ts` + `RosterRuntime.ts`): the roster is DATA on unchanged AF-030/071 shapes — fourteen commanders (the AF-030/071 trio as vanguard/guardian/scientist + eleven new: Torque, Whisper, Longfang, Thunderline, Cipher, Aviary, Keystone, Chord, Nadir, Redline, Relay), philosophy assignment a BIJECTION (asserted); "every Commander remains unique" asserted THREE ways — AF-030's own findOverlap law, fourteen distinct fingerprints, and a stronger roster rule (no two share a passive trigger+bonus pair); all fourteen pass AF-071's seventeen-part completeness function AND drive AF-071's unchanged progression runtime in tests (a talent unlocks, its AF-028 bonus goes live, the ascension upgrade fires at gates spread across Ascensions I–III); TWO AF-028 debts paid — droneEffectiveness (Aviary's flock) and orbitalPower (Thunderline's batteries) get their FIRST producers, asserted by id; recruitment is meaningful and TOTAL (all seven sources recruit someone, the trio arrives with the campaign, every requirement names real AF-068/exploration/faction content) with `RosterRuntime` gating on reached sources, recruiting append-only, and tracking usage/victories with win rate DERIVED never stored — the prototype exposes no remove/retire/nerf/buff operation ("every Commander remains viable forever" is what the code cannot do); "25+/50+/100+ without redesign" EXECUTED — `syntheticCommanderFor(n)` generates deterministic entries forever, one hundred synthetics + the roster yielding 114 distinct fingerprints and 100 complete architectures through unchanged AF-030/071 functions; browser-verified — `Longlight (assault/vanguard) · roster 3/14 · uses 0 (0% wr)` live on the extended commander line, zero page errors | Aligned | 862 tests green (12 new) incl. the triple-distinctness battery + per-commander playability + the hundred-synthetic scalability proof + a 1,000-season seeded sweep (the usage ledger never drifts; the trio never leaves) | Approved 9.5/10 | **LOCKED** |
| AF-073 | Ship Framework | v1 *(ten spec classes mapped TOTALLY onto AF-031's locked eleven-class shelf, 22 architecture parts, five ability stages, seven defensive + eight offensive identity kinds, eight module kinds, eight mastery metrics, eight neutral customisation kinds — the AF-071 commander precedent applied to hulls, over AF-031's byte-for-byte unchanged ship system)* | Complete — spec + implementation (`src/game/ships/shipFrameworkData.ts` + `ShipOutfittingRuntime.ts`): `ShipProfileDef` wraps each AF-031 `ShipDef` BY ID — "nothing remains undefined" is a FUNCTION (`shipArchitectureFor`, all 22 parts asserted per hull); the spec's MOVEMENT MODEL has been live since AF-020 and this module NAMES it rather than rebuilding it (all eight fields resolve to real MovementProfile keys on the actual object — no second movement system); the five-stage ability structure completes AF-031's two stages with an ultimate REUSING AF-030's exact charge-gated shape (no second ultimate model), a special mechanic, and the AF-069-gated ascension upgrade — a REAL module (resolution-tested) fitting FREE outside the slot count, once, permanently; modules are LOADOUTS and mastery is a LEDGER, and the code knows the difference — slot-gated fitting, cached aggregation (reference-tested), unfitting ALLOWED (refitting is gameplay, not content removal) while the mastery side (usage/kills/bossVictories/distance) is append-only with no reset/clear/wipe operation (prototype-asserted); identity never repeats — primary defence AND offensive identity unique per hull (precision/avoidance, areaDamage/hull, beamWeapons/energyBarrier); heat/cargo capacities registered DORMANT (the biomeId pattern) awaiting first consumers; the roster extends additively (AF-031's pair asserted unchanged + the Aurelia Mk. I science vessel, all passing AF-031's OWN findShipOverlap law); browser-verified — `Wayfarer Mk. II (scout/corvette) · precision/avoidance · modules 0/2 · uses 0` live on the extended ships line, zero page errors | Aligned | 874 tests green (12 new) incl. the 22-part completeness function + the movement-model resolution + the module battery + a 1,000-career seeded sweep (slots never exceeded; the mastery ledger never drifts; bonuses always match the fit) | Approved 9.5/10 | **LOCKED** |
| AF-074 | Ship Roster Framework | v1 *(twelve manufacturers — the spec's nine + the three already canon — each with all FIVE identity parts; eight tiers; ten specialisations; seven build kinds; seven collection kinds; six research kinds; six legendary traits; five prototype mechanics; eight stat kinds; five balance axes with raw statistics asserted ABSENT; eight cosmetic kinds — the AF-072 roster move applied to hulls)* | Complete — spec + implementation (`src/game/ships/shipRosterData.ts` + `ShipCollectionRuntime.ts`): the fleet is DATA on unchanged AF-031/073 shapes — ten hulls, specialisation a BIJECTION (the trio: Wayfarer/exploration common, Bastion/tanking uncommon, Aurelia/scientificOperations ANCIENT + seven new: Sable Dart/speed Helios, Falchion/criticalHits Vanguard, Hivemother/droneWarfare Nova Forge, Dawnspire/energyWeapons Ancient Foundry LEGENDARY, Ballista/missiles Atlas, Caduceus/support Aegis, Maelstrom X-1/crowdControl Prototype Division PROTOTYPE); "tier affects acquisition, never viability" is UNREPRESENTABLE — entry keys asserted exactly {shipId, manufacturerId, tier, specialisation, collectionKind, discoveryMethod}, no stat field; MYTHIC honestly registered-empty (the bossId-null pattern) and "legendary does not mean strictly stronger" is a number (the Dawnspire's hull asserted not the fleet's largest); distinctness asserted FOUR ways (AF-031's own overlap law, ten fingerprints, unique trigger+bonus pairs, unique defence/offence identity pairs — the last catching a REAL authored collision, the Dawnspire re-armoured in precursor stone alloy); all ten pass AF-073's 22-part completeness function with real gated modules across Ascensions I–III; collection is PERMANENT — `ShipCollectionRuntime` gates on registered collection kinds (all seven used), collects append-only (no remove/retire/scrap on the prototype), statistics derived never stored; "25+/50+/100+ without redesign" EXECUTED — one hundred deterministic synthetics + the fleet yield 110 distinct fingerprints and 100 complete architectures through unchanged AF-031/073 functions; browser-verified — `Wayfarer Mk. II (scout/corvette) · common/exploration · fleet 1/10` live on the ships line, zero page errors | Aligned | 884 tests green (10 new) incl. the quadruple-distinctness battery + the tier-shape key inspection + the hundred-synthetic proof + a 1,000-career seeded sweep (the usage ledger never drifts; no hull is ever lost) | Approved 9.5/10 | **LOCKED** |
| AF-075 | Weapon Framework | v1 *(seventeen spec categories, ten fire modes, and ten projectile-system kinds mapped TOTALLY onto AF-032's locked shelves — the naming-layer discipline three times in one module; twenty architecture parts; ten elements; six evolution sources; eight mastery metrics; seven synergy surfaces; seven neutral customisation kinds — the AF-071/073 profile precedent applied to weapons)* | Complete — spec + implementation (`src/game/weapons/weaponFrameworkData.ts` + `WeaponMasteryRuntime.ts`): `WeaponProfileDef` wraps each AF-032 `WeaponDef` BY ID — "nothing remains undefined" is a FUNCTION (`weaponArchitectureFor`, all twenty parts asserted per weapon; heat generation registered DORMANT, the AF-073 pattern); "elements integrate with status systems" is a total map WITH a consistency law — ten elements onto AF-021's status kinds (kinetic→none: purity is a mapping too), and every profile's element must agree with its def's actual statusOnHit (asserted across the arsenal — the element layer NAMES what the weapon already does); "evolution enhances identity, not replaces it" follows AF-032's OWN precedent — the Coil Ripper→Mk. II chain shifts category, so identity preservation is NAME LINEAGE + MANUFACTURER, both asserted, with registered sources per evolving weapon; mastery is a LEDGER fed by REAL combat — both damage-application sites record hits, boss hits record boss damage, shots sync from AF-032's own fire counter, accuracy DERIVED and clamped, no reset/clear/wipe (prototype-asserted); the arsenal extends additively — AF-032's four asserted unchanged at the head + the HAILBORN ARRAY, a cryo weapon giving the freeze status its first WEAPON producer (the cold spreads from AF-064's biome to the arsenal through the same registry); browser-verified — `Coil Ripper · railguns/electrical→shock · mastery 0 kills 0%acc` live on the weapons line with the element→status map visible in-game, zero page errors | Aligned | 893 tests green (9 new) incl. the three total maps + the element-def consistency law + the evolution identity-preservation law + a 1,000-career seeded combat sweep (the ledger never drifts; accuracy stays in range) | Approved 9.5/10 | **LOCKED** |
| AF-076 | Weapon Roster Framework | v1 *(fourteen weapon manufacturers — the spec's ten + the four already shipped — each with all SIX identity parts; eight tiers; TWENTY-ONE weapon families (the spec's four example categories × four each + every in-use category); seven collection kinds; six research kinds; seven legendary traits; seven prototype mechanics; eight stat kinds; five balance axes with numerical superiority asserted ABSENT — the AF-074 fleet move applied to the arsenal)* | Complete — spec + implementation (`src/game/weapons/weaponRosterData.ts` + `WeaponCollectionRuntime.ts`): the arsenal is DATA on unchanged AF-032/075 shapes — ten weapons (AF-032's four + AF-075's Hailborn asserted unchanged at the head + five new: Atlas Cluster Battery common, Helios Prism Array epic, Paragon Flux Driver PROTOTYPE, Foundry Sunlance LEGENDARY — the First Light vault thread — Salvage Scattergun common), all passing AF-032's own overlap law AND AF-075's element/status consistency law with the twenty-part completeness function; WEAPON FAMILIES are a THREE-LAYER binding — every family's category on AF-075's shelf, every entry's family matching its profile's category (def→profile→entry bound and tested together); "tier affects acquisition, never viability" re-proven (no stat field by key inspection, mythic honestly empty again, the legendary Sunlance asserted not the arsenal's highest damage); PROTOTYPE gameplay gives AF-075's dormant heat register its first DATA DISCIPLINE — the Flux Driver's heat asserted strictly above every other profile ("heat management" as authored numbers on the registered-dormant field, exactly the consumer it awaited); collection is PERMANENT (gated on registered kinds, append-only, no remove/retire/scrap, uses counted per weapon); "limitless expansion" EXECUTED — one hundred deterministic synthetics + the arsenal yield 110 distinct fingerprints and 100 complete architectures through unchanged AF-032/075 functions; browser-verified — `Coil Ripper · uncommon/railguns:precision · railguns/electrical→shock · arsenal 1/10` live with tier, family, element and collection all visible in-game, zero page errors | Aligned | 904 tests green (11 new) incl. the three-layer family binding + the prototype heat discipline + the hundred-synthetic proof + a 1,000-career seeded sweep (the ledger never drifts; no weapon is ever lost) | Approved 9.5/10 | **LOCKED** |
| AF-077 | Relic Framework | v1 *(eight spec tiers, sixteen spec categories, and six spec stacking rules mapped TOTALLY onto AF-029's locked shelves — fusion combinations binding to the existing evolutionRequires field, registered future; fifteen architecture parts; six evolution triggers; seven discovery sources; six build-defining kinds; eight synergy surfaces; six customisation kinds; seven collection states — the AF-071/073/075 profile precedent applied to relics)* | Complete — spec + implementation (`src/game/relics/relicFrameworkData.ts` + `RelicCollectionRuntime.ts`): the spec's central demand was ALREADY AF-029's law — "relics should rarely provide only flat bonuses" is enforced by `validateRelicDef`'s behaviour-clause requirement, shipped at AF-029 and re-asserted across the extended reliquary (the "conditional effect" architecture part IS that clause); "every relic feels handcrafted" is a FUNCTION (`relicArchitectureFor`, all fifteen parts per relic; profiles stat-free by key inspection); the reliquary extends additively — AF-029's seven asserted unchanged + the SINGULARITY KEEPSAKE, a quantum relic with a REAL trade-off (+damage, −speed: "the Zone charges rent") passing AF-029's validator and its UNCHANGED RelicSystem in tests (acquisition, stack-cap uniqueness, synergy detection); TWO authoring conventions discovered by the locked engine's own machinery and recorded for every future author — uniqueness is `maxStacks: 1`, and synergy declarations live on the alphabetically-earlier relic (both caught by tests, fixed in data, documented); collection is a MONOTONE STATE LATTICE (discovered→owned→mastered with mastery gated on ownership, states never regress, evolution a permanent parallel mark, no demote/forget operation) fed by REAL play — both composition-root acquisition sites advance the lattice and AF-029's own evolution callback records evolutions forever; browser-verified — `relics · reliquary 0/8 owned, 0 evolved` live on the overlay, zero page errors | Aligned | 913 tests green (9 new) incl. the three total maps + the no-flat-bonus law re-assertion + the monotone-lattice battery + a 1,000-career seeded collection sweep (the lattice never regresses; mastered ≤ owned ≤ discovered always) | Approved 9.5/10 | **LOCKED** |
| AF-078 | Relic Roster Framework | v1 *(fifteen relic families mapped TOTALLY onto AF-077's categories, six set thresholds, nine build-network surfaces, eight discovery origins each with an AUTHORED identity, seven ancient + six void + six prototype trait vocabularies, seven evolution routes mapped onto AF-077's triggers, eight collection kinds, seven museum features, three forbidden outcomes registered BY NAME — the AF-076 roster move applied to relics)* | Complete — spec + implementation (`src/game/relics/relicRosterData.ts`): "set bonuses change gameplay, not only statistics" is UNREPRESENTABLE — `RelicSetBonusDef` is a threshold + trigger + description with no numeric field (key-inspected), detection pure and order-independent, verified against ALL 512 possible loadouts by brute force; THE ENGINE TAUGHT THE THIRD AUTHORING CONVENTION through two broken drafts — AF-029's evolution is a FUSION (ember+frost consumed, Cinder Heart granted), so fusion INPUTS must never be set pieces: sets are built from the fusion's PRODUCT and evolution only ever ADVANCES a set (asserted — fusing completes the Thermal Cycle rather than breaking it); "power requires sacrifice" is a LAW — every void-family relic asserted to carry a negative effect clause (the VEIL FRAGMENT pays in shield lattice), the reliquary extending additively through both inherited validators; the MUSEUM is derivation, not a system (`museumEntryFor` assembles all seven features from data every relic already carries, asserted for all nine); the roster went LIVE in the composition root — drop pools upgraded from the sandbox seven to the full nine, the reliquary lattice spanning all profiles, live set detection on the overlay; browser-verified — `relics · sets 0 live · reliquary 0/9 owned` live, zero page errors | Aligned | 923 tests green (10 new) incl. the 512-loadout brute-force verification + the fusion-aware order-independence battery through the REAL RelicSystem + a 1,000-career seeded acquisition sweep (set bonuses never regress, fusion included) | Approved 9.5/10 | **LOCKED** |

| AF-079 | Equipment Framework | v1 *(seventeen architecture parts; the spec's sixteen module categories mapped TOTALLY onto AF-028's twelve; AF-073's eight ship-module kinds mapped TOTALLY onto the sixteen — ONE engineering vocabulary; the six spec slot kinds realised by four EXISTING AF-028 mechanisms — no second slot system; seven resource surfaces; eight synergy surfaces; six upgrade routes; six evolution routes; seven collection states; six customisation kinds — the AF-071/073/075/077 profile precedent applied to equipment)* | Complete — spec + implementation (`src/game/equipment/equipmentFrameworkData.ts` + `EquipmentCollectionRuntime.ts`): AF-028's engine untouched — `EquipmentProfileDef` wraps modules BY ID with manufacturer (AF-074's REAL ship-manufacturer register: equipment is ship-installed technology), visual identity, lore, energy/heat/weight, rarity, synergy tags, upgrade/evolution routes, statistics and expansion hooks, all seventeen parts proven per module by `equipmentArchitectureFor`; "NO MODULE EXISTS ONLY TO INCREASE NUMBERS" is a FUNCTION — `engineeringDimensionsFor` counts non-numeric dimensions and every profiled module scores ≥ 1, with the bare-stat refit cannon deliberately UNPROFILED as the counterexample proving the law rejects things (its home is AF-032/075's weapon framework); the CRYO MANIFOLD is the first equipment item with an ACTIVE module through AF-028's unchanged shape (Emergency Vent, 15 s) and the architecture makes its energy requirement MANDATORY — actives draw AF-031's real energy, passive-only modules owe none (asserted both ways); heatOutput and weight join the dormant registers (the biomeId pattern) with `engineeringLoadFor` already summing §Debug's energy/heat/mass; the WORKSHOP is AF-077's monotone lattice with crafting in owning's seat (unseen→discovered→crafted→mastered, evolved permanent and parallel, no-removal API asserted by prototype inspection); LIVE in the composition root — `equipment4: cryo-manifold` validated and aggregated by AF-028's REAL engine (vanguard 2pc + manifold stack to +16% boost efficiency, the first live active module), item map upgraded to the workshop six; browser-verified — `equipment · draw 20 · heat 12 · mass 40 · workshop 4/5 crafted` live, zero page errors | Aligned | 933 tests green (10 new) incl. the three total maps + the engineering law with counterexample + the manifold through the REAL validator/aggregator + the lattice battery + a 1,000-career seeded engineering sweep (lattice never regresses; crafted ≤ discovered; mastered ≤ crafted; load never negative) | Approved 9.5/10 | **LOCKED** |
| AF-080 | Equipment Roster Framework | v1 *(fourteen engineering manufacturers — the spec's twelve + the two already shipped — each with all SIX identity parts AND a shipwright cross-binding to AF-074's register (nine bind, five engineering-only); eighteen module families mapped TOTALLY onto AF-079's sixteen categories; ten engineering philosophies; ten advanced-synergy surfaces; seven set kinds; six research kinds; eight collection kinds; seven lab features; two forbidden outcomes; eight accessibility surfaces; four performance disciplines — the AF-076/078 roster move applied to engineering)* | Complete — spec + implementation (`src/game/equipment/equipmentRosterData.ts`): the ecosystem is DATA on unchanged AF-028/079 shapes — ten modules (AF-079's six asserted unchanged at the head + Aegis Bastion Array, Aegis Ward Projector, Horizon Flux Capacitor, Nova Warden Hive), all passing AF-079's seventeen-part completeness AND the engineering law; THREE-LAYER BINDING with a new consistency law — entry family → profile category exact, engineering manufacturer resolves, and a module by a shipwright-bound maker must carry that shipwright's AF-074 id in its profile (entry→profile→AF-074, machine-checked); entries identity-only (no stat field by key inspection); SETS CLASSIFIED BY KIND with gameplay clauses over AF-028's UNCHANGED SetDef engine — the vanguard set revealed as a HYBRID set (three manufacturers, asserted) and the new BASTION set as Aegis's manufacturer set (one maker, asserted), threshold-exact through the real validator/aggregator; PROTOTYPE ENGINEERING PAYS — the Flux Capacitor carries instability as a REAL negative clause (the AF-078 void law crossing into modules), a conditional passive, uniqueness, a reactor gate proven to reject and accept through the real engine, and strictly the hottest heat register (the AF-076 prototype-heat discipline on equipment); the Warden Hive is the first EQUIPMENT producer of AF-028's registered-future droneEffectiveness kind; the ENGINEERING LAB is derivation (`engineeringLabFor`, all seven features from data every module carries, asserted for all nine); "no module dominates every build" is a PARETO assertion across four engineering axes; LIVE — item map upgraded to the full ten, both Bastion pieces installed so set detection has a subject, workshop spanning all nine profiles; "limitless" EXECUTED — 100 synthetics + roster = 110 distinct ids through unchanged laws; browser-verified — `sets 2 · draw 20 · heat 17 · mass 57 · workshop 6/9 crafted` live, zero page errors | Aligned | 946 tests green (13 new) incl. the three-layer binding + the prototype-pays battery with the reactor gate proven both ways + the Pareto no-dominance law + the 110-id synthetic proof + a 1,000-build seeded sweep through AF-028's REAL validator/aggregator (set bonuses match brute force exactly on every valid build) | Approved 9.5/10 | **LOCKED** |
| AF-081 | Research Framework | v1 *(fifteen primary + ten secondary branches mapped TOTALLY onto AF-024's fifteen categories; twelve architecture parts; ten scientific disciplines; eight gameplay-unlock kinds; seven major-discovery kinds; eight laboratories with authored specialisations; seven resources with an honesty flag; seven discovery routes with authored identities; eight synergy surfaces; six presentation features; two forbidden outcomes; eight accessibility surfaces — the AF-071/079 profile precedent applied to research)* | Complete — spec + implementation (`src/game/research/researchFrameworkData.ts`): AF-024's engine untouched — `ResearchProjectProfileDef` wraps each node BY ID (branch, discipline, laboratory, lore, gameplay unlock, visual identity, codex entry, hooks) with `researchArchitectureFor` proving all twelve parts; "NEVER SIMPLY INCREASE STATISTICS" is UNREPRESENTABLE — `ResearchGameplayUnlock` is a kind + description with no numeric field (key-inspected), and LATTICE ATTUNEMENT makes the law LITERAL: the first crystalResonance project (category registered-empty since AF-024) carries `effect: null`, a breakthrough whose entire meaning is gameplay; THREE-LAYER BINDING per project (profile branch → mapped category → the node's own category, exact) plus lab resolution and REAL codex resolution against AF-026; RESEARCH TIME honours DR-005 as a COMPLIANCE assertion (`completionTimeMs === 0` for every project — the owner ruling as a tested architecture part); resources HONEST — exactly one live today (AF-024's real point economy), six registered awaiting producers; presentation and debug are DERIVATION (`dependencyMapFor` mirrors the tree exactly; `researchEfficiencyFor` + `scientificProgressFor` pure and bounded, live on the overlay); composition root upgraded to the framework tree through the unchanged engine; browser-verified — `research · eff 0% · sci 0%` live, zero page errors | Aligned | 955 tests green (9 new) incl. "complete every research project" executed LITERALLY through the real engine (hidden discovery revealed, every dependency satisfied, every point spent, efficiency exactly 1, reset refunds all and preserves the discovery) + a 1,000-career seeded sweep (unlocks never regress; points never negative; the spent ledger never drifts) | Approved 9.5/10 | **LOCKED** |
| AF-082 | Research Roster Framework | v1 *(fifteen roster disciplines mapped TOTALLY onto AF-081's ten; eight research tiers; eight philosophy surfaces; nine discovery-network sources; nine specialised laboratories mapped TOTALLY onto AF-081's eight-lab register with a per-project EQUALITY law; seven galactic-science surfaces; six experimental traits; seven ancient unlocks; seven infinite-research kinds; eight collection kinds; seven archive features; two forbidden outcomes — the AF-076/078/080 roster move applied to science)* | Complete — spec + implementation (`src/game/research/researchRosterData.ts`): the ecosystem is DATA on unchanged AF-024/069/081 shapes — the tree grows fourteen → eighteen and EVERY addition is PURE GAMEPLAY (unlockFlag effects; the tree's numeric-effect count asserted UNCHANGED by this module); THREE FIRST-PRODUCER categories filled (Drone Doctrine → droneEngineering, Gene Tempering → commanderDevelopment, Void Containment → voidResearch — all registered-empty since AF-024) plus THE AFTERLIGHT NETWORK chained behind the hidden Ancient Conduit (the spec's §Ancient Research, literally); all four additions carry complete AF-081 twelve-part profiles through the unchanged architecture function; entries identity-only (no stat field by key inspection) with the LAB EQUALITY law (spec lab → AF-081 lab === profile lab, exact for all eighteen); TIER AFFECTS OPPORTUNITY NEVER NUMBERS — six tiers carrying projects, legendary/transcendent honestly registered-empty; the network draws on ≥6 distinct sources ("knowledge comes from playing the game" as breadth); INFINITE RESEARCH rides AF-069's REAL geometric cost engine (`infiniteResearchProjectFor` priced by researchNodeCostFor exactly, all seven kinds cycling, one hundred distinct never-flattening projects proven); the SCIENTIFIC ARCHIVE is derivation (`scientificArchiveFor`, all seven features from data every project carries, asserted for all eighteen); LIVE — composition root upgraded to the eighteen-project tree through the unchanged engine (validation, hidden reveal, DR-005 all intact); browser-verified — research line on the full roster, zero page errors | Aligned | 966 tests green (11 new) incl. "complete every research path" executed LITERALLY (all eighteen through the real engine, the ancient chain end-to-end) + the unchanged-numeric-count proof + the hundred-project infinite ladder on AF-069's exact costs + a 1,000-career seeded sweep (unlocks never regress; the ledger never drifts; the Network NEVER fires before the conduit) | Approved 9.5/10 | **LOCKED** |
| AF-083 | Mission Framework | v1 *(fourteen architecture parts; seventeen spec categories mapped TOTALLY onto AF-037's fifteen; nine structure phases each naming a LIVE seam — no second state machine; eight objective kinds on AF-037's TWO engine mechanisms; nine generation inputs + eight variation sources each naming the live system they draw from; eight spec events mapped onto AF-037's ten (already bound to the real environmental vocabulary); ten reward kinds with honesty flags; five failure consequences; nine history fields; two forbidden outcomes — the AF-071→081 profile precedent applied to expeditions)* | Complete — spec + implementation (`src/game/missions/missionFrameworkData.ts`): AF-037's engine untouched — `MissionProfileDef` wraps each expedition BY ID (framework category, threat budget, faction presence, environmental systems, narrative hooks, world-state key, reward kinds, hooks) with `missionArchitectureFor` proving all fourteen parts ("nothing remains undefined" as a function); the roster gains its FIRST expeditions beyond the sandbox — WINTERLINE RESCUE (rescue/Frozen Reach/T3, civilian recovery with a stay-under optional) and FIRST LIGHT EXCAVATION (ancientVault/Ancient Core/T6, real boss potential on the Sentinel seam) — with biomes resolving against REAL biome defs, bosses against AF-035, factions against AF-039; THREAT BUDGET STRICTLY MONOTONE with difficulty (120→320→600); Mission Selection went LIVE — all expeditions selectable with category/difficulty/budget previews (§Accessibility), the selected template feeding the real generator and runtime, the overlay mission line showing framework category + budget + rolled modifiers; the Winterline Rescue COMPLETED through AF-037's real runtime in tests (rescue counters, stay-under revocation permanent, optionals never blocking); browser-verified — `Winterline Rescue (rescue · budget 320) [Spawn] · modifiers [doubleRewards]` live, zero page errors | Aligned | 976 tests green (10 new) incl. the three total maps with the environmental-vocabulary chain asserted end-to-end + 3,000 generated expeditions (deterministic per seed, modifiers always from the template's own pool, ids unique, variation real) + a 1,000-career seeded runtime sweep (progress monotone; stay-under never resurrects) | Approved 9.5/10 | **LOCKED** |
| AF-084 | Mission Roster Framework | v1 *(seventeen mission families mapped TOTALLY onto AF-083's seventeen categories with a per-expedition three-layer binding; eight tiers; ten objective-network surfaces onto AF-037's objective types; eight world-reactivity surfaces naming live systems; nine dynamic events onto AF-037's ten; seven legendary features; seven chain kinds; ten generator inputs; eight collection kinds; eight Operations Centre features; two forbidden outcomes — the AF-076→082 roster move applied to expeditions)* | Complete — spec + implementation (`src/game/missions/missionRosterData.ts`): the ecosystem is DATA on unchanged AF-037/083 shapes — entries identity-only (family + tier, key-inspected); "TIER CHANGES COMPLEXITY AND REWARD QUALITY, NOT ENEMY STRENGTH" PROVEN in data (the common-tier Winterline Rescue asserted HARDER than the special-tier incursion — the tier ladder is provably not the difficulty ladder — while the ancient-tier excavation runs strictly more modifier slots than every lower tier; mythic/galaxyEvent honestly empty); CHAIN MISSIONS as ordered lists of REAL expeditions — THE VAULT SIGNAL (ancientMysteries) chains the incursion into the excavation via the pure `nextChainStageAfter`, walked end-to-end with terminal null; THE EXPEDITION LOG makes "every expedition becomes part of the player's personal history" LITERAL — `ExpeditionLogRuntime` permanent/append-only with monotone sequences, defeats remembered as honestly as victories, no-removal API by prototype inspection, WIRED at the real endRun seam; the GALAXY OPERATIONS CENTRE is derivation (`operationsCentreFor`, all eight features from the log + existing ledgers — replay honest via AF-037's seed determinism); browser-verified — a simulated defeat lands as `log 1 (0 perfect)` on the overlay mission line, zero page errors | Aligned | 985 tests green (9 new) incl. the three total maps + the tier-is-not-enemy-strength proof + the chain battery + the append-only/no-removal log proofs + 3,000 expeditions with globally unique instance ids + a 1,000-career seeded log sweep (sequence strictly monotone; perfect ≤ victories; the ledger never drifts) | Approved 9.5/10 | **LOCKED** |
| AF-085 | Faction Framework | v1 *(fifteen architecture parts; the spec's ten major factions as a CIVILISATION REGISTER realised entirely through existing things — six diplomatic factions on AF-039's register, three enemy civilisations in AF-026's real codex (Conclave/Paragon/Eclipsed), and the Afterlight Initiative as the PLAYER'S OWN ORGANISATION bound to AF-068's campaign — NO ELEVENTH FACTION; eight reputation tracks, eight interaction routes, six evolution drivers, seven political instruments mapped TOTALLY onto CONFLICT_STATES, eight economy sectors, seven territory effects, nine reward realisations, seven accessibility surfaces — the AF-071→084 profile precedent applied to civilisations)* | Complete — spec + implementation (`src/game/factions/factionFrameworkData.ts`): AF-039's engine untouched — `FactionProfileDef` wraps each profiled faction BY ID with the identity uniques the def didn't carry (architecture style, music theme, dialogue voice, signature reward kind, economy sectors, hooks); `factionArchitectureFor` proves all fifteen parts; "NO FACTION SHOULD OVERLAP EXCESSIVELY" is a UNIQUENESS LAW — five identity axes asserted pairwise-distinct across all six profiled civilisations, with signature rewards as REAL FactionRewardDef kinds reflecting identity (Guild→commanders, Nomads→ships, Custodians→lore) and relics HONESTLY realised as in-run AF-029 drops (no new kind forced into the locked union); politics ride the REAL engine — instruments drive setRelationship with symmetry/self-alliance/default invariants; "FACTIONS EVOLVE WITHOUT THE PLAYER" is a DETERMINISM TEST (same seed → identical event histories on elapsed time alone); the reputation ladder proven monotone across its full clamped range; the encyclopedia is derivation; LIVE — the overlay factions line shows `profiled 6/10 · civs 10 (6 diplomatic)`; browser-verified, zero page errors | Aligned | 995 tests green (10 new) incl. the no-eleventh-faction register battery + the five-way uniqueness law + the monotone ladder over the full clamped range + the player-independent deterministic event engine + a 1,000-galaxy seeded simulation through the REAL FactionRuntime (symmetry and self-alliance never break; instruments always apply; events stay on the shelf) | Approved 9.5/10 | **LOCKED** |
| AF-086 | Living Faction Ecosystem | v1 *(twelve-part Civilisation Model realising AF-039's ten registered-but-never-ticked attributes + four NEW live registers; eight-phase Life Cycle as a closed ring; eight Diplomatic AI factors; ten Galactic Events with authored deltas; nine relationship kinds mapped TOTALLY onto AF-039's eight conflict states; eight warfare kinds with a hard-capped player-influence term; eight economic outputs; eight scientific focuses mapped onto AF-082's fifteen-discipline roster; the SIXTH specialisation uniqueness axis; eight historical record kinds; eight player-impact surfaces, all bounded; seven Council session kinds honestly deferred — the first true background CIVILISATION SIMULATION, over unchanged AF-039/085 shapes)* | Complete — spec + implementation (`src/game/factions/livingEcosystemData.ts` + `CivilisationSimulationRuntime.ts`): AF-039/085 untouched — `MODEL_PART_REALISATION` names how every one of the twelve spec parts is realised (existing attribute / new register / existing reference); the LIFE CYCLE is a closed ring (Transformation→Expansion) with FORCED advancement after a bounded epoch count — "no civilisation remains unchanged forever" as a guarantee, proven across 500 simulated epochs; `diplomaticDecisionFor` scores all eight spec factors from REAL state and returns the reasoning alongside the score, applying through AF-039's UNCHANGED `setRelationship`; WARFARE hard-caps player influence (`PLAYER_WAR_INFLUENCE_CAP` 0.2) — an absurd fed input still clamps — and always resolves through the real engine to `ceasefire`, logged to history; GALACTIC EVENTS carry authored attribute-deltas (the mission-modifier pattern) with four of ten bound to REAL AF-039 faction events, no new bus vocabulary; `GalacticHistoryRuntime` is AF-084's append-only log pattern turned on the galaxy itself; Scientific Priority is the SIXTH uniqueness axis (AF-085's five plus this one), mapped onto AF-082's REAL roster, pairwise-distinct across all six profiled civilisations; "NO FACTION PERMANENTLY DOMINATES" is a SIMULATED PROOF — staggered starting phases + a 50-seed×500-epoch sweep confirm the epoch-100 leader is not always the epoch-500 leader; the Galactic Council is HONESTLY registered with zero voting behaviour (the spec's own deferral); LIVE — `civSim` ticks on the same ambient fixed-loop schedule as factionRuntime/galaxyRuntime/marketRuntime, Support/Oppose feed the bounded politics impact surface, the factions overlay line reads stability/growth/wars/econ/sci/history; browser-verified ticking from baseline to a real first epoch over wall-clock time, zero page errors | Aligned | 1006 tests green (11 new) incl. the closed-ring life-cycle proof + the eight-factor diplomatic scoring battery + the bounded-player-influence proof with an absurd input + a full war declared/resolved/logged through the real engine + the permanent-history append-only/no-removal battery + a 50-seed × 500-epoch simulation sweep (attributes always clamped, phases always advance, history only grows, leadership demonstrably rotates) | Approved 9.5/10 | **LOCKED** |
| AF-087 | Codex Framework | v1 *(fourteen architecture parts; the spec's seventeen primary categories REALISED — twelve onto AF-043's real category shelf, five onto EXISTING system references (AF-038 galaxy systems, AF-082's REAL afterlight-network research node, AF-039/086's void faction+event) with NO union widening; nine discovery routes; ten entry-structure sections (eight derived, two new profile fields); the six-stage Discovery Progression ladder; eight multimedia kinds with an honesty flag; eight timeline-coverage items mapped onto AF-043's nine eras; one relation mechanism read eight ways; eight collection-reward realisations onto AF-026's REAL CosmeticRewardKind union or existing systems; six Player Journal surfaces — the AF-071→086 profile precedent applied to the Codex, plus its own new progression + journal runtimes)* | Complete — spec + implementation (`src/game/codex/codexFrameworkData.ts` + `CodexProgressionRuntime.ts`): AF-043's engine untouched — `CodexEntryProfileDef` wraps every entry BY ID with subcategory, discovery method, gameplay information, scientific notes, audio ref, hooks; `codexArchitectureFor` proves all fourteen parts for EVERY one of the real forty sandbox entries (hand-authored or deterministically derived — the derivation never collides with a hand-authored profile); the DISCOVERY PROGRESSION LADDER is the module's one new mechanical surface — `CodexDiscoveryRuntime`, the SIXTH monotone-lattice appearance, riding on top of AF-043's UNCHANGED binary unlock gate (the lattice never checks isUnlocked itself — the same decoupling every prior collection runtime keeps), no-removal API asserted, a 1,000-career regression sweep and a 200-seed SHUFFLED-VISITING-ORDER sweep both hold — "complete the Codex repeatedly" executed literally; THE PLAYER JOURNAL is real and distinct from the galaxy's own history — three independent toggle sets (pins/bookmarks/favourites), free-text notes, a PERMANENT Discovery History and a deliberately BOUNDED Search History (fifty entries, oldest dropped — explicitly NOT AF-084/086's permanent galaxy history, by design); `relatedKnowledgeKindFor` classifies the ONE existing relatedEntryIds mechanism eight ways with an honest null for the unclassifiable; Collection Rewards realise onto AF-026's REAL reward union or existing systems (Museum Displays → AF-078's derivation, Historical Records → AF-086's GalacticHistoryRuntime), zero new reward kinds; LIVE — every newly-unlocked entry auto-records Observed on the existing per-frame section-completion seam, completing a section masters every entry inside it, and the Statistics screen gained a REAL "Pin Featured Discovery" button; browser-verified end to end — click → `Journal: 1 pinned … Featured: The Archive (pinned)`, overlay reading `observed 13 · mastered 13 · journal 0pin/0bm/0fav` at boot, zero page errors | Aligned | 1019 tests green (13 new) incl. full 14-part+10-section completeness for all forty real entries + the no-union-widening realisation battery + the monotone-lattice skip-ahead/no-removal proofs + a 1,000-career regression sweep + the full player-journal battery with the search-history cap proven exactly + the 200-seed shuffled-order "complete the Codex repeatedly" sweep | Approved 9.5/10 | **LOCKED** |
| AF-088 | Codex Ecosystem | v1 *(seventeen Knowledge Web nodes — fifteen onto AF-087's primary-category register, two reaching further into AF-043's REAL timeline/events shelves; eight-tier Discovery ladder COMPOSED around AF-087's unchanged six-stage lattice (Detected before, Archived after — requiring Mastered first); eight Scientific Archive kinds; eight Museum exhibit kinds with an honesty flag; ten Timeline Archive items mapped onto AF-043's nine eras with one honestly DYNAMIC exception; eight Discovery Reward realisations; eight Expedition Journal kinds — AUTOMATICALLY fed, never player-curated; the three NEW Player Notebook surfaces AF-087 doesn't carry; eight REAL search functions; twelve Collection Trackers; six Community Support items honestly deferred per AF-000's own constitution — the AF-076→086 ecosystem move applied to the Codex a second time)* | Complete — spec + implementation (`src/game/codex/codexEcosystemData.ts` + `CodexEcosystemRuntime.ts`): AF-043/087 untouched — `CodexArchiveRuntime` WRAPS an AF-087 `CodexDiscoveryRuntime` INSTANCE by composition (never modification), proving the six shared tiers preserve AF-087's own order exactly (Analysed = the identical "studied" ordinal slot, its own vocabulary still readable through its own unmodified API) and that Archived requires Mastered first, permanently; `knowledgeWebFor` turns the ONE flat relatedEntryIds mechanism into a genuine seventeen-bucket graph, verified against the real forty-entry roster; TWO MORE permanent ledgers (`ScientificArchiveRuntime`, `ExpeditionJournalRuntime` — the seventh/eighth append-only appearance) with the Expedition Journal AUTOMATICALLY fed at three REAL seams (the unlock-check, the real endRun, the real research-unlock callback) — the spec's own automatic-vs-curated split, honoured exactly; the PLAYER NOTEBOOK is COMPLETED not duplicated — AF-087's four existing surfaces verified untouched, AF-088's three new ones (comparison notes, research goals, expedition plans) in their own extension runtime; eight Search System kinds are REAL functions tested against the real roster; Collection Tracking's overall figure asserted to EQUAL AF-043's own discoveryPercent exactly (no drift); COMMUNITY SUPPORT HONESTLY DEFERRED — a registered list, zero networking code, matching AF-000's own offline-first rule; LIVE — the Discoveries/Scientific-Papers feed fires on AF-087's existing per-frame seam, Mission-History/Bosses feed the real endRun, Recovered-Research feeds the real research-unlock callback, and the Statistics screen gained a real "Set/Clear Research Goal" button beside AF-087's Pin; browser-verified end to end — `Journal: … Featured: The Archive (not pinned, goal)`, overlay reading `archived 0 · museum 83% · sci-archive 13 · expo-journal 13 · goals 0` at boot, zero page errors | Aligned | 1035 tests green (16 new) incl. the knowledge-web real-roster bucketing proof + the eight-tier structural-containment proof + the composition proofs for CodexArchiveRuntime + the notebook completion-not-duplication assertion + all eight search functions against the real roster + the collection-completion identity check + a 500-seed shuffled-order sweep driving every one of the forty real entries from Unknown to Archived through BOTH runtimes together without ever regressing | Approved 9.5/10 | **LOCKED** |
| AF-089 | Galactic Economy Framework | v1 *(thirteen-part Economy Architecture; seventeen resource categories — nine onto AF-025's REAL ResourceType shelf, eight honest new stockpile registers; eight-stage Production Chain as a CLOSED RING (the AF-086 life-cycle pattern applied to goods); eight Trade Network routes mapped TOTALLY onto AF-040's real merchant kinds + one economic event; nine Colonies attributes (six onto AF-086's real simulated state, three new); ten Industries with primary-output bindings; eight bounded Player Participation actions; eight Market System price drivers; nine Galaxy Events (five REUSED from AF-085/086, four authored); eight Infrastructure kinds; eight Reward realisations; two forbidden outcomes — the industrial substance behind AF-086's own economic attributes)* | Complete — spec + implementation (`src/game/economy/galacticEconomyData.ts` + `GalacticEconomyRuntime.ts`): AF-025/039/040/085/086 untouched — `GalacticEconomyRuntime` takes AF-086's REAL CivilisationSimulationRuntime as a collaborator, reading population/industrialOutput/technology/economicPower directly and writing back ONLY through the real bounded feedPlayerImpact (ambient events never touch AF-086's state — strictly one-directional); the PRODUCTION CHAIN is a proven CLOSED RING (Recovery→Extraction, every stage reachable from any start, forced advancement after a bounded epoch count, stockpile increasing ONLY at Distribution/Recovery — nothing manufactured from nothing); SCARCITY IS REAL — population-driven consumption zeroes stockpile on shortfall and dents growth/employment, proven over real epochs with AF-086's own state asserted untouched; Trade Network routes map TOTALLY onto AF-040's merchant kinds (Emergency Supply reaching one layer further into AF-040's own economic events); of nine Galaxy Events, FIVE REUSE a real AF-085/086 mechanism directly (tradeBoom/economicCollapse/scientificRevolution/piracy/tradeEmbargo) and FOUR author their own resource deltas — never re-firing another module's engine; PRIMARY INDUSTRY is a SEVENTH identity-uniqueness axis (AF-085's five + AF-086's sixth + this one), pairwise-distinct across all six profiled civilisations; every Player Participation action is BOUNDED (AF-086's "accelerate, never dictate" law again), two reaching into AF-086's own bounded feed for a DOUBLE cap; Invest in Development permanently builds real infrastructure past a flat threshold; the Market multiplier COMPOSES with AF-040's own pricing, never replacing it; LIVE — the economy ticks on the same ambient schedule as civSim/factionRuntime/marketRuntime, a victorious expedition delivers real resources to the mission's present faction's colony at the real endRun seam, the overlay economy line reads supply/demand/routes/industry/pop/health; browser-verified ticking over real wall-clock time (`supply 1530→1421 · pop 60→61`), zero page errors | Aligned | 1052 tests green (17 new) incl. the closed-ring production-chain proof + the scarcity battery with AF-086 state asserted untouched + the primary-industry uniqueness assertion + every bounded action incl. double-capped cross-module feeds + the market-multiplier bounds proof + a 50-galaxy × 300-epoch simulation sweep (stockpiles never negative; chain always valid; health always in [0,100]) | Approved 9.5/10 | **LOCKED** |
| AF-090 | Civilisation Framework | v1 *(thirteen-part Civilisation Architecture; ten settlement types realised onto AF-089's real infrastructure/industry shelves or AF-087's primary categories; ten population stats (three reusing AF-086/089's real registers, seven new); ten settlement-infrastructure upgrades (four sharing an id with AF-089's real InfrastructureKindDef shelf, six new finer-scoped); eight Galaxy Restoration routes; nine research-gateable Megastructures sited at REAL AF-038 regions; seven-stage Colony Development as a LINEAR monotone ladder — the first non-ring growth mechanic in the chain; eight bounded Player Investment actions (four delegating to AF-089's real methods); ten Civilisation Specialisations; eight World Evolution surfaces; seven Galactic Projects; eight Reward realisations; two forbidden outcomes — six named settlements giving the rebuild a visible face)* | Complete — spec + implementation (`src/game/civilisation/civilisationFrameworkData.ts` + `CivilisationFrameworkRuntime.ts`): AF-024/038/039/085/086/087/089 untouched — six settlements sited at REAL AF-038 systems whose region matches each faction's own AF-039 territory EXACTLY, one per profiled civilisation, settlement type PAIRWISE-DISTINCT (the EIGHTH identity-uniqueness axis); the LADDER IS LINEAR, deliberately never a ring — Legendary Status a true ceiling with an Infinity threshold never even evaluated (the runtime stops tracking progress on arrival), thresholds strictly ascending, proven to advance at most one stage per epoch over 400 epochs; HISTORICAL TIMELINE needed NO fifth ledger — every milestone logs directly to AF-086's REAL GalacticHistoryRuntime; settlement types/population stats/infrastructure upgrades all realise onto EXISTING AF-087/089 shelves where one fits, honest new finer-scoped registers where none does; PLAYER INVESTMENT is bounded a THIRD time (AF-086's law again) — FOUR of eight actions DELEGATE to AF-089's own real bounded methods (proven double-capped with an absurd input) rather than reimplementing them; Influence Priorities is PERMANENT (once at Specialisation, never switchable); MEGASTRUCTURES are genuinely research-gated — proven refused pre-unlock and permitted post-unlock through the REAL AF-024/082 engine (walking the actual prerequisite chain, the AF-081/082 pattern reused); Specialisation's favoured stat is proven to grow measurably faster than an unfavoured one; LIVE — settlements/megastructures tick on the same ambient schedule as civSim/galacticEconomy, a mission victory now funds BOTH the AF-089 colony AND the AF-090 settlement at the same real endRun seam, the galaxy overlay line reads settlements/pop/upgrades/construction/dev/rating/megastructures; browser-verified ticking over real wall-clock time (`construction 0%→1% · pop 60→61`), zero page errors | Aligned | 1070 tests green (18 new) incl. the linear-ladder no-skip/no-regression proof + the historical-timeline reuse proof + all four delegating investment actions double-capped + the megastructure research-gate proven both ways through the real engine + a 30-galaxy × 500-epoch simulation sweep (construction never regresses; rating always in [0,100]) | Approved 9.5/10 | **LOCKED** |
| AF-091 | Audio Framework | v1 *(11-part cue architecture; 15 primary categories, 12 onto AF-045's real shelf + 3 new; adaptive-music layers + occlusion composed over resolveMusicState/distanceAttenuation; biome/faction sound identities with a generated fallback; combat priorities proven against the real VoicePool; 6 boss-music stages, 4 reused; voice/exploration bindings across 8 prior modules)* | Complete — `audioFrameworkData.ts`: AF-045 untouched; cue profiles complete for all 11 parts; composed pure functions extend resolveMusicState/distanceAttenuation; live tension % on the audio overlay line, browser-verified | Aligned | 1079 tests green (9 new) | Approved 9.5/10 | **LOCKED** |
| AF-092 | Visual Direction Framework | v1 *(8 pillars/8 art influences/7 colour elements per faction/8 biome elements/8 materials/8 lighting/9 VFX/8 cinematic/7 animation/7 camera/8 photo mode/7 accessibility/5 performance; faction colour signatures = 9th identity-uniqueness axis; biome visual identity with a generated fallback; VFX min-priority table mirrors AF-091's combat priorities; materials/cinematic realise onto real AF-025/044/045/016/068/072/087/089/090/038/041 systems)* | Complete — `visualDirectionData.ts`: no locked module touched; `colourSignaturesAreDistinct()` proven disjoint from AF-007's rarity ladder too; `biomeVisualIdentityFor` hand-authored + generated; live lighting/skybox text appended to the biome overlay line, browser-verified | Aligned | 1087 tests green (8 new) | Approved 9.5/10 | **LOCKED** |
| AF-093 | User Experience Framework | v1 *(8 pillars/9 UI architecture parts/14 primary interfaces/11 HUD elements/8 contextual triggers/5 nav devices/7 feedback channels/8 inventory features/7 build kinds/7 notification categories/7 onboarding features/11 accessibility surfaces/5 performance disciplines/6 debug surfaces; Primary Interfaces realised 3 live GameStateId / 4 fused into a hosting state / 7 honest future; build kinds realised onto AF-027's real Loadout; notifications realised onto the real lootNotices queue)* | Complete — `uxFrameworkData.ts`: no locked module touched; every one of 14 interfaces carries a complete 9-part profile via `uiArchitectureFor`; live UX summaries (navigation/HUD/build counts, a genuine frame-time input-latency proxy) appended to the existing gameState/input/inventory overlay lines, browser-verified | Aligned | 1095 tests green (8 new) | Approved 9.5/10 | **LOCKED** |
| AF-094 | Technical Architecture Framework | v1 *(8 architecture principles/14 core modules/13 data domains/15 save categories/7 save-compatibility features/8 event categories/7 plugin points/8 configuration areas/8 testing kinds/7 crash-recovery features/8 analytics categories/8 dev tools/7 accessibility-persistence properties/7 performance disciplines/7 debug surfaces; a "meta" module realising the spec onto the real 44-Runtime codebase, EventBus/GameEvents, SaveSlice's versioned/checksummed engine, and Pool<T> rather than inventing 14 redundant manager classes)* | Complete — `technicalArchitectureData.ts`: no locked module touched; Core Modules realised 1 existing/8 renamed/2 distributed/1 orphaned (GameManager exists, unwired)/2 future (Visual — no renderer exists; Analytics — none exists); event-category bindings type-checked against real `keyof GameEvents`; `eventQueueSummary` proven against a real `EventBus` with a 120-seed sweep; live module/event-queue counts and real per-slice schema versions appended to the gameState/saveFramework overlay lines, browser-verified | Aligned | 1107 tests green (12 new) | Approved 9.5/10 | **LOCKED** |
| AF-095 | Master Quality Assurance Framework | v1 *(10-stage QA pipeline/17 validation domains/12 gameplay-testing domains/9 performance metrics/8 accessibility-validation checks/7 lore-validation checks/9 content-pipeline stages/8 balance metrics/8 release-pipeline stages/8 live-monitoring categories/7 dashboard features/6 QA-accessibility categories/6 debug surfaces; a second "meta" module delegating to AF-094's real registries for Performance Validation rather than re-declaring them)* | Complete — `qualityAssuranceData.ts`: no locked module touched; QA + release pipelines both proven LINEAR (no cycling, always reaches the terminal stage); the honest scale gap (200,000 largest known sweep vs. the spec's 1,000,000 target) registered via `simulationScaleGapSummary` rather than papered over; Lore/Balance Validation bound to real structural cross-module proofs (AF-090 settlement↔territory, AF-086 append-only history, AF-088 Knowledge Web, roster balance axes); live QA/regression/accessibility/release-readiness summaries appended to the meta overlay line, browser-verified | Aligned | 1120 tests green (13 new) | Approved 9.5/10 | **LOCKED** |
| AF-096 | Development Constitution | v1 *(doc-only governance module — no Debug section in spec, no code; Core Philosophy/Design Pillars/Non-Negotiable Rules/Gameplay+Content+Technical+Creative Governance/Quality Standard/World Consistency/Player Experience/Expansion Governance/Permanent Review Questions/Success Metrics)* | Complete — `docs/DEVELOPMENT_CONSTITUTION.md`: slots into the authority chain beneath AF-000's Master Constitution and AF-014's Master Design Constitution as the permanent review lens for every AF-016+ module; every overlap with an existing authority (AF-014's Ten Commandments/Player Promise, the Master Constitution's own Afterlight Test) is named as the same law, not restated as a second one; gives the 2026-07-09 art-style ruling its permanent home under World Consistency §Art Direction | Aligned | No code — doc-only | Approved 9.5/10 | **LOCKED** |
| AF-097 | Content Production Framework | v1 *(13-stage Master Content Pipeline/22 supported-content kinds/14 Content Template fields/11 dependency targets/15-step implementation order/8 Content Scale targets/8 automated-validation checks/7 quality-target traits; no Debug section in spec — a real, tested library, not a live runtime addition)* | Complete — `contentProductionData.ts`: no locked module touched; pipeline + implementation order both proven LINEAR; 17/22 supported-content kinds already have a real registry (Planets/Dialogue/Species/item-level Resources/authored Museum Entries are the 5 honest gaps); Content Scale reports real cited roster counts against the spec's 8 targets with no fabricated progress (bosses 1/250 furthest behind, enemy types 61/500 closest); 7/8 Automated Validation checks delegate to real AF-092/094/095 functions (only replayability is future) | Aligned | 1129 tests green (9 new) | Approved 9.5/10 | **LOCKED** |
| AF-098 | Commander Production Framework | v1 *(14-stage production pipeline/37-field "Every Commander Must Define" template/10 design standards/10 roster-diversity axes/12 personality traits (dialogue-only BY SHAPE)/8 recruitment methods/12 dialogue categories/7 mastery features/8 accessibility surfaces/4 performance disciplines; extends AF-030/071/072/026 unchanged)* + Roster Expansion Batch 1 (8 new commanders, 14→22 toward AF-097's 100+ target, Project Owner directed) | Complete — `commanderProductionData.ts`: no locked module touched; 21/37 template fields already real (the other 16 are shape-only); `PersonalityDialogueHint` has no bonus field, proven the same way as the real `CommanderRelationshipDef` shape law. `commanderExpansionRoster.ts`: 8 fully-detailed new commanders (Wardbreaker/Lanternkeep/Static/Halcyon/Chorus/Fulcrum/Driftline/Aftercare) each with real content for all 16 new template fields, proven distinct from the full 22-commander roster via the real fingerprint/architectureFor laws, genuinely recruitable through the real RosterRuntime on AF-072's unchanged recruitment sources; live `roster 3/22` on the commander overlay line, browser-verified | Aligned | 1151 tests green (22 new) | Approved 9.5/10 | **LOCKED** |
| AF-099 | Commander CMD-001 — Dr. Lyra Voss "The Pathfinder" | v1 *(the first individually-specified, fully production-ready Commander — Commander Identity/Core Fantasy/Personality/Visual Design/Animation/Voice/Music Motif/Passive/Ability One+Two/Ultimate/Signature Mechanic/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships/Mastery Track/Strengths+Weaknesses/AI Behaviour/Dialogue Examples/Codex Summary/Museum Entry/Accessibility, all conforming to AF-071/072/098)* | Complete — `cmd001LyraVoss.ts`: no locked module touched; every field realised onto AF-030/071's real shapes or AF-098's `CommanderExpandedProfileDef`; proven distinct from the full 22-commander roster via the real fingerprint/architectureFor laws; recruits via the real `campaign` source gated on "The Silent Observatory"; her Field Commander branch gives `droneEffectiveness` a second real producer; a second real Codex commander entry added additively; all 20 Preferred-X ids and her 5 spec dialogue lines carried exactly; structural weaknesses (no damage/criticalDamage bonus) proven by test; live `roster 3/23` and `codex 16/51` on the overlay, browser-verified | Aligned | 1162 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-100 | Commander CMD-002 — Adrian Kane "The Vanguard" | v1 *(the second individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships incl. Deep Respect for CMD-001/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd002AdrianKane.ts`: no locked module touched; proven distinct from the full 23-commander roster via the real fingerprint/architectureFor laws; his Deep Respect for Dr. Lyra Voss is a real cross-commander relationship binding to her actual roster id, and his Codex entry cross-references hers; recruits via the real `campaign` source gated on "The Last Bastion"; all 20 Preferred-X ids and 5 spec dialogue lines carried exactly; structural weaknesses proven by test; live `roster 3/24` and `codex 16/52` on the overlay, browser-verified | Aligned | 1173 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-101 | Commander CMD-003 — Elias Ryker "The Engineer" | v1 *(the third individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships incl. Close Friend CMD-001 + Professional Respect CMD-002/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd003EliasRyker.ts`: no locked module touched; his signature gives `droneEffectiveness` a third real producer alongside `kite-aviary` and CMD-001's talent branch; proven distinct from the full 24-commander roster via the real fingerprint/architectureFor laws; his Close Friend/Professional Respect relationships bind to CMD-001/CMD-002's real roster ids, and his Codex entry cross-references both — the roster's first three-way continuity web; recruits via the real `campaign` source gated on "Echoes of the Foundry"; live `roster 3/25` and `codex 16/53` on the overlay, browser-verified | Aligned | 1184 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-102 | Commander CMD-004 — Seraphina Cael "The Quantum Weaver" | v1 *(the fourth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with all 3 prior commanders/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd004SeraphinaCael.ts`: no locked module touched; proven distinct from the full 25-commander roster via the real fingerprint/architectureFor laws; her relationships to CMD-001/002/003 all bind to their real roster ids, and her Codex entry cross-references all three — the roster's first four-way continuity web; recruits via the real `research` source gated on "The Impossible Equation"; live `roster 3/26` and `codex 16/54` on the overlay, browser-verified | Aligned | 1194 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-103 | Commander CMD-005 — Kael Drake "The Hunter" | v1 *(the fifth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with exactly 3 of the 4 prior commanders/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd005KaelDrake.ts`: no locked module touched; proven distinct from the full 26-commander roster via the real fingerprint/architectureFor laws; his three spec'd relationships (Kane/Voss/Cael) bind to their real roster ids, with no relationship to CMD-003 invented since the spec is silent on that pairing; his Codex entry cross-references exactly those three; recruits via the real `campaign` source gated on "The Last Trail"; live `roster 3/27` and `codex 16/55` on the overlay, browser-verified | Aligned | 1204 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-104 | Commander CMD-006 — Aria Sol "The Resonant" | v1 *(the sixth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with exactly 3 of the 5 prior commanders/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd006AriaSol.ts`: no locked module touched; her "Crystal Ascendancy" faction is lore-consistent with `vane-chord`'s existing biography, no new faction invented; proven distinct from the full 27-commander roster via the real fingerprint/architectureFor laws; her three spec'd relationships (Cael/Voss/Ryker) bind to their real roster ids, with no relationship to Kane or Drake invented; recruits via the real `story` source gated on "The Crystal Choir"; live `roster 3/28` and `codex 16/56` on the overlay, browser-verified | Aligned | 1214 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-105 | Commander CMD-007 — Orion Vale "The Voidrunner" | v1 *(the seventh individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with exactly 3 of the 6 prior commanders/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd007OrionVale.ts`: no locked module touched; proven distinct from the full 28-commander roster via the real fingerprint/architectureFor laws; his three spec'd relationships (Cael/Voss/Kane) bind to their real roster ids, with no relationship to Ryker, Drake, or Sol invented; recruits via the real `legendaryMissions` source gated on "The Black Crossing"; live `roster 3/29` and `codex 16/57` on the overlay, browser-verified | Aligned | 1224 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-106 | Commander CMD-008 — Nova Iskander "The Swarmmaster" | v1 *(the eighth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with exactly 3 of the 7 prior commanders/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd008NovaIskander.ts`: no locked module touched; her passive/signature give `droneEffectiveness` a fourth real producer; proven distinct from the full 29-commander roster via the real fingerprint/architectureFor laws; her three spec'd relationships (Ryker/Kane/Sol) bind to their real roster ids, with no relationship to Voss, Cael, or Drake invented; recruits via the real `hiddenDiscoveries` source gated on "Hive Protocol"; live `roster 3/30` and `codex 16/58` on the overlay, browser-verified | Aligned | 1234 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-107 | Commander CMD-009 — Cassia Thorne "The Starforged" | v1 *(the ninth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with exactly 3 of the 8 prior commanders/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd009CassiaThorne.ts`: no locked module touched; proven distinct from the full 30-commander roster via the real fingerprint/architectureFor laws; her three spec'd relationships (Ryker/Iskander/Kane) bind to their real roster ids, with no relationship to Voss, Cael, Drake, Sol, or Vale invented; her faction and several Preferred-X ids resolve to real, existing Atlas Dynamics content already in the game; recruits via the previously-unused `exploration` source gated on "The Burning Foundry"; live `roster 3/31` and `codex 16/59` on the overlay, browser-verified | Aligned | 1244 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-108 | Commander CMD-010 — Aurelion Vex "The Chronomancer" | v1 *(the tenth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with exactly 3 of the 9 prior commanders/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd010AurelionVex.ts`: no locked module touched; proven distinct from the full 31-commander roster via the real fingerprint/architectureFor laws; his three spec'd relationships (Cael/Voss/Sol) bind to their real roster ids, with no relationship to Kane, Ryker, Drake, Vale, Iskander, or Thorne invented; first real use of the "haunted" personality trait; several Preferred-X ids resolve to strong real-data ties (Quantum Horizon prototype equipment, real scienceVessel/frigate hull classes, the real freeze-status weapon, the "Axiom" singularity biome); recruits via the real `research` source gated on "The Broken Hour"; live `roster 3/32` and `codex 16/60` on the overlay, browser-verified | Aligned | 1254 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-109 | Commander CMD-011 — Valen Ash "The Tempest" | v1 *(the eleventh individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with exactly 3 of the 10 prior commanders/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd011ValenAsh.ts`: no locked module touched; proven distinct from the full 32-commander roster via the real fingerprint/architectureFor laws; his three spec'd relationships (Sol/Voss/Vex) bind to their real roster ids, with no relationship to Kane, Ryker, Cael, Drake, Vale, Iskander, or Thorne invented; first real use of the `orbitalCommander` archetype and `assault` class; recruits via `factionReputation` — the last of AF-072's 7 recruitment sources to see real content — gated on "Eye of the Storm"; live `roster 3/33` and `codex 16/61` on the overlay, browser-verified | Aligned | 1264 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-110 | Commander CMD-012 — Nyx Korven "The Phantom" | v1 *(the twelfth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with exactly 3 of the 11 prior commanders/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd012NyxKorven.ts`: no locked module touched; proven distinct from the full 33-commander roster via the real fingerprint/architectureFor laws; her three spec'd relationships (Drake/Voss/Kane) bind to their real roster ids, with no relationship to Ryker, Cael, Sol, Vale, Iskander, Thorne, Vex, or Ash invented; first exact `recon`/`recon` archetype-class double-match; recruits via the real `story` source gated on "The Invisible War"; live `roster 3/34` and `codex 16/62` on the overlay, browser-verified | Aligned | 1274 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-111 | Commander CMD-013 — Dr. Mira Syn "The Bioforge" | v1 *(the thirteenth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with exactly 3 of the 12 prior commanders/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd013MiraSyn.ts`: no locked module touched; her passive's "Defeated biological enemies" wording maps literally onto the real `onKill` trigger; proven distinct from the full 34-commander roster via the real fingerprint/architectureFor laws; her three spec'd relationships (Sol/Voss/Cael) bind to their real roster ids, with no relationship to Kane, Ryker, Drake, Vale, Iskander, Thorne, Vex, Ash, or Korven invented; first real use of the `support` archetype; recruits via the real `research` source gated on "The Last Seed"; live `roster 3/35` and `codex 16/63` on the overlay, browser-verified | Aligned | 1284 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-112 | Commander CMD-014 — Rhea Solari "The Photon" | v1 *(the fourteenth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with exactly 3 of the 13 prior commanders/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd014RheaSolari.ts`: no locked module touched; proven distinct from the full 35-commander roster via the real fingerprint/architectureFor laws; her three spec'd relationships (Ash/Sol/Voss) bind to their real roster ids, with no relationship to Kane, Ryker, Cael, Drake, Vale, Iskander, Thorne, Vex, Korven, or Syn invented; first real use of the `assault` archetype; several Preferred-X ids resolve to strong real-data ties (`dawnspire`'s energyWeapons specialisation, `helios-prism-array`'s name, `solar-wastes`'s literal solar theme); recruits via the real `campaign` source gated on "The Dying Sun"; live `roster 3/36` and `codex 16/64` on the overlay, browser-verified | Aligned | 1294 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-113 | Commander CMD-015 — Zephyr Kain "The Singularity" | v1 *(the fifteenth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with exactly 3 of the 14 prior commanders/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd015ZephyrKain.ts`: no locked module touched; proven distinct from the full 36-commander roster via the real fingerprint/architectureFor laws; his three spec'd relationships (Cael/Vex/Voss) bind to their real roster ids, with no relationship to Kane, Ryker, Drake, Sol, Vale, Iskander, Thorne, Ash, Korven, or Syn invented; first real use of the `prototypePilot` archetype — closing out all 10 AF-030 archetypes; several Preferred-X ids resolve to strong real-data ties (`paragon-flux-driver`'s real singularity category, `singularity-keepsake`'s real singularity relic category, the "Axiom" singularity biome); recruits via the real `hiddenDiscoveries` source gated on "The Falling Sky"; live `roster 3/37` and `codex 16/65` on the overlay, browser-verified | Aligned | 1304 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-114 | Commander CMD-016 — Astrid Reyes "The Warden" | v1 *(the sixteenth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with exactly 3 of the 15 prior commanders/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd016AstridReyes.ts`: no locked module touched; per the spec's own "Reduce overlap with Adrian Kane" directive, her `support`/`hybrid` archetype/class and `onShieldBreak` passive trigger are deliberately distinct from Kane's `guardian`/`defender`/`onDamageTaken` kit, verified by a dedicated test; proven distinct from the full 37-commander roster via the real fingerprint/architectureFor laws; her three spec'd relationships (Kane/Syn/Iskander) bind to their real roster ids, with no relationship to Ryker, Cael, Drake, Sol, Vale, Thorne, Vex, Ash, Korven, Solari, or Kain invented; several Preferred-X ids resolve to strong real-data ties (`nova-warden-hive`'s literal "Warden" name, `warden-token`'s real name, `bastion-hull-mk1`'s real guardian-class frigate); recruits via the real `legendaryMissions` source gated on "The Final Evacuation"; live `roster 3/38` and `codex 16/66` on the overlay, browser-verified | Aligned | 1315 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-115 | Commander CMD-017 — Lucien Orion "The Starlancer" | v1 *(the seventeenth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with FOUR of the 16 prior commanders (not the usual three)/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd017LucienOrion.ts`: no locked module touched; proven distinct from the full 38-commander roster via the real fingerprint/architectureFor laws; his FOUR spec'd relationships (Ash/Drake/Kane/Voss) — the roster's first commander with four instead of three, and the first "Professional Rival" relationship (Drake) — all bind to their real roster ids, with no relationship to Ryker, Cael, Sol, Vale, Iskander, Thorne, Vex, Korven, Syn, Solari, Kain, or Reyes invented; first exact `assault`/`assault` archetype-class double-match; several Preferred-X ids resolve to strong real-data ties (`vanguard-thrusters`'s real thruster item, `sable-dart-mk1`'s real interceptor class/speed specialisation, `gambler-die`'s real +0.5 criticalDamage effect); recruits via the real `exploration` source gated on "The Redline"; live `roster 3/39` and `codex 16/67` on the overlay, browser-verified | Aligned | 1325 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-116 | Commander CMD-018 — Ivan Volkov "The Titan" | v1 *(the eighteenth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with exactly 3 of the 17 prior commanders/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd018IvanVolkov.ts`: no locked module touched; per the spec's own "Reduce overlap with Adrian Kane and Astrid Reyes" directive, his `engineer`/`assault` archetype/class and `onDamageTaken`/`shieldCapacity` passive are deliberately distinct from BOTH Kane's `guardian`/`defender`/`shieldRegeneration` and Reyes's `support`/`hybrid`/`shieldRegeneration` kits, verified by a dedicated test; proven distinct from the full 39-commander roster via the real fingerprint/architectureFor laws; his three spec'd relationships (Kane/Reyes/Thorne) bind to their real roster ids, with no relationship to Ryker, Cael, Drake, Sol, Vale, Iskander, Vex, Ash, Korven, Syn, Solari, Kain, or Orion invented; several Preferred-X ids resolve to strong real-data ties (`barrier-plate`'s real high-shieldCapacity module, `novasplitter`'s real Ironmoor Foundry tie, `machine-expanse`'s "Forge Primus" lore); recruits via the real `factionReputation` source gated on "The Iron Gate"; live `roster 3/40` and `codex 16/68` on the overlay, browser-verified | Aligned | 1336 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-117 | Commander CMD-019 — Selene Myrr "The Oracle" | v1 *(the nineteenth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with exactly 3 of the 18 prior commanders/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd019SeleneMyrr.ts`: no locked module touched; per the spec's own "Reduce overlap with reconnaissance and research Commanders" directive, her `support`/`support` archetype/class deliberately avoids the `recon` archetype used four times already (Voss/Drake/Vex/Korven), verified by a dedicated test; proven distinct from the full 40-commander roster via the real fingerprint/architectureFor laws; her three spec'd relationships (Vex/Voss/Cael) bind to their real roster ids, with no relationship to Kane, Ryker, Drake, Sol, Vale, Iskander, Thorne, Ash, Korven, Syn, Solari, Kain, Reyes, Orion, or Volkov invented; several Preferred-X ids resolve to strong real-data ties (Quantum Horizon manufacturer, real scienceVessel hull class, the "Axiom" singularity biome); recruits via the real `research` source gated on "The Infinite Equation"; live `roster 3/41` and `codex 16/69` on the overlay, browser-verified | Aligned | 1347 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-118 | Commander CMD-020 — Caelus Nova "The Architect" | v1 *(the twentieth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with FOUR of the 19 prior commanders (not the usual three)/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd020CaelusNova.ts`: no locked module touched; his primary faction "Atlas Dynamics" is his own first-ever primary-faction assignment away from Afterlight Initiative; per the spec's own "Reduce overlap with Elias Ryker and Nova Iskander" directive, his `orbitalCommander`/`support` archetype/class and passive/signature are deliberately distinct from BOTH Ryker's `engineer`/`engineer` and Iskander's `droneCommander`/`hybrid` kits, verified by a dedicated test; proven distinct from the full 41-commander roster via the real fingerprint/architectureFor laws; his FOUR spec'd relationships (Ryker/Thorne/Iskander/Voss) — the roster's second commander with four instead of three, and the first "Inspired By" relationship (Voss) — all bind to their real roster ids, with no relationship to Kane, Cael, Drake, Sol, Vale, Vex, Ash, Korven, Syn, Solari, Kain, Reyes, Orion, Volkov, or Myrr invented; several Preferred-X ids resolve to real Atlas Dynamics products matching his own faction; recruits via the real `campaign` source gated on "The First Foundation"; live `roster 3/42` and `codex 16/70` on the overlay, browser-verified | Aligned | 1358 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-119 | Commander CMD-021 — Talia Vega "The Echo" | v1 *(the twenty-first individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with FOUR of the 20 prior commanders (not the usual three)/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd021TaliaVega.ts`: no locked module touched; per the spec's own "Reduce overlap with Selene Myrr and Nyx Korven" directive, her `crystalSpecialist`/`scientist` archetype/class and passive/signature are deliberately distinct from BOTH Myrr's `support`/`support` and Korven's `recon`/`recon` kits, verified by a dedicated test; proven distinct from the full 42-commander roster via the real fingerprint/architectureFor laws; her FOUR spec'd relationships (Myrr/Voss/Korven/Iskander) — the roster's third commander with four instead of three — all bind to their real roster ids, with no relationship to Kane, Ryker, Cael, Drake, Sol, Vale, Thorne, Vex, Ash, Syn, Solari, Kain, Reyes, Orion, Volkov, or Nova invented; her Signal Relay ability gives `droneEffectiveness` a fifth real producer; several Preferred-X ids resolve to strong real-data ties (`static-node`'s humming-signal behaviour, `resonant-collectors`'s literal "resonant" name, echo/trace-themed biome lore); recruits via the real `hiddenDiscoveries` source gated on "The Silent Signal"; live `roster 3/43` and `codex 16/71` on the overlay, browser-verified | Aligned | 1369 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-120 | Commander CMD-022 — Darius Rhem "The Catalyst" | v1 *(the twenty-second individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with FOUR of the 21 prior commanders (not the usual three)/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd022DariusRhem.ts`: no locked module touched; per the spec's own "Reduce overlap with Photon and Tempest Commanders" directive, his `prototypePilot`/`experimental` archetype/class and passive/signature are deliberately distinct from BOTH Solari's `assault`/`scientist` and Ash's `orbitalCommander`/`assault` kits, verified by a dedicated test; proven distinct from the full 43-commander roster via the real fingerprint/architectureFor laws; his FOUR spec'd relationships (Thorne/Cael/Ryker/Voss) — the roster's fourth commander with four instead of three — all bind to their real roster ids, with no relationship to Kane, Drake, Sol, Vale, Iskander, Vex, Ash, Korven, Syn, Solari, Kain, Reyes, Orion, Volkov, Myrr, Nova, or Vega invented; several Preferred-X ids resolve to strong real-data ties (`coil-ripper-mk2`'s chainLightning behaviour, `cinder-heart`'s detonate-on-critical effect, `harmonic-overload`'s literal "Overload" name); recruits via the real `research` source gated on "The Domino Principle"; live `roster 3/44` and `codex 16/72` on the overlay, browser-verified | Aligned | 1380 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-121 | Commander CMD-023 — Eliana Ross "The Horizon" | v1 *(the twenty-third individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with FOUR of the 22 prior commanders (not the usual three)/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd023ElianaRoss.ts`: no locked module touched; per the spec's own "Reduce overlap with Lyra Voss" directive, her `droneCommander`/`support` archetype/class, passive/signature, and even her "fearless" personality trait (avoiding Voss's own "curious" despite spec'd "Highly Curious") are deliberately distinct from Voss's `recon`/`scientist` kit, verified by a dedicated test; proven distinct from the full 44-commander roster via the real fingerprint/architectureFor laws; her FOUR spec'd relationships (Voss/Orion/Nova/Vega) — the roster's fifth commander with four instead of three — all bind to their real roster ids, with no relationship to Kane, Ryker, Cael, Drake, Sol, Vale, Thorne, Vex, Ash, Korven, Syn, Solari, Kain, Reyes, Volkov, Myrr, or Rhem invented; several Preferred-X ids resolve to strong real-data ties (`wayfarer-hull-mk2`'s expedition lore, `warp-charting`'s real fast-travel unlock, `meridian-rest-frontier`'s literal "frontier" name); recruits via the real `exploration` source gated on "Beyond the Map"; live `roster 3/45` and `codex 16/73` on the overlay, browser-verified | Aligned | 1391 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-122 | Commander CMD-024 — Kieran Solace "The Diplomat" | v1 *(the twenty-fourth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with FOUR of the 23 prior commanders (not the usual three)/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd024KieranSolace.ts`: no locked module touched; per the spec's own "Reduce overlap with Selene Myrr and Astrid Reyes" directive, his `crystalSpecialist`/`defender` archetype/class and passive/signature are deliberately distinct from BOTH Myrr's `support`/`support` and Reyes's `support`/`hybrid` kits, verified by a dedicated test; proven distinct from the full 45-commander roster via the real fingerprint/architectureFor laws; his FOUR spec'd relationships (Reyes/Myrr/Vega/Kane) — the roster's sixth commander with four instead of three — all bind to their real roster ids, with no relationship to Ryker, Cael, Drake, Sol, Vale, Iskander, Thorne, Vex, Ash, Korven, Syn, Solari, Kain, Orion, Volkov, Nova, or Rhem invented; first real use of the "diplomatic" personality trait; recruits via the real `story` source gated on "The Last Embassy"; live `roster 3/46` and `codex 16/74` on the overlay, browser-verified | Aligned | 1403 tests green (12 new) | Approved 9.5/10 | **LOCKED** |
| AF-123 | Commander CMD-025 — Xanthe Oris "The Nanoforge" | v1 *(the twenty-fifth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with FOUR of the 24 prior commanders (not the usual three)/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd025XantheOris.ts`: no locked module touched; per the spec's own "Reduce overlap with Elias Ryker, Nova Iskander and Caelus Nova" directive — the roster's first TRIPLE overlap-reduction directive — her `guardian`/`experimental` archetype/class and passive/signature are deliberately distinct from Ryker's `engineer`/`engineer`, Iskander's `droneCommander`/`hybrid`, and Nova's `orbitalCommander`/`support` kits, verified by a dedicated test; proven distinct from the full 46-commander roster via the real fingerprint/architectureFor laws; her FOUR spec'd relationships (Ryker/Thorne/Iskander/Nova) — the roster's seventh commander with four instead of three — all bind to their real roster ids, with no relationship to any other prior commander invented; her Fabrication branch marks the sixth real `droneEffectiveness` producer; recruits via the real `hiddenDiscoveries` source gated on "The Grey Ocean"; live `roster 3/47` and `codex 16/75` on the overlay, browser-verified | Aligned | 1414 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-124 | Commander CMD-026 — Ronan Drake "The Sentinel" | v1 *(the twenty-sixth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with FOUR of the 25 prior commanders (not the usual three)/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd026RonanDrake.ts`: no locked module touched; per the spec's own "Reduce overlap with Adrian Kane, Astrid Reyes and Ivan Volkov" directive — the roster's second TRIPLE overlap-reduction directive — his `orbitalCommander`/`recon` archetype/class and passive/signature are deliberately distinct from Kane's `guardian`/`defender`, Reyes's `support`/`hybrid`, and Volkov's `engineer`/`assault` kits, verified by a dedicated test; proven distinct from the full 47-commander roster via the real fingerprint/architectureFor laws; his FOUR spec'd relationships (Volkov/Kane/Reyes/Iskander) — the roster's eighth commander with four instead of three — all bind to their real roster ids, with no relationship to any other prior commander invented; recruits via the real `story` source gated on "The Long Watch"; live `roster 3/48` and `codex 16/76` on the overlay, browser-verified | Aligned | 1425 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-125 | Commander CMD-027 — Sora Helix "The Alchemist" | v1 *(the twenty-seventh individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with FOUR of the 26 prior commanders (not the usual three)/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd027SoraHelix.ts`: no locked module touched; per the spec's own "Reduce overlap with Darius Rhem and Valen Ash" directive, her `engineer`/`scientist` archetype/class and passive/signature are deliberately distinct from BOTH Rhem's `prototypePilot`/`experimental` and Ash's `orbitalCommander`/`assault` kits, verified by a dedicated test; proven distinct from the full 48-commander roster via the real fingerprint/architectureFor laws; her FOUR spec'd relationships (Rhem/Syn/Oris/Cael) — the roster's ninth commander with four instead of three — all bind to their real roster ids, with no relationship to any other prior commander invented; strong real-data continuity find — `ember-core`/`frost-shard`/`cinder-heart` are literally elemental relics matching "Elemental Relics" exactly; recruits via the real `research` source gated on "The Impossible Formula"; live `roster 3/49` and `codex 16/77` on the overlay, browser-verified | Aligned | 1436 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-126 | Commander CMD-028 — Dorian Fen "The Beastmaster" | v1 *(the twenty-eighth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with FOUR of the 27 prior commanders (not the usual three)/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd028DorianFen.ts`: **owner-authorised name substitution** — the verbatim prompt's Full Name "Orion Vale" collides with the already-locked CMD-007 "Orion Vale 'The Voidrunner'" (AF-105), an unrelated character; flagged to the Project Owner before implementation, who chose to rename this Commander to Dorian Fen rather than implement a duplicate full name (see `docs/modules/AF-126-*.md` for the full flag/decision record); no locked module touched; per the spec's own "Reduce overlap with Mira Syn" directive, his `droneCommander`/`hybrid` archetype/class and passive/signature are deliberately distinct from Syn's `support`/`scientist` kit, verified by a dedicated test; proven distinct from the full 49-commander roster via the real fingerprint/architectureFor laws; his FOUR spec'd relationships (Syn/Ross/Helix/Reyes) — the roster's tenth commander with four instead of three — all bind to their real roster ids, with no relationship to any other prior commander invented; his companion kit gives `droneEffectiveness` its seventh real producer; recruits via the real `exploration` source gated on "The Last Guardian"; live `roster 3/50` and `codex 16/78` on the overlay, browser-verified | Aligned | 1448 tests green (12 new) | Approved 9.5/10 | **LOCKED** |
| AF-127 | Commander CMD-029 — Vega Noctis "The Voidwalker" | v1 *(the twenty-ninth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with FOUR of the 28 prior commanders (not the usual three)/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd029VegaNoctis.ts`: no locked module touched; per the spec's own "Reduce overlap with Zephyr Kain and Aurelion Vex" directive, her `voidSpecialist`/`experimental` archetype/class and passive/signature are deliberately distinct from BOTH Kain's `prototypePilot`/`scientist` and Vex's `recon`/`support` kits, verified by a dedicated test; her Ability One shares its display name "Phase Step" with CMD-007 Orion Vale's ability (not a structural conflict since active.id isn't part of the fingerprint law), so her active.id is namespaced (`void-phase-step`) to avoid a literal id collision, verified by a dedicated test; proven distinct from the full 50-commander roster via the real fingerprint/architectureFor laws; her FOUR spec'd relationships (Vex/Kain/Myrr/Ross) — the roster's eleventh commander with four instead of three — all bind to their real roster ids, with no relationship to any other prior commander invented; strong real-data continuity finds — `voidlance`/`veil-fragment`/`warp-charting`/`void-expanse` are all literal thematic matches; recruits via the real `hiddenDiscoveries` source gated on "Into Nothing"; live `roster 3/51` and `codex 16/79` on the overlay, browser-verified | Aligned | 1460 tests green (12 new) | Approved 9.5/10 | **LOCKED** |
| AF-128 | Commander CMD-030 — Lysandra Aether "The Celestial" | v1 *(the thirtieth individually-specified, fully production-ready Commander — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with FOUR of the 29 prior commanders (not the usual three)/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd030LysandraAether.ts`: no locked module touched; per the spec's own "Reduce overlap with Photon, Oracle and Horizon Commanders" directive — the roster's third TRIPLE overlap-reduction directive — her `orbitalCommander`/`hybrid` archetype/class and passive/signature are deliberately distinct from Solari's `assault`/`scientist`, Myrr's `support`/`support`, and Ross's `droneCommander`/`support` kits, verified by a dedicated test; proven distinct from the full 51-commander roster via the real fingerprint/architectureFor laws; her FOUR spec'd relationships (Voss/Myrr/Vex/Ross) — the roster's twelfth commander with four instead of three — all bind to their real roster ids, with no relationship to any other prior commander invented; recruits via the real `legendaryMissions` source (matching the spec's "Final Endgame" recruitment difficulty and its framing as the roster's culminating Commander), gated on "When Stars Remember"; strong real-data continuity find — `helios-prism-array` literally matches "Solar Prism"; live `roster 3/52` and `codex 16/80` on the overlay, browser-verified | Aligned | 1471 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-129 | Commander CMD-031 — Atlas Prime "The Founder" | v1 *(the thirty-first individually-specified, fully production-ready Commander and the roster's secret post-game capstone — full identity/Core Fantasy/Personality/Visual+Animation+Voice+Music/Passive/Ability One+Two/Ultimate/Signature/Talent Tree/6 Preferred-X categories/Recruitment+Legendary Mission/Relationships with ALL THIRTY prior commanders (not three or four)/Mastery/Strengths+Weaknesses/AI Behaviour/Dialogue/Codex/Museum/Accessibility, conforming to AF-071/072/098)* | Complete — `cmd031AtlasPrime.ts`: no locked module touched; uniquely among all commanders, this spec gives no overlap-reduction directive (he is designed to harmonise every system rather than specialise), so no pairwise-distinctness test was needed, though he is still proven structurally distinct from the full 52-commander roster via the real fingerprint/architectureFor laws; per the spec's "Every Commander recognises Atlas" relationships text (taken literally rather than trimmed to the usual three or four), he holds one relationship — each with a real targetId and dialogueHint — for all thirty prior commanders (CMD-001 through CMD-030), verified by a dedicated completeness test; his Codex entry's `relatedEntryIds` mirrors this exactly, the roster's largest at 30 entries; recruits via the real `legendaryMissions` source gated on "The Founder," matching his unique "Secret Post-Game"/Ω-Class rating; strong real-data continuity find — his preferred equipment includes three real `vanguard`-family items; live `roster 3/53` and `codex 16/81` on the overlay, browser-verified | Aligned | 1482 tests green (11 new) | Approved 10/10 (spec's own elevated bar) | **LOCKED** |
| AF-130 | Commander Bond Network | v1 *(a permanent, additive cross-Commander relationship layer — Bond Types/Bond Levels/How Bonds Grow/Gameplay Bonuses/Dual Ultimates/Camp Interactions/Dynamic Dialogue/Personal Quests/Group Events/Emotional System/Museum Expansion/Accessibility, integrating every Commander from AF-000 through AF-129)* | Complete — `bondNetworkData.ts` + `BondNetworkRuntime.ts`: no locked module touched, including AF-071's `CommanderProfileDef`/`CommanderRelationshipDef` — its explicit "dialogue, NOT gameplay balance" law is preserved exactly, with this module's gameplay bonuses living entirely in its own new layer; per the spec's "every pair of Commanders possesses one relationship type," `seedBondGraph` builds all 1,378 pairs across the real 53-commander roster (22 foundation + 31 individually-specified), seeding authored AF-071 relationship pairs at level 3/"Historic Connection" and defaulting the rest to a new 21st bond type, "Unacquainted," at level 0; the spec's six Dual Ultimate pairs resolve to real ids, including the non-obvious "Orion + Mira" → Dorian Fen resolution (the AF-126 owner-authorised rename of "Orion Vale"), confirmed independently by Fen's own Close-Friend relationship to Mira Syn and locked in by a dedicated test; `personalQuestSetsFor` gives all 53 commanders 3 personal + friendship + legacy + final-resolution quest ids, additive to AF-071's existing personalMissions; `growBond`/`EmotionalMemoryLog` are strictly non-decreasing/append-only per "nothing resets artificially"; `DebugSnapshot` gains a new `bonds` field via the same extension pattern used by every AF-039–070 module before it; live `bonds 1378 pairs · 131 discovered · 0 maxed · dual ultimates 0/6` on the overlay, browser-verified | Aligned | 1493 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-131 | The Living Expedition Ship | v1 *(the A.S.V. Afterlight player-home hub — 20 Ship Sections/Bridge Functions/Engineering+Laboratory casts/Commander Rooms/Player Room/Training Hall/Simulation Chamber/Museum tie-in/Memorial Garden/Companion Habitat/10 Ship Upgrades/Daily Life/Dynamic Events/Player Interaction/Audio/Seasons, integrating every Commander)* | Complete — new `src/game/livingShip/` module (`livingShipData.ts` + `LivingShipRuntime.ts`), deliberately namespaced apart from AF-031's combat Ship Framework; no locked module touched (AF-031/AF-045/AF-129 all untouched); resolves the spec's first-name-only Engineering cast (Cassia/Elias/Caelus/Nova/Xanthe) and Laboratory cast (Lyra/Seraphina/Mira/Sora) onto real, correctly-named roster ids, verified by a dedicated test; `commanderRoomsFor` gives all 53 real roster commanders a unique room, mirroring AF-130's roster-driven generator pattern; ship upgrades only ever increase (capped at level 5 per category), matching "each visual upgrade permanently changes the ship"; the ship name defaults to "A.S.V. Afterlight" and is renamable per "Customisable later"; `MemorialGardenLog` structurally enforces "Never exploit grief. Celebrate legacy" — every entry requires a non-empty legacyNote or the call throws; `CompanionHabitatRuntime` is a real deduplicating companion registry; `DebugSnapshot` gains a new `ship` field via the same AF-039–070/AF-130 extension pattern, rendered clearly distinct from the pre-existing `ships` (combat fleet) line; live `ship A.S.V. Afterlight · upgrades 0/50 (0/10 maxed) · rooms 53 · companions 0 · memorial 0` on the overlay, browser-verified | Aligned | 1503 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-132 | The Living Galaxy | v1 *(an additive galaxy-simulation layer — Galactic Simulation fields/Colony Growth/Trade Network/Dynamic News/Random Discoveries/Galactic Economy/Politics/Weather/Population Life/Wildlife/Research/Player Reputation/Historical Timeline/Festivals/Crime/Deep Space, composing with rather than redefining the real AF-041/086/089/090 stack)* | Complete — new `src/game/livingGalaxy/` module (`livingGalaxyData.ts` + `LivingGalaxyRuntime.ts`); a dedicated research pass before implementation confirmed population/economy/infrastructure/research/food-water-energy/industry/political-stability/exploration/security are already tracked across AF-086/089/090, and dynamic events + permanent history are already covered by AF-041/086/Codex, so none of that is redefined; adds exactly what was confirmed missing — pollution/wildlife/healthcare/crime/weather per real star system (`EnvironmentalRuntime`, seeded from the real `SANDBOX_GALAXY.systems`), a Dynamic News feed, a non-exhausting Random Discoveries pool, a Festival calendar, a Deep Space phenomena generator, and `PlayerReputationLedger` (a real stateful ledger where before only a static threshold lookup existed); a 500-epoch simulation test satisfies the spec's own "simulate hundreds of in-game years" directive without duplicating AF-089's already-expensive multi-epoch test; `LivingGalaxyChronicle` is a parallel, append-only log that complements rather than modifies the locked `GalacticHistoryRuntime`; `DebugSnapshot` gains a new `livingGalaxy` field via the same AF-039–070/AF-130/AF-131 extension pattern, rendered as `galaxyLife` and kept distinct from the pre-existing `galaxy`/`ship`/`ships` lines; live `galaxyLife pollution 20 · wildlife 60 · rep 0 · chronicle 0 · festival Founders Day · unresolved crime 0` on the overlay, browser-verified | Aligned | 1512 tests green (9 new) | Approved 9.5/10 | **LOCKED** |
| AF-133 | The Legacy Engine | v1 *(a permanent-record additive layer — 13 Legacy Categories/Galactic History/Player Chronicle/Galactic Records/Memory System/Commander Memories/Personal Gifts/Player Journal/Photo Mode/Time Capsules/Anniversaries/Legacy Museum wing/Multiple Save Legacies/Ending Legacy, composing with rather than duplicating MetaProgression/Museum/GalacticHistoryRuntime/AF-130's EmotionalMemoryLog/SaveProfileManager)* | Complete — new `src/game/legacy/` module (`legacyEngineData.ts` + `LegacyEngineRuntime.ts`); a dedicated research pass before implementation confirmed MetaProgression's free-form statistics map, the Museum's existing 4-kind MuseumWing concept, GalacticHistoryRuntime's externally-callable record(), EmotionalMemoryLog's free-string kind param, SaveProfileManager's lack of cross-save inheritance, and the absence of any photo-capture system — none redefined; Commander Memories reuse AF-130's EmotionalMemoryLog directly rather than duplicating a class, since its kind param was already a free string; NpcMemoryLog is a genuinely new class because it has real fading behaviour EmotionalMemoryLog lacks (minor memories fade, historic ones never do); GalacticRecordBoard.submit respects each record kind's real better-direction ("Fastest expedition" is the sole lower-is-better kind) so "every record can eventually be surpassed" holds both ways, verified by a dedicated test; GalacticHistoryLog optionally forwards into the real, locked GalacticHistoryRuntime via a constructor hook without ever duplicating it; exportLegacySnapshot/inheritedFlavourLines give "Multiple Save Legacies" real composable inheritance flavour without touching SaveProfileManager; TimeCapsuleVault structurally refuses same-epoch reopening; DebugSnapshot gains a new `legacy` field via the same established extension pattern; live `legacy xp 0 (Explorer) · records 0/9 · history 0 · favourite planet — · journal 0 · gifts 0 · photos 0` on the overlay, browser-verified | Aligned | 1525 tests green (13 new) | Approved 9.5/10 | **LOCKED** |
| AF-134 | The Living Museum | v1 *(humanity's memory — 15 Museum Sections/Earth Archive/Commander Hall/Discovery System/Curation/Visitors/Interactive Exhibits/Restoration Lab/Commander Contributions/Special Exhibitions/Theater/Library/Audio Archive/Player Exhibit/Children's Education/Galactic Impact, additive over the locked Museum/Codex system and composing with AF-130's BondNetworkRuntime and AF-133's GiftLedger)* | Complete — new `src/game/livingMuseum/` module (`livingMuseumData.ts` + `LivingMuseumRuntime.ts`); never touches the locked `MuseumWing["kind"]` 4-value union — the 15 Museum Sections are a genuinely new, parallel structure; Commander donations reuse AF-133's real GiftLedger/PersonalGiftDef directly rather than a new donation type, resolving the spec's five named examples (Atlas/Lyra/Cassia/Orion/Astrid) to real roster ids, including the same AF-126 owner-authorised "Orion"→Dorian Fen rename resolution used for AF-130's "Orion + Mira" Dual Ultimate, confirmed a second time by his companion-field-journal donation matching his beastmaster identity, verified by a dedicated test; strongestBondLevelFor computes Commander Hall room sizing entirely through BondNetworkRuntime's existing public bondFor() API, with no new method added to that locked class; RestorationLab is a genuinely new progression system (progress only grows, capped at 100); MuseumCollectionRegistry<K> is one small generic class serving Theater/Library/Audio Archive uniformly instead of three near-duplicate classes; DebugSnapshot gains a new `livingMuseum` field via the same established extension pattern, rendered as `museumLife`; live `museumLife quality 10 · restoration 0/0 · donations 5 · visitors 0 · theater 0 · library 0 · audio 0` on the overlay, browser-verified | Aligned | 1535 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-135 | The Chronicle of Humanity | v1 *(the definitive historical record — 16 Structure Sections/Dynamic Writing/Historical Authors/Multiple Perspectives/Player Biography/Commander Histories/Planetary History/Timeline/Oral History/Book Publishing/Living Map/Generational History/Anniversary Publications/Academic Debates/Player Writable Entries/The Final Chronicle, built as real composition over AF-130/133/134 rather than duplicating any of them)* | Complete — new `src/game/chronicle/` module (`chronicleData.ts` + `ChronicleRuntime.ts`); "Timeline" reuses AF-133's real OfficialHistoricalRecord/GalacticHistoryLog shape directly rather than a second parallel type; generatePlayerBiography/generateFinalChronicle are pure aggregators over AF-133's real PlayerChronicle/LegacyProgressTracker, tracking nothing new; commanderHistoryFor composes AF-130's EmotionalMemoryLog and BondNetworkRuntime (via its existing public bondFor(), no new method added) with AF-133's GiftLedger; Oral History/Book Publishing/Player Writable Entries are three more instantiations of AF-134's generic MuseumCollectionRegistry<K> with new K types instead of three more near-duplicate registries; Anniversary Publications reuses AF-133's real ANNIVERSARY_KINDS/isAnniversary outright, needing no new code; Generational History composes AF-133's real exportLegacySnapshot/inheritedFlavourLines; the one genuinely new primitive is EvolvingEntry ("entries never overwrite, they expand, older versions remain archived"), which alone satisfies both Dynamic Writing and Academic Debates, verified by a dedicated test confirming every version stays archived; PlanetaryChronicle applies it to per-planet narrative history, kept deliberately distinct from AF-132's numeric EnvironmentalRuntime; DebugSnapshot gains a new `chronicle` field via the same established extension pattern; live `chronicle planets 0 · oral history 0 · books 0 · writable 0 · commander memories 0 · final chronicle records 0` on the overlay, browser-verified | Aligned | 1543 tests green (8 new) | Approved 9.5/10 | **LOCKED** |
| AF-136 | The Dynamic Story Engine | v1 *(10 Story Pillars/World Story State/Story Director/Personal Storylines/Galactic Storylines/Emergent Moments/Commander Development/Player Reputation Stories/Story Branches/Quiet Moments/Long-Term Consequences/Narrative Callbacks/Campaign Themes/Ending Generation/Post-Campaign Documentary, real new narrative primitives composing with rather than duplicating AF-056/130/133/135)* | Complete — new `src/game/storyEngine/` module (`storyEngineData.ts` + `StoryEngineRuntime.ts`); StoryPillarTracker's 10 abstract narrative values are deliberately distinct from AF-133's 13 concrete Legacy Categories, and dominantPillars() returns multiple co-dominant pillars rather than one forced winner; deriveCampaignTheme/reputationTitleFor are pure functions of existing state (the pillar tracker; AF-133's real LegacyProgressTracker.topCategory()) — no new stat tracking; StoryDirector evaluates broad combat/exploration/downtime pacing from its own new counters, deliberately distinct from AF-056's DirectorConductor (combat-encounter recovery windows, zero shared state), and its pacingBias() is a computed value handed back rather than wired into any locked event roller; CommanderStorylineLog reuses AF-135's real EvolvingEntry directly for the five Personal Storyline beats rather than a new versioning primitive; generateEndingSummary composes AF-135's real generateFinalChronicle rather than a second aggregator; NarrativeCallbackLog.eligibleCallbacks only surfaces entries once enough epochs have genuinely passed; DebugSnapshot gains a new `storyEngine` field via the same established extension pattern; live `story pillars [none yet] · theme Hope after disaster · rep The Explorer · pacing combat 0%/explore 0%/downtime 0% · callbacks 0 · branch lean 0 · commander story beats 0` on the overlay, browser-verified | Aligned | 1552 tests green (9 new) | Approved 9.5/10 | **LOCKED** |
| AF-137 | The Galactic Event Engine | v1 *(a new SCALE-tier classification — 5 Event Tiers/Event Generation inputs/Event Chains/Community+Commander+World+Discovery+Player+Emergency+Historical+Ship+Exploration+Economic Events, layered alongside rather than redefining AF-041's TYPE-based WorldEventRuntime/EventCategory, composing with AF-130/132/133/136 per the spec's own instruction)* | Complete — new `src/game/eventEngine/` module (`eventEngineData.ts` + `EventEngineRuntime.ts`); EVENT_TIERS (Local/Regional/Sector/Galactic/Legendary) is a different axis from AF-041's 10 TYPE-based categories — "Sector"/"Legendary" appear as coincidental vocabulary on both, but the two unions never share code or state, and AF-041 is never modified; tierWeightsFor takes plain numeric summaries rather than importing AF-130/132/133/136 directly, so a caller composes real values from EnvironmentalRuntime/PlayerReputationLedger/BondNetworkRuntime/StoryPillarTracker while the Event Engine itself stays fully decoupled and testable in isolation; higher composed activity biases weight toward larger-scale tiers, verified by a dedicated test, while Legendary's weight never moves regardless of input, matching "never guaranteed" structurally; rollTier performs real weighted-random selection, wired live via a seeded Rng fork logging one real opening event at startup; EventChainRuntime tracks real progress through the spec's own eight-step mining-boom example; DebugSnapshot gains a new `eventEngine` field via the same established extension pattern; live `events weights [Local 10.0, Regional 5.1, Sector 2.1, Galactic 1.0, Legendary 0.1] · logged 1 · mining-boom chain 0/8 (Mining boom)` on the overlay, browser-verified | Aligned | 1558 tests green (6 new) | Approved 9.5/10 | **LOCKED** |
| AF-138 | The Civilisation Engine | v1 *(humanity rebuilding itself — 6 Civilisation Stages/18 Civilisation Attributes/Population Simulation/Education/Culture/Infrastructure/Government/Public Opinion/Megaprojects/Landmarks/Immigration/Economic Specialisation/Social Events/Generational Change/Galactic Identity, additive over the locked AF-090 CivilisationFrameworkRuntime)* | Complete — new `src/game/civilisationEngine/` module (`civilisationEngineData.ts` + `CivilisationEngineRuntime.ts`); a dedicated research pass before implementation confirmed AF-090's `SettlementDevelopmentStage`/`CivilisationSpecialisation` are closed, locked unions with different names/counts/theming than this spec's 6 stages/9 specialisations, so this module defines its own separate classifications; also confirmed `PopulationStat` declares `population`/`employment` but AF-090 never mutates either per settlement (only 7 of 10 declared stats are live) — a first-draft mapping onto those two dead fields threw `undefined.toFixed()` at runtime during browser verification, caught and fixed by sourcing Population from AF-086's real per-faction `CivilisationSimulationRuntime.stateFor(factionId)?.attributes.population` and Employment from a live-workforce-average proxy instead; the other 5 overlapping attributes (Education/Health/Security/Public Happiness/Industry) do read AF-090's real, live `populationStats` fields; `CIVILISATION_MEGAPROJECTS` use distinct `megaproject-*` ids, never colliding with AF-090's real `MEGASTRUCTURES` despite two sharing generic display-name wording ("Orbital Ring," "Planetary Shield") — documented as a low-stakes vocabulary overlap per the AF-137 precedent, not requiring a Project Owner call; `MegaprojectTracker` mirrors AF-090's own `investInMegastructure` pattern; `LandmarkRegistry` needed no new class — the 6th direct reuse of AF-134's generic `MuseumCollectionRegistry<K>`; `GovernmentPriorityTracker` only ever nudges a lean, matching "player influences direction, never absolute control"; `PublicOpinionTracker` kept separate from AF-136's closed-union `StoryBranchTracker`; DebugSnapshot gains a new `civilisationEngine` field via the same established extension pattern; live `civEngine Lucent Gate Resonance Mines stage Colony · pop 60 · culture 20 · megaprojects 0/8 · landmarks 0 · immigration 0 · social Graduations · gov lean none · opinion 0.0 · careers 0` on the overlay, browser-verified | Aligned | 1570 tests green (12 new) | Approved 9.5/10 | **LOCKED** |
| AF-139 | The Evolution Engine | v1 *(11 Evolution Pillars — Biological/Technological/Architectural/Cultural/Scientific/Economic/Political/Educational/Environmental/Exploratory/Historical, plus Equipment/Commander/Companion/Colony/Transport/Language/Research/AI/Planetary/Player Evolution and the Great Projects, composing with an unusually wide slice of the locked stack rather than duplicating any of it)* | Complete — new `src/game/evolutionEngine/` module (`evolutionEngineData.ts` + `EvolutionEngineRuntime.ts`); a dedicated research pass before implementation surveyed equipment/commander/companion/settlement/wildlife/research/meta-progression/megaproject systems and confirmed most of this spec's pillars have no real backing anywhere — no craftsmanship axis distinct from AF-007's `RARITY_LADDER`, no visual architecture ladder, no species-level adaptation (AF-132's `EnvironmentalRuntime` only tracks one aggregate wildlife index per system), no companion growth/traits (AF-131's `CompanionHabitatRuntime` is a pure dedup registry), no sequential Rookie→Legend rank ladder, no technology-era aggregate, no transport-tier ladder; the research also found this spec's 7 Great Projects examples collide directly with AF-090's real `MEGASTRUCTURES` and AF-138's real `CIVILISATION_MEGAPROJECTS` ("Dyson Swarm" ≈ `dyson-array-lucent`, "Galactic University" ≈ `megaproject-interstellar-university`, "Deep Space Observatory Ring" ≈ `megaproject-deep-space-telescope`, "Atlas Gateway Network" ≈ `quantum-gate-axiom`) — rather than build a third near-duplicate roster, `GREAT_PROJECT_LINEAGE` documents which real project each example maps onto and `greatProjectsProgressSummary` reports one unified progress lens across both real rosters (17 projects total), with only "Interstellar Seed Vault" left deferred since it had no real match; `architecturalStageFor` purely maps AF-090's real 7-stage `SettlementDevelopmentStage` onto a new 6-stage visual ladder; `playerEvolutionRankFor` derives purely from AF-026's real `MetaProgression.snapshot.accountLevel`; `commanderMaturityScore`/`technologyEraFor` compose AF-071/AF-130's and AF-024's real state via plain numbers, per the decoupled-composition pattern AF-137's `tierWeightsFor` established; `transportTierFor` gates its top tiers behind AF-024's real `warp-charting` research unlock; `EquipmentEvolutionTracker`, `HistoricalArchitectureLedger`, `SpeciesAdaptationRegistry`, and `CompanionEvolutionTracker` (keyed by AF-131's real `CompanionHabitatEntry.id`) are genuinely new stateful primitives; `LanguageEvolutionLog` reuses AF-135's real `EvolvingEntry` directly; DebugSnapshot gains a new `evolutionEngine` field via the same established extension pattern; live `evolution Salvage Age · transport Walking Paths · architecture Emergency Shelters (1 layers) · commander Rookie Officer · rank Rookie · equipment Prototype · species records 0 · companion none yet · phrases 1 · great projects 0/17 (avg 0%)` on the overlay, browser-verified | Aligned | 1583 tests green (13 new) | Approved 9.5/10 | **LOCKED** |
| AF-140 | The Infinite Endgame Engine | v1 *(8 Endgame Pillars — Infinite Exploration/Living Civilisation/Scientific Advancement/Commander Legacy/Galaxy Restoration/Museum Expansion/Legendary Engineering/Historic Discovery, plus Great Expeditions/Legendary Projects/Commander Legacies/Expedition Council/Mega Discoveries/Annual Events, gated behind AF-069's real EndgameRuntime unlock)* | Complete — new `src/game/endgameEngine/` module (`endgameEngineData.ts` + `EndgameEngineRuntime.ts`); a dedicated research pass before implementation found this spec collides more heavily than any prior module — 7 of 8 "Legendary Projects" examples are exact/near duplicates of AF-090's real `MEGASTRUCTURES`, AF-138's real `CIVILISATION_MEGAPROJECTS`, or AF-139's real `GREAT_PROJECT_LINEAGE` ("Dyson Swarm," "Galactic Library," "Atlas Gateway Network," "Deep Space Observatory Ring" exact-string dupes) — so this module reuses AF-139's real `greatProjectsProgressSummary` directly rather than a fourth roster, leaving only "Museum of Civilisations" deferred; "Colony Specialisation" overlaps 7-of-9 with AF-138's real `CIVILISATION_ENGINE_SPECIALISATIONS`, so `megacityThresholdMet` composes AF-090's real per-settlement `specialisation` field directly instead of a new union; "Annual Events" has two exact-string dupes of AF-132's real `FESTIVALS` ("Founders Day," "Museum Anniversary"), kept as its own small `AnnualEndgameCalendar` per the AF-138 `SocialEventCalendar` precedent; "Infinite Research" turned out already real — AF-082's `infiniteResearchProjectFor`/`INFINITE_RESEARCH_KINDS` already implement "no stat inflation, knowledge expands instead," composed directly with zero new tracked state; the endgame gate composes AF-069's real, already-locked `EndgameRuntime.snapshot.unlocked` directly; `ExpeditionCouncilTracker` mirrors AF-138's real `GovernmentPriorityTracker` lean-only mechanic on its own priority vocabulary; `FrontierExpeditionRegistry`/`GREAT_EXPEDITION_DESTINATIONS` form a deliberately separate, parallel late-game roster, never wired into AF-038's real, closed `SANDBOX_GALAXY` (the same separation AF-131's Living Ship used against AF-031); `CommanderLegacyRuntime`'s successors are append-only, narrative-only lore records, never extending AF-030's real closed roster arrays; `MegaDiscoveryLog`/`EmergentIndustryLedger` are genuinely new append-only logs with no collisions found; `GalacticMuseumExpansionTracker` adds only two new counters, additive over AF-134's real `MuseumQualityTracker`; AF-135's Chronicle needed no new code at all since it's already unbounded; DebugSnapshot gains a new `endgameEngine` field, rendered as `infEndgame` to stay distinct from AF-069's existing `endgame` line; live `infEndgame locked — the endgame begins after the main campaign` (the same real gate AF-069 already uses) on a fresh run, browser-verified | Aligned | 1596 tests green (13 new) | Approved 9.5/10 | **LOCKED** |
| AF-141 | The Galactic Creator Engine | v1 *(15 Creation Categories — Architecture/Ships/Commander Rooms/Museum Exhibits/Gardens/Parks/Research Facilities/Planet Decorations/Photography/Stories/Music Playlists/Expedition Flags/Memorials/Statues/Educational Displays, plus Settlement Design/Player Gallery/Custom Museum Exhibitions/Memorial Design/Photo Stories/Library Contributions/Botanical+Observatory Design/Soundtrack Collection/Player Heritage/Commander Participation/Community Projects, composing with AF-131/133/134/135/138/140's real creation-adjacent systems)* | Complete — new `src/game/galacticCreator/` module (`galacticCreatorData.ts` + `GalacticCreatorRuntime.ts`); a dedicated research pass before implementation found AF-131's real `MemorialGardenLog` remains the one real memorial store (never duplicated — `MEMORIAL_SUBJECT_KINDS` only tags it), AF-133's real `PhotoAlbum` is a flat map with no album-grouping concept (so `PhotoAlbumCurator` composes it by reference, adding only the genuinely new album/caption layer), AF-134's real `MuseumCollectionRegistry`/AF-140's `temporaryExhibitionThemeFor` never carried curatorial theme/lighting/narration/music/notes (so `ExhibitionCuratorRuntime` fills exactly that gap), and AF-138's real `CIVILISATION_LANDMARK_KINDS` only tallies "Gardens"/"Public observatories" as flavour strings with zero element inventory (so `CreationElementStudio<TElement>` — one generic reused for both Garden and Observatory design — is the genuinely new territory); AF-135's `PLAYER_WRITABLE_ENTRY_KINDS`/AF-133's `PlayerJournalRuntime` kinds were checked and confirmed not to carry this spec's "Travel journals/Planet guides/Commander tributes" vocabulary, so `LIBRARY_CONTRIBUTION_KINDS` is its own distinct list; Expedition Flags (`ExpeditionFlagRegistry`, `flag-*` id namespace, distinct from `FactionDef.symbol`'s real `icon-*-sigil` ids) and Soundtrack Playlists (`SoundtrackPlaylistRegistry`) are both confirmed genuinely new/cosmetic; commander participation resolves Cassia/Lyra/Orion/Atlas to real roster ids `thorne-starforged`/`voss-pathfinder`/`fen-beastmaster`/`prime-founder` (the AF-126 Orion→Dorian Fen rename confirmed a third time) gated by `contributionEligible` composing AF-130's real bond level; `CommunityProjectTracker` mirrors AF-138's real `MegaprojectTracker` accumulate-progress pattern over its own distinct `community-project-*` ids, with two name-adjacent (not identical) examples documented per the AF-137/138/139/140 precedent rather than merged; `CreationHeritageLedger` is structurally parallel to AF-139's `HistoricalArchitectureLedger` but kept separate since it's keyed per creation rather than per settlement; DebugSnapshot gains a new `galacticCreator` field, rendered as `creator`; live `creator albums 0 · exhibitions 0 · flags 0 · garden elements 2 · observatory elements 0 · playlists 1 · commander ideas 0 · community 0/6 · heritage New Creation` on the overlay, browser-verified | Aligned | 1609 tests green (13 new) | Approved 9.5/10 | **LOCKED** |
| AF-142 | The Modular Universe Engine | v1 *(20 Module Categories, Module Registration/System Compatibility/QA Framework/Save Compatibility/Live Event Support/Mod Support Architecture/Performance/Developer Toolkit, an architectural module composing with AF-024/070/093/094/095 rather than duplicating any of them)* | Complete — new `src/game/moduleUniverse/` module (`moduleUniverseData.ts` + `ModuleUniverseRuntime.ts`); a dedicated research pass before implementation found this collides with locked *architectural* modules more than content rosters — AF-070's real `LiveOpsRegistry` already fully implements "Live Event Support" (Seasons + structural FOMO-rejection) and "Save Compatibility" (`compatibilityFor`, a monotone version number), both composed with zero new code; AF-070's real `ContentPackDef` genuinely lacks a dependency graph/tags/cross-system flags (it registers content packs, not whole modules), so `ModuleRegistry` fills exactly that gap at a different granularity; `moduleQaReport` genuinely fulfils the "Dependencies"/"Museum integration" gates AF-095's `qualityAssuranceData.ts` explicitly flags as still-future; `ModuleRegistry.loadModule`/`unloadModule` model dynamic loading at the data/state level only, consistent with AF-094's honest `asynchronousLoading: false`; `topologicalOrderOver` mirrors AF-024's real `ResearchTree.validate()` grey/black-DFS cycle-detection spirit rather than inventing a second algorithm; `ContentDiscoveryFeed` is kept separate from AF-132's real `NEWS_CATEGORIES`/`DISCOVERY_KINDS`; Faction/Commander/World expansion checklists and the Developer Toolkit are kept as pure reference data with no runtime validator, since a registered "module" describes a whole system rather than a specific faction/commander/planet instance; a real performance bug was caught during the module's own "stress-test thousands of new modules" self-review — the first draft's O(n²)-per-registration cycle check made a 2000-module stress run take ~116s, fixed by adjacency-list-based topological sort (O(n+e) per call) down to ~1.5s; DebugSnapshot gains a new `moduleUniverse` field, rendered as `universe`; live `universe modules 1/20 categories · compat [AF-130, AF-132, AF-133, AF-134, AF-135, AF-139] · qa passed · loaded 1 · order resolved · discoveries 1/6` on the overlay, browser-verified | Aligned | 1620 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-143 | The Atlas Development Framework | v1 *(10 Foundation Standards, Design Bible/Content+Commander+World Validation/Art+Audio+Narrative Pipelines/Performance Targets/Accessibility Gate/Documentation/Automated QA/Design Score (9.5/10 gate)/Developer Toolset/Post-launch Support/Knowledge Base, composing with AF-070/093/094/095/097 rather than duplicating any of them)* | Complete — new `src/game/atlasFramework/` module (`atlasFrameworkData.ts` + `AtlasFrameworkRuntime.ts`); a dedicated research pass before implementation found this module's "Design Score >9.5/10" gate is, almost verbatim, this project's own real standing process — `docs/FOUNDATION_LOCK.md:46`'s real ten-category gate (Gameplay/Visual/Technical Quality · Performance · Accessibility · Replayability · Scalability · Documentation · Lore Consistency · Integration, ≥9.5/10), already half-acknowledged inside AF-095's `developerReview` field — so this module builds the spec's own separate nine-category `DESIGN_SCORE_CATEGORIES` rubric (`DesignScoreCard`) as new, distinct tooling rather than mechanically re-deriving the real gate; "Design Bible" needed zero new code since AF-097's real `CONTENT_TEMPLATE_FIELDS`/`contentTemplateCompletenessFor` already cover it; `commanderCompletenessFor`/`worldCompletenessFor` are confirmed genuinely new (no completeness checker of this shape exists anywhere for individual Commanders/Worlds), built as decoupled pure functions over plain signal values per the AF-137 `tierWeightsFor` discipline; `PostLaunchSupportTracker`/`KnowledgeBaseRegistry` are confirmed genuinely new since neither AF-070's `LiveOpsRegistry` nor AF-142's `ModuleRegistry` tracks any metric over time; the Accessibility Gate (8 items) is kept decoupled from AF-095's differently-named `ACCESSIBILITY_VALIDATION_CHECKS`/AF-093's `accessibilityTags`; the Developer Toolset is kept as pure reference data given heavy name overlap with AF-142's `DEVELOPER_TOOLKIT_SURFACES` (Relationship Simulator exact duplicate) and AF-094's `DEVELOPER_TOOLS_KINDS`; Content Validation/Art/Audio/Narrative Pipelines/Performance Targets/Documentation/Automated QA are all kept as pure reference checklists, since AF-094/095/097 already conceptually own those human-authorship process steps; a real bug was caught during browser verification — the debug closure's first draft wrote to `PostLaunchSupportTracker` every render frame instead of once, growing it unbounded (145 vs 1 entry) — fixed by moving the seed call to the one-time instantiation block; DebugSnapshot gains a new `atlasFramework` field, rendered as `atlas`; live `atlas design gate passed · commander checklist 5/9 · world checklist 9/10 · post-launch 1 · knowledge base 1` on the overlay, browser-verified | Aligned | 1629 tests green (9 new) | Approved 9.5/10 | **LOCKED** |
| AF-144 | The Afterlight Operating System | v1 *(17 AOS Responsibilities, System Bus/World State/Simulation Clock/Priority Engine/Context Engine/Memory Manager/Decision Router/Prediction Engine/Performance Orchestrator/Recovery System/Debug Framework/Live Telemetry, composing with AF-001/044/056/094/133 rather than duplicating any of them)* | Complete — new `src/game/aos/` module (`aosData.ts` + `AosRuntime.ts`); a dedicated research pass before implementation — the most architecturally sweeping spec yet — confirmed the real System Bus already exists as AF-001's generic `EventBus<E>` (instantiated here with a new `AosEventMap` rather than a second bus class) and the real Memory Manager already exists as AF-133's `NpcMemoryLog` (minor-fades/historic-permanent, zero new code needed); confirmed genuinely new since every existing clock/state/priority concept in the codebase is local to one module with no unifying analog anywhere: `WorldStateStore<T>` (real single source of truth over Current/Historical/Projected/Temporary/Emergency state), `SimulationClockRegistry` (read-only reporting aggregator, never a second source of truth for any real clock), `PriorityEngine` (real High/Medium/Low frame-divisor throttling, distinct from AF-056's combat-only `DirectorConductor`), `DecisionRouter` (player-sourced requests always win ties), `PredictionEngine` (real linear-trend forecaster), `PerformanceBudgetTracker`, `RecoveryLog` (world-state-contradiction recovery, distinct from AF-044's save-slice corruption recovery), and `TelemetryCollector` (confirmed genuinely new — AF-094's `ANALYTICS_TRACKING_CATEGORIES` has zero producer anywhere; counts event kinds only, no payload data); "Debug Framework" kept as pure reference data since the real `DebugOverlay` already covers simulation-viewer/performance-profiler and the other 5 tools need interactive UI with no runtime surface here; a real bug (`WorldStateStore.setCurrent` archiving the outgoing value with the wrong epoch, breaking historical lookups) was caught while writing tests and fixed; DebugSnapshot gains a new `aos` field; live `aos responsibilities 17 · bus events 1 · world state stable (settlements 6) · clocks [civ 0] · priority player=High · decision→living-galaxy · forecast 52 (100%) · throttle [none] · recoveries 1 · dialogue friendly=true` on the overlay, browser-verified | Aligned | 1642 tests green (13 new) | Approved 9.5/10 | **LOCKED** |
| AF-145 | The Atlas Core | v1 *(Core Philosophy/Twelve Atlas Principles/Design Validation/Emotional Compass/Player Experience Pillars/System Hierarchy/Quality Bar/Accessibility+Long-Term Philosophy/The Final Question — sent as a follow-up filling the numbering gap AF-146 had already documented; a third, separate in-universe charter, never modifying docs/CONSTITUTION.md or AF-146's real data)* | Complete — new `src/game/atlasCore/` module (`atlasCoreData.ts` + `AtlasCoreRuntime.ts`); like AF-146, this module's own text describes itself in nearly the same terms the project's real supreme governing document already uses ("Nothing supersedes the Core" vs. the real Constitution's "None may contradict it") — per the same standing rule already applied to AF-146, implemented as a third, separate, new charter rather than modifying the real Constitution or AF-146's locked classes; "Design Validation" (8 questions, all-must-pass) is documented against the real Constitution's two all-must-pass gates and AF-146's `EXPANSION_TEST_REQUIREMENTS` rather than merged; "The Twelve Atlas Principles" (paired "X over Y" contrasts) overlaps in spirit with AF-146's `TEN_PILLARS` but is structurally distinct (12 paired entries vs. 10 single values), kept as its own `AtlasPrincipleDef` type; "System Hierarchy" (10 items) is the third parallel "which systems does this govern" list after AF-142's real `SYSTEM_COMPATIBILITY_TARGETS` (12) and AF-144's real `AOS_RESPONSIBILITIES` (17); "Quality Bar" (8 adjectives, no numeric score) overlaps in spirit with AF-143's real `DESIGN_SCORE_CATEGORIES` and the real Constitution's/`FOUNDATION_LOCK.md`'s 9.5/10 gate, kept separate with no gate of its own; an "Atlas" naming note documents the shared motif with AF-143's Atlas Development Framework, the real founder commander Atlas Prime, and AF-139/140's Atlas Gateway Network megaproject, none an identity collision; `designValidationPassed` is a real all-must-pass function; `AtlasCoreComplianceRegistry`/`AtlasPrincipleReinforcementLedger` mirror AF-146's real ledger shape (typed to this module's own `AtlasPrincipleId` rather than reusing AF-146's `Pillar`-typed classes); DebugSnapshot gains a new `atlasCore` field; live `atlasCore principles 12 · hierarchy 10 systems · features 1 (1 validated) · latest reinforced 2 · dominant principle hope-over-despair` on the overlay, browser-verified | Aligned | 1655 tests green (6 new) | Approved 9.5/10 | **LOCKED** |
| AF-146 | The Afterlight Design Constitution | v1 *(Purpose/Mission Statement/Ten Pillars/Prohibited+Required Design Patterns/Content Test/Expansion Test/Technical+Artistic+Audio+Narrative Principles/Player+Developer Promise/The Final Promise — a new in-universe design charter, explicitly never modifying or superseding the project's real docs/CONSTITUTION.md)* | Complete — new `src/game/designConstitution/` module (`designConstitutionData.ts` + `DesignConstitutionRuntime.ts`); this module's own text describes itself in nearly the same terms the project's real supreme governing document already uses (`docs/CONSTITUTION.md`, "None may contradict it," the same "rebuilding civilisation... hope" mission) — per the standing rule that nothing may contradict the real Constitution absent explicit Project Owner authorisation, this module is implemented as its own new, clearly-separate charter, the same honest-precedent treatment AF-143 gave its own real-9.5-process discovery; the real Constitution's two existing all-must-pass gates ("THE AFTERLIGHT TEST," the "DESIGN DECISION MATRIX") are documented rather than merged with this module's own 8-of-10-partial-pass Content Test and all-required Expansion Test; AF-136's real `STORY_PILLARS` shares "Hope"/"Discovery" verbatim with this module's `TEN_PILLARS` but is kept separate as a different axis (per-playthrough narrative themes vs. meta-design pillars), per the AF-137/138/139/140 vocabulary-overlap precedent; `contentTestScore`/`expansionTestPassed` are real threshold functions; `FeatureComplianceRegistry`/`PillarReinforcementLedger` give the Content Test and Ten Pillars real, inspectable evidence rather than design-promise text with nothing behind it; DebugSnapshot gains a new `designConstitution` field, rendered as `constitn`; live `constitn features 1 (1 passed content test) · latest expansion-ocean-worlds 10/10 · expansion passed · dominant pillar Wonder · reinforcements 2` on the overlay, browser-verified | Aligned | 1649 tests green (7 new) | Approved 9.5/10 | **LOCKED** |
| AF-147 | The Afterlight Franchise Bible | v1 *(Purpose/Franchise Mission/Universe Foundations (Core Themes)/Timeline Authority (Eras)/Canon Levels/Visual+Music+Language Identity/Commander+World Standards/Technology Rules/Expansion Rules/Merchandise+Adaptation Guidelines/Community Values/Future Vision/The Franchise Test — explicitly "not a gameplay system," the fourth franchise-governance module after the real docs/CONSTITUTION.md and AF-145/146's charters)* | Complete — new `src/game/franchiseBible/` module (`franchiseBibleData.ts` + `FranchiseBibleRuntime.ts`); confirmed genuinely new: `eraFor` maps epochs onto a real, named 8-Era chronology absent anywhere else (AF-068's `CampaignRuntime` chapters are a gameplay-campaign structure, not a franchise timeline); `CanonAuthorityResolver` is a real conflict resolver over the spec's own 6-tier canon ladder, mirroring AF-144's real `DecisionRouter` tier-comparison shape but arbitrating lore statements, a distinct domain; "Core Themes" (10) is the densest vocabulary overlap yet with the real Constitution's Design Pillars, AF-146's `TEN_PILLARS`, and AF-145's Atlas Principle virtues, kept as its own fourth list with a genuinely new minimum-count rule ("at least three themes") distinct from the other three lists' exact-N/all-N gates; "The Franchise Test" (6, all-must-pass) is the fifth occurrence of the same checklist-gate mechanic after the real Constitution's two gates, AF-146's Expansion Test, and AF-145's Design Validation, kept as its own sixth question list; Visual/Music/Language Identity, Commander/World Standards, Technology Rules, Merchandise/Adaptation Guidelines, Community Values, and Future Vision are all kept as pure reference data with no runtime validator, since this module explicitly disclaims being a gameplay system; `CanonRecordLedger`/`FranchiseComplianceRegistry` give real, inspectable evidence rather than design-promise text; DebugSnapshot gains a new `franchiseBible` field; live `franchise era Earth Era · canon tiers 6 (statements 2, authoritative "afterlight-1") · projects 1 (1 compliant)` on the overlay, browser-verified | Aligned | 1663 tests green (8 new) | Approved 9.5/10 | **LOCKED** |
| AF-148 | The Atlas Canon Engine | v1 *(Canon Pyramid/Historical Consistency/Knowledge States/Discovery Model/Lore Validation/Commander+Planet Continuity/Artifact Authenticity/Academic Evolution/Multiple Perspectives/Timeline Protection/Canon Tools, a genuine consistency engine composing directly with AF-133/134/135's real classes rather than restating design philosophy)* | Complete — new `src/game/canonEngine/` module (`canonEngineData.ts` + `CanonEngineRuntime.ts`); a dedicated research pass before implementation found this composes far more directly with real storage than AF-145/146/147 did: "Discovery Model" is fully implemented by AF-135's real `EvolvingEntry` (confirmed no mutate/delete method exists), reused directly; "Multiple Perspectives" reuses AF-135's real `AuthorVoice` union directly rather than a third overlapping perspective union; "Planet Continuity" composes AF-135's real `PlanetaryChronicle` via its existing `write()`; `CanonEventLedger`'s event shape (witnesses/evidence/museum+chronicle refs) is materially richer than AF-133's real `OfficialHistoricalRecord` and optionally forwards a summary into that real log via the same `forwardTo` pattern AF-133 established for AF-086; `KnowledgeStateTracker` implements the three-form event model with Objective Reality genuinely enforced-immutable (throws on contradiction, matching Canon Pyramid Level One as a real invariant); `CommanderContinuityLedger` is materially richer than AF-135's real thin-numeric `commanderHistoryFor`; `ArtifactAuthenticityRegistry` (provenance/confidence) is confirmed genuinely absent from AF-134's `GiftLedger`/`RestorationLab`; `expansionRespectsTimeline` is a real semantic non-invalidation gate, confirmed genuinely new since AF-070/142's registries only guard id collisions; `canonPyramidRank` mirrors AF-147's real `canonTierRank` pattern but governs a different axis (in-fiction narrative authority vs. real-world source authority), never importing AF-147's `CanonTier`; `loreValidationReport` composes plain boolean signals per the AF-137 decoupling discipline, since only 2-3 of AF-095's real 7 `LORE_VALIDATION_CHECKS` have any existing backing; "Canon Tools" kept as pure reference data since `hasDiverged`/`expansionRespectsTimeline` collectively are the real Canon Conflict Detector/Timeline Validator; DebugSnapshot gains a new `canonEngine` field; live `canonEng events 1 · knowledge diverged=true · lore 9/9 (passed) · continuity facts 1 · artifacts 1` on the overlay, browser-verified | Aligned | 1672 tests green (9 new) | Approved 9.5/10 | **LOCKED** |
| AF-149 | The Atlas Protocol | v1 *(Seven Stages/System Impact Map/Atlas Questions/Red+Green Flags/Iteration Loop/Documentation/Post-Launch Review/Atlas Score (9.5/10 gate)/Developer Oath/Final Validation — execution workflow explicitly distinguished from AF-145's philosophy, composing with AF-094/095/097/143's real pipelines and scoring shapes rather than duplicating any of them)* | Complete — new `src/game/atlasProtocol/` module (`atlasProtocolData.ts` + `AtlasProtocolRuntime.ts`); confirmed "The Seven Stages" is the fifth distinct pipeline-stage list after the real Constitution's 11-stage Development Pipeline, AF-097's real `CONTENT_PIPELINE_STAGES`, AF-095's own differently-scoped same-named `CONTENT_PIPELINE_STAGES`, and AF-095's `RELEASE_PIPELINE_STAGES`, so `ATLAS_PROTOCOL_STAGES`/`nextAtlasProtocolStage` follow the same real `next*Stage` pattern rather than a new shape; "The Atlas Score" mirrors AF-143's real `DesignScoreCard` shape exactly but typed to its own separate `AtlasScoreCategory` union, since AF-143's class is hand-typed to a closed union rather than a reusable generic; "System Impact Map" is the fifth parallel "which systems does this touch" list after AF-142/144/145's real lists; "Final Validation" is the seventh all-must-pass checklist gate in this codebase; `FeatureLifecycleTracker` moves a feature through the Seven Stages one at a time, never skipping; `IterationCycleTracker` makes "never ship the first version" a real structural rule (ready only after ≥2 genuine cycles); `systemImpactReportFor`/`featureFlagAssessment`/`finalValidationPassed` are decoupled composers over plain signals; DebugSnapshot gains a new `atlasProtocol` field; live `protocol stage Integration · impact [Gameplay, Civilisation, History] · flags reject=false green=3 · atlas score 9.7 (passed) · cycles 2 ready=true · final validation passed` on the overlay, browser-verified | Aligned | 1681 tests green (9 new) | Approved 9.5/10 | **LOCKED** |
| AF-150 | The Afterlight Universe Master Index | v1 *(34 Master Catalogue categories, Unique Identifiers/Relationship Graph/Dependency Map/Search Engine/Version History/Quality Tracking/Documentation Links/Expansion Support/Developer Dashboard/Player-Facing Derivatives/Failsafe Rules — a meta-registry over individual game objects, a different granularity than AF-142's real ModuleRegistry, reusing its real dependency-graph algorithm)* | Complete — new `src/game/masterIndex/` module (`masterIndexData.ts` + `MasterIndexRuntime.ts`); confirmed genuinely distinct from AF-142's real `ModuleRegistry` (whole modules) and AF-148's real `CanonEventLedger` (one richer event-record type) by operating at individual-OBJECT granularity across all 34 catalogue categories; `MasterIndexRegistry`'s dependency-graph algorithm deliberately mirrors AF-142's real adjacency-list fix (the same O(n+e) approach that corrected an O(n²)-per-registration bug discovered during that module's own self-review) rather than regressing to the slower approach, confirmed by a retained 2000-entry stress test completing in under a second; "Expansion Support" restates AF-142's own "no manual integration required" philosophy at this finer granularity; "Failsafe Rules" is the eighth all-must-pass checklist gate in this codebase; "Quality Tracking" is a different shape than AF-143/149's real per-feature `DesignScoreCard`/`AtlasScoreCard` (per-category scores keyed by object id), so `QualityTracker` is its own new class; "Player-Facing Derivatives" lists systems that are all already real and locked, kept as pure reference confirmation; `RelationshipGraph`/`DependencyMap`/`VersionHistoryLedger` are all real, append-only; DebugSnapshot gains a new `masterIndex` field; live `masterIdx entries 1/34 categories · order resolved · relationships 1 · dependency links 1 · version history 1 · quality 9.3` on the overlay, browser-verified | Aligned | 1691 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-151 | The Atlas Knowledge Graph | v1 *(Graph Nodes/19 Graph Edge kinds/Semantic Search/Intelligent Discovery/Narrative+Authoring Support/Commander+Planet+Historical+Evolution Graph views/Prediction Support/Automated Validation, a semantic edge layer sitting above AF-150's real MasterIndexRegistry/RelationshipGraph, sharing node ids rather than a second id space)* | Complete — new `src/game/knowledgeGraph/` module (`knowledgeGraphData.ts` + `KnowledgeGraphRuntime.ts`); confirmed AF-150's real `RelationshipGraph` is a simple untyped edge list with no strength/confidence/historical-context/date fields, so this module's 19-kind `GraphEdgeKind`/richer `GraphEdge` shape is a genuinely new semantic layer coexisting over the same node ids, never a replacement; "Isolated nodes" is the one item genuinely computable purely from graph structure across "Intelligent Discovery"/"Automated Validation" (both otherwise overlapping with AF-150's real `DEVELOPER_DASHBOARD_CHECKS`/AF-148's canon checks), implemented by `KnowledgeGraph.isIsolated`; "Prediction Support" overlaps in wording with AF-144's real `PREDICTION_KINDS`/`PredictionEngine` (a numeric trend forecaster) but `suggestConnections`' shared-neighbour graph traversal is a structurally different, confirmed-distinct mechanic; "Commander/Planet/Historical/Evolution Graph" are all filtered views over one real edge store via `subgraphFor`, not four near-duplicate classes; `chronologyViolations` composes a plain lookup function per the AF-137 decoupling discipline rather than importing AF-150's `MasterIndexRegistry` directly; DebugSnapshot gains a new `knowledgeGraph` field; live `knowGraph edges 2 · isolated=false · neighbours 1 · suggestions [commander-thorne-starforged] · chronology violations 0` on the overlay (the suggestion engine correctly surfaced a real 2-hop connection), browser-verified | Aligned | 1697 tests green (6 new) | Approved 9.5/10 | **LOCKED** |
| AF-152 | The Atlas World Model | v1 *(World Objects/World Context/Spatial+Temporal+Social+Ecological+Economic Awareness/Mission Understanding/Memory Model/Goal System/World Queries/Simulation Support/Predictive Reasoning/Visual Debugging/Performance — the third Atlas layer, "stores understanding" above AF-151's relationships, composing with AF-133/144's real classes rather than duplicating any of them)* | Complete — new `src/game/worldModel/` module (`worldModelData.ts` + `WorldModelRuntime.ts`); confirmed "Memory Model" is already fully implemented by AF-133's real `NpcMemoryLog` (reused directly, the same reuse AF-144's Memory Manager already made) and "Performance" is exactly AF-144's real `PriorityEngine` (reused directly); "Predictive Reasoning" overlaps heavily with AF-144's real `PredictionEngine`/`PREDICTION_KINDS` (Population growth/Wildlife migration/Festival scheduling near-exact matches) and — unlike AF-151's structurally-different `suggestConnections` — asks for the SAME numeric trend-forecasting mechanic, so it's reused directly rather than duplicated; `WorldModelRegistry`'s own `WorldContext` deliberately excludes History/Relationships/Goals since those already have real homes (AF-135/148's chronicle/canon, AF-151's Knowledge Graph, this module's own `GoalTracker`); `SpatialAwarenessTracker` is deliberately decoupled from AF-038's real galaxy coordinate system via plain caller-supplied location ids; six domain-awareness catalogues (Spatial/Temporal/Social/Ecological/Economic/Mission) are kept as pure reference data rather than six near-duplicate storage classes, since the real composition happens by feeding real domain-system values into `WorldContext` at the call site; "Simulation Support" is the sixth parallel "which systems does this touch" list after AF-142/144/145/149's real lists; DebugSnapshot gains a new `worldModel` field; live `worldModel entities 1 · needing help 0 · top goal "Research anomaly" · location settlement-lucent-gate · memories 1 · importance 70 · forecast 52 · priority High` on the overlay (goal ranking correctly ordered by priority), browser-verified | Aligned | 1704 tests green (7 new) | Approved 9.5/10 | **LOCKED** |
| AF-153 | The Atlas Simulation Director | v1 *(Responsibilities (17 systems)/Simulation Tiers/Attention System/Simulation Budget/Emotional Pacing/System Synchronisation/Event Prioritisation/Living Background/Load Balancing/Narrative Guardrails/Emergence Manager/Diagnostics — the executive orchestration layer, composing with AF-136/144/148/151's real classes rather than duplicating any of them)* | Complete — new `src/game/simulationDirector/` module (`simulationDirectorData.ts` + `SimulationDirectorRuntime.ts`); confirmed "System Synchronisation"/"Event Prioritisation" are exactly AF-144's real `DecisionRouter` and "Simulation Budget"/"Load Balancing" are exactly AF-144's real `PerformanceBudgetTracker`, both reused directly via the existing `aosDecisionRouter`/`aosPerformanceBudget` instances; `SimulationTierEngine` mirrors AF-144's real `PriorityEngine` frame-divisor mechanism but over its own separate 5-tier union, since that class is hand-typed to its own closed 3-tier union; `allocateSimulationBudget` is a genuinely different granularity from AF-144's domain-based tracker — proportional per-entity CPU shares from real Attention Scores; `EmotionalPacingTracker` (8 categories) is confirmed materially wider than AF-136's real `StoryDirector` (3 categories), kept separate; `narrativeGuardrailsRespected` is the eighth all-must-pass checklist gate in this codebase, a different scope from AF-148's `expansionRespectsTimeline`; `EmergenceOpportunityLog`'s real discovery mechanism composes AF-151's real `KnowledgeGraph.suggestConnections` rather than reimplementing it; "Responsibilities" is the seventh parallel "which systems does this touch" list after AF-142/144/145/149/152's real lists; DebugSnapshot gains a new `simulationDirector` field; live `simDir tier Immediate · attention 65 (budget 100%) · pacing imbalance none · guardrails respected · emergence 1 · throttle [none]` on the overlay, browser-verified | Aligned | 1714 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-154 | The Atlas Orchestrator | v1 *(Primary Responsibilities (17 systems)/Player Experience Model/Pacing Model (8-stage cycle)/Discovery Curve/Surprise Engine/Emotional Balancer/System Negotiation/Content Rotation/Long-term Memory/Engagement Map/Expansion Readiness/Player Journey Model/Orchestration Tools/Failsafe Rules — the experience-management layer above AF-153's Simulation Director, composing with AF-136/144/149/153's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasOrchestrator/` module (`atlasOrchestratorData.ts` + `AtlasOrchestratorRuntime.ts`); confirmed "System Negotiation" is exactly AF-144's real `DecisionRouter`, "Expansion Readiness" is exactly AF-149's real `systemImpactReportFor`/`SYSTEM_IMPACT_CATEGORIES` (an 11-category superset covering the requested 5), and "Surprise Engine" is confirmed the same mechanic as AF-153's real `EmergenceOpportunityLog` (shared "Historic callback"/"Commander reunions" vocabulary) — all three reused directly via existing instances, no new classes; `PacingCycleTracker`'s 8-stage cycle genuinely differs in membership from AF-153's real `EMOTIONAL_PACING_CATEGORIES` (also 8 values) and is explicitly a closed loop, so it gets its own `nextPacingStage` mirroring AF-095/097/149's real `next*PipelineStage` pattern rather than AF-153's imbalance-only shape; `DiscoveryCurveTracker` is the third "kind of notable moment" list after AF-136's real `EMERGENT_MOMENT_KINDS` and AF-153's `EMERGENCE_OPPORTUNITY_KINDS`, kept separate since it measures time-since-last rather than surfacing or logging; `LongTermMemoryLog` is player-journey-level, distinct from AF-133's real per-NPC `NpcMemoryLog`; `PLAYER_JOURNEY_TIERS` is the fourth player-rank-title list after AF-139's real `PLAYER_EVOLUTION_RANKS`/`COMMANDER_MATURITY_STAGES` and AF-136's real `PLAYER_REPUTATION_TITLES`, with no exact membership match; `resolveByFailsafePriority` is a strict ordering, deliberately not folded into the codebase's all-must-pass checklist-gate family; `PlayerExperienceTracker`, `emotionalToneNeedsRebalancing`, `ContentRotationTracker`, and `EngagementMap` (advisory-only) are all confirmed genuinely new; "Primary Responsibilities" is the eighth parallel "which systems does this touch" list after AF-142/144/145/149/152/153's real lists; DebugSnapshot gains a new `atlasOrchestrator` field; live `orchestr pacing Discovery (next Conversation, stalled=false) · experience wonder 55 · discovery dry=true · rotation recommend "Rare wildlife" · memory "First contact with a remote civilisation." · engagement recommend "museum" · negotiation winner player-input · expansion impact [Gameplay, Narrative, Accessibility] · tone rebalancing=true · failsafe Player progress · journey Experienced Pathfinder · surprises 1` on the overlay, browser-verified | Aligned | 1728 tests green (14 new) | Approved 9.5/10 | **LOCKED** |
| AF-155 | The Atlas Intelligence Engine | v1 *(Intelligence Layers (6-stage)/Knowledge Sources (13 systems)/Commander+Colony+Scientific+Exploration+Social+Strategic Reasoning/Memory Integration/Learning Loop (6-stage)/Uncertainty Model/Collaborative Intelligence/Discovery Suggestions/Developer Tools — the reasoning layer above AF-153/154, composing with AF-030/133/134/135/144/151's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasIntelligence/` module (`atlasIntelligenceData.ts` + `AtlasIntelligenceRuntime.ts`); confirmed "Potential Commander collaborations" is exactly AF-151's real `KnowledgeGraph.suggestConnections`, reused directly; the Uncertainty Model's confidence formula echoes but is genuinely distinct from AF-144's real `PredictionEngine.forecast` (ranking margin, not time-series variance); Commander Reasoning is deliberately built so AF-030's real `PersonalityTrait` never weights the numeric `rankOptions` score, respecting that module's own "dialogue-only... not just discouraged by convention" design law, confirmed by a dedicated test; "Intelligence Layers" and the "Learning Loop" read as near-synonyms but are confirmed NOT identical (Layers include "Prediction", the Loop includes "Improve", neither has the other's) — kept as two separate typed stage lists driven by one new generic `CyclicStageTracker<TStage>`; the six "reasoning" sections each name their own factor vocabulary but share one `rankOptions` decoupled composer rather than six near-identical classes; "Knowledge Sources" is the ninth parallel "which systems does this touch" list after AF-142/144/145/149/152/153/154's real lists; "Discovery Suggestions" is the fourth "kind of notable moment" list after AF-136/153/154's real lists; `knowledgeRichnessScore` (Memory Integration) and `CollaborativeProblemLog` (Collaborative Intelligence) are both confirmed genuinely new; DebugSnapshot gains a new `atlasIntelligence` field; live `intel layer Observation (next Understanding) · loop Observe (next Interpret) · decision investigate-signal confidence 0.91 (confident) · knowledge richness 8 · collaborations 1 · suggestions 1` on the overlay, browser-verified | Aligned | 1739 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-156 | The Atlas Decision Engine | v1 *(Decision Pyramid (7 levels)/Decision Factors (10)/Commander+Citizen+Colony+Research+Exploration+Wildlife+Social Decisions/Uncertainty/Ethical Framework/Player Influence/Group Decisions/Long-term Planning (5 horizons)/Decision Explanations — the action layer above AF-155's reasoning layer, composing with AF-030/153/154/155's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasDecision/` module (`atlasDecisionData.ts` + `AtlasDecisionRuntime.ts`); confirmed Decision Pyramid Levels III–VI are exactly AF-155's real `rankOptions` (composed directly by `explainDecision`), "Uncertainty" is exactly AF-155's real Uncertainty Model (`suggestUncertaintyResponse` reused directly), the Pyramid's own 7-level progression is driven by AF-155's real generic `CyclicStageTracker<TStage>` directly, and "Group Decisions" is the same mechanic as AF-155's real "Collaborative Intelligence" (`CollaborativeProblemLog`) at a formal-body granularity — all four reused directly, no second scoring/confidence/stage-tracker/consensus mechanism; the module's biggest apparent overlap (AF-155's six reasoning-FACTOR lists vs. this module's seven Decisions sections) is resolved as a genuinely different axis — decision SLOTS, not evaluation criteria — confirmed by a test showing `EXPLORATION_DECISION_KINDS` shares zero members with AF-155's real `EXPLORATION_REASONING_FACTORS`; "Decision Factors" lists "Personality" but this module never lets AF-030's real `PersonalityTrait` weight the numeric score, respecting that module's own design law, confirmed by a dedicated test; `ethicalAlignmentScore`, `cappedPlayerInfluence` (hard 0.5 ceiling, "never absolute control"), and `planningHorizonRank` are all confirmed genuinely new; `DecisionLog.isRepetitive` satisfies the self-review's "eliminate repetitive choices" directive by generalising AF-153/154's real stall/imbalance check across any decision domain; DebugSnapshot gains a new `atlasDecision` field; live `decision pyramid Need (next Context) · decision expand-north confidence 0.38 rejected [expand-south] · repetitive=false · ethics 18 · player influence 0.50 (cap 0.5) · horizon rank 3` on the overlay, browser-verified | Aligned | 1749 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-157 | The Atlas Planning Engine | v1 *(Planning Horizons (5, seconds→decades)/Plan Components (11 fields)/Commander+Colony+Scientific+Exploration+Economic+Environmental Planning/Adaptive Planning/Collaborative Planning/Plan Negotiation/Plan Memory/Contingency System/Visualisation — the coordination layer above AF-156's decisions, composing with AF-150/153/155/156's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasPlanning/` module (`atlasPlanningData.ts` + `AtlasPlanningRuntime.ts`); confirmed "Collaborative Planning" is the THIRD instance of the identical mechanic in this codebase after AF-155's own Collaborative Intelligence and AF-156's Group Decisions (both `CollaborativeProblemLog`), and "Plan Negotiation" is the same weigh-and-rank mechanic as AF-156's real `explainDecision` — both reused directly, no third scoring/consensus mechanism; "Planning Horizons" (Immediate/Short-Term/Medium-Term/Long-Term/Generational) is confirmed a genuinely different axis from AF-156's real `LONG_TERM_PLANNING_HORIZONS` (decision scope) and AF-153's real `SIMULATION_TIERS` (spatial scope) despite all three sharing "Immediate" as a name, verified by a dedicated test; the six planning-domain sections are a different axis again from AF-155's reasoning factors and AF-156's decision kinds, kept as six new `*_PLAN_KINDS` reference unions; `PlanRegistry.dependenciesSatisfied` mirrors AF-150's real dependency-graph philosophy at the plan granularity (a dependency only resolves once registered AND complete); `PlanAdaptationLog`, `PlanMemoryArchive`, and `ContingencySet`/`hasContingencyCoverage` (a structural 6-slot check, never a numeric score) are all confirmed genuinely new; DebugSnapshot gains a new `atlasPlanning` field; live `planning plan reforest-verdance horizon Long-Term · contingency coverage=true · dependencies satisfied=true · adaptations 1 · memory 0 · negotiation winner reforest-verdance` on the overlay, browser-verified | Aligned | 1757 tests green (8 new) | Approved 9.5/10 | **LOCKED** |
| AF-158 | The Atlas Future Engine | v1 *(Future Horizons (5, minutes→centuries)/Future States (5 branching scenarios)/Forecast Factors (11)/Commander+Colony+Research+Exploration+Environmental+Economic Forecasting/Risk Analysis/Opportunity Analysis/Multiple Futures/Future Memory/Visualisation — the foresight layer above AF-157's planning, composing with AF-144/155/157's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasFuture/` module (`atlasFutureData.ts` + `AtlasFutureRuntime.ts`); confirmed numeric point-forecasts compose AF-144's real `PredictionEngine.forecast` directly ("Population growth" is a verbatim shared member between `COLONY_FORECASTING_KINDS` and AF-144's real `PREDICTION_KINDS`), the same direct reuse AF-152 already made; "Future Horizons" is confirmed the FOURTH "Immediate"-sharing time/scope ladder after AF-153/156/157's real lists, verified by a dedicated test that all four differ; "Opportunity Analysis" mirrors the SHAPE of AF-155's real `DiscoverySuggestionLog` but never its TYPE (zero member overlap, confirmed the fifth "notable moment" list), and "Future Memory" mirrors the SHAPE of AF-157's real `PlanMemoryArchive` (one verbatim shared member, "Educational material", the rest genuinely different) — both new classes typed to their own unions per the established missed-generalisation precedent; "Future States" (5 branching scenarios with continuously-updating confidence) is confirmed genuinely new, a fundamentally different shape from AF-144's single-point-forecast `PredictionEngine`; `mostLikelyFutureState` and `RiskLog` ("risk creates preparation, not punishment") are both new; DebugSnapshot gains a new `atlasFuture` field; live `future forecast 52 (confidence 100%) · likely state Most Likely Future · risk 0.40 · opportunities 1 · future memory 0` on the overlay, browser-verified | Aligned | 1766 tests green (9 new) | Approved 9.5/10 | **LOCKED** |
| AF-159 | The Atlas Possibility Engine | v1 *(Possibility Sources (12 systems)/Discovery Categories (12)/Scientific+Engineering+Commander+Colony Innovation/Player Inspiration/Serendipity/Cross-Disciplinary Thinking/Cultural Evolution/Unsolved Mysteries/Opportunity Network/Innovation Memory/Visualisation — the imagination layer above AF-158's Future Engine, composing with AF-151/155/157/158's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasPossibility/` module (`atlasPossibilityData.ts` + `AtlasPossibilityRuntime.ts`); confirmed "Serendipity" is exactly the shape of question AF-151's real `KnowledgeGraph.suggestConnections` already answers, reused directly at the call site; "Player Inspiration" mirrors the SHAPE of AF-155's real `DiscoverySuggestionLog` and AF-158's real `OpportunityLog` but never their TYPE (zero member overlap, confirmed the sixth "notable moment" list), and "Innovation Memory" mirrors the SHAPE of AF-157's real `PlanMemoryArchive`/AF-158's real `FutureMemoryArchive` (the third mirrored memory-archive shape) — both new classes typed to their own unions per the established missed-generalisation precedent; "Possibility Sources" is the tenth parallel "which systems does this touch" list after AF-142/144/145/149/152/153/154/155/158's real lists; `PossibilityRegistry` (full Opportunity Network shape), `SerendipityLog` (free-text, since the spec's own examples are full scenarios not category names), `MysteryLog` (stays open until resolved), and `CulturalTrendTracker` (plain trend names, not a closed union) are all confirmed genuinely new; DebugSnapshot gains a new `atlasPossibility` field; live `possible possibilities 1 (Scientific) · inspiration 1 · serendipity 1 · mysteries unsolved 1/1 · cultural adopters 0 · innovation memory 0` on the overlay, browser-verified | Aligned | 1775 tests green (9 new) | Approved 9.5/10 | **LOCKED** |
| AF-160 | The Atlas Wisdom Engine | v1 *(Wisdom Sources (13 systems)/Wisdom Dimensions (12)/Commander+Scientific+Civilisation+Ecological+Cultural Wisdom/Historical Reflection/Mentorship System/Ethical Deliberation/Reflection Loop (6-stage)/Civilisation Values (10)/Generational Transfer (5-stage ladder)/Wisdom Memory/Developer Tools — the philosophical-maturation layer above AF-155–159, composing with AF-030/139/155/157/158/159's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasWisdom/` module (`atlasWisdomData.ts` + `AtlasWisdomRuntime.ts`); confirmed "Reflection Loop" is driven directly by AF-155's real generic `CyclicStageTracker<TStage>` (already reused for AF-155's own two lists and AF-156's Decision Pyramid), no new tracker class; "Commander Wisdom" is a genuinely different axis from AF-030's real fixed/dialogue-only `PersonalityTrait` and AF-139's real coarse 4-stage `COMMANDER_MATURITY_STAGES`, verified by a dedicated zero-overlap test; "Civilisation Values" is confirmed another entry in the codebase's recurring abstract-value-list family (after the real Constitution's Design Pillars, AF-146's `TEN_PILLARS`, AF-145's Atlas Principle virtues, AF-147's `CORE_THEMES`); "Scientific Wisdom"/"Ethical Deliberation" are two more instances of the established all-must-pass checklist-gate mechanic; "Wisdom Memory" mirrors the SHAPE of AF-157/158/159's real `PlanMemoryArchive`/`FutureMemoryArchive`/`InnovationMemoryArchive` (the fourth mirrored memory-archive shape), typed to its own union; `MentorshipLedger` is confirmed the first real mentor/mentee relationship ledger in the codebase (prior mentions elsewhere were reference vocabulary only); `generationalTransferRank` mirrors AF-148/154/157's real indexOf-rank pattern as a linear ladder rather than a cycle; DebugSnapshot gains a new `atlasWisdom` field; live `wisdom loop Experience (next Reflection) · wisdom 4.2 (patience 15) · mentor commander-thorne-starforged · scientific review=false · ethical review=true · transfer rank 1 · memory 0` on the overlay, browser-verified | Aligned | 1783 tests green (8 new) | Approved 9.5/10 | **LOCKED** |
| AF-161 | The Atlas Philosophy Engine | v1 *(Philosophical Domains (12)/Questions (7)/Commander Philosophy/Civilisation Dialogue/Academic Schools (5)/Scientific+Leadership Philosophy/Player Philosophy (observed, never labelled)/Cultural Reflection/Historical Reinterpretation/Philosophical Events/Interdisciplinary Thinking/Developer Tools — the reflective layer above AF-160's Wisdom Engine, composing with AF-135/159/160's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasPhilosophy/` module (`atlasPhilosophyData.ts` + `AtlasPhilosophyRuntime.ts`); confirmed "Philosophical Domains" is the closest two lists have ever come in this codebase to full duplication, sharing exactly 3 exact-string members with AF-160's real `WISDOM_DIMENSIONS` (Engineering/Leadership/Exploration) and near-synonym pairs for eight more, verified by a dedicated test, yet kept separate since the two tag genuinely different questions (judgement vs. debate topic); "Cultural Reflection" is exactly AF-159's real `CulturalTrendTracker`, reused directly via the existing `culturalTrends` instance; "Historical Reinterpretation" is exactly AF-135's real `PlanetaryChronicle`/`EvolvingEntry.expand` ("depth, not contradiction"), reused directly via the existing `chroniclePlanets` instance; "Interdisciplinary Thinking" (4 directional pairs) is confirmed a genuinely different shape from AF-159's real symmetric `CROSS_DISCIPLINARY_PAIRS`, zero exact-pair overlap verified by a dedicated test; "Commander Philosophy"'s six illustrative names (Atlas Prime/Cassia/Lyra/Orion/Sora/Vega) don't correspond to any real roster id, so `CommanderBeliefTracker` is built generically over real commander ids instead; "Player Philosophy" deliberately exposes only per-dimension tallies via `PlayerPhilosophyObserver`, never a combined label, confirmed by a dedicated test; `AcademicInfluenceTracker` and `PhilosophicalEventLog` are both confirmed genuinely new; DebugSnapshot gains a new `atlasPhilosophy` field; live `philosophy belief "Hope through unity." · school Conservation School · observed stewardship 1 · events 1 · cultural adopters 1 · chronicle versions 1` on the overlay, browser-verified | Aligned | 1792 tests green (9 new) | Approved 9.5/10 | **LOCKED** |
| AF-162 | The Atlas Purpose Engine | v1 *(Purpose Domains (12)/Individual+Commander+Civilisation+Player Purpose/Institutional Purpose/Long-term Missions/Purpose Evolution (5-stage ladder)/Cultural Purpose/Crisis of Purpose/Shared Purpose/Legacy of Purpose/Purpose Network/Developer Tools — the motivational layer above AF-161's Philosophy Engine, composing with AF-151/155/157/158/159/160/161's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasPurpose/` module (`atlasPurposeData.ts` + `AtlasPurposeRuntime.ts`); confirmed "Purpose Domains" is the HEAVIEST vocabulary overlap yet in this codebase, sharing 8 of 12 exact-string members with AF-161's real `PHILOSOPHICAL_DOMAINS`, verified by a dedicated test, yet kept separate since the two now sit alongside AF-160's `WISDOM_DIMENSIONS` as three lists asking three different questions over the same vocabulary; "Cultural Purpose" is confirmed almost entirely a subset of this module's own `PURPOSE_DOMAINS` (5 of 6 members), flagged as an internal near-duplicate; "Shared Purpose" is the fourth instance of AF-155/156/157's real `CollaborativeProblemLog` mechanic, reused directly via the existing `collaborativeProblems` instance; "Purpose Network" is exactly AF-151's real `KnowledgeGraph`, reused directly via the existing `knowledgeGraph` instance; "Legacy of Purpose" mirrors the SHAPE of AF-157/158/159/160's real memory-archive classes (the fifth mirrored shape), typed to its own union; "Player Purpose" is deliberately less strict than AF-161's real `PlayerPhilosophyObserver` (per the spec's own "reveal purpose" wording), with `dominantPurpose()` returning null until at least one action is observed, confirmed by a dedicated test; `IndividualPurposeTracker`/`CommanderPurposeTracker` mirror AF-161's real `CommanderBeliefTracker` full-history shape; `LongTermMissionTracker` gives real progress tracking; DebugSnapshot gains a new `atlasPurpose` field; live `purpose individual Scientist · commander legacy "Found a wildlife sanctuary." · player dominant Conservationist (stewardship? 1) · mission progress 40% complete=false · evolution rank 1 · shared contributors 2 · network neighbours 1 · memory 0` on the overlay, browser-verified | Aligned | 1803 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-163 | The Atlas Meaning Engine | v1 *(Meaning Domains (12)/Personal+Player+Collective Meaning (curated superlatives)/Community+Scientific+Historical+Cultural+Ecological+Exploration+Institutional Meaning/Symbols/Meaning Through Time/Quiet Moments/Developer Tools — the emotional-interpretation layer above AF-162's Purpose Engine, composing with AF-133/162's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasMeaning/` module (`atlasMeaningData.ts` + `AtlasMeaningRuntime.ts`); confirmed "Meaning Domains" shares 5 exact-string members with AF-162's real `PURPOSE_DOMAINS`, verified by a dedicated test, but confirmed NOT a new overlap record (AF-162's own 8-match overlap with AF-161 remains heaviest); "Personal Meaning"/"Player Meaning"/"Collective Memory" all share the identical curate-a-single-best-of-entry mechanic, so one generic `MeaningCurator<TCategory>` serves all three rather than three near-identical trackers, confirmed by a dedicated test; `PersonalMeaningCategory` is confirmed a genuinely different concept from AF-133's real `NpcMemoryLog` (every memory vs. one curated highlight per category); "Symbols" and "Meaning Through Time" share the identical growing-significance-through-reinforcement mechanic, so one generic `SignificanceTracker` serves both rather than two separate classes, confirmed by a dedicated test exercising both an object and an event; `CommunityMeaningTracker` and `QuietMomentLog` are both confirmed genuinely new; DebugSnapshot gains a new `atlasMeaning` field; live `meaning personal "First contact with the Verdance wildlife." · player favourite "Verdance" · collective "The Verdance famine relief." · symbol significance 1 · community meaning "Where the founder's helmet is displayed." · quiet moments 1` on the overlay, browser-verified | Aligned | 1811 tests green (8 new) | Approved 9.5/10 | **LOCKED** |
| AF-164 | The Atlas Experience Engine | v1 *(Experience Pillars (10)/Experience States (10)/Micro+Macro Experiences/Experience Rhythm/First-Time Moments/Returning Moments/Surprise Management/Player Expression/Emotional Memory/Atmospheric Design/Long-Term Experience (4-stage ladder)/Shared Experiences/Developer Tools — the experiential layer above AF-163's Meaning Engine, composing with AF-153/154/155/160/162/163's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasExperience/` module (`atlasExperienceData.ts` + `AtlasExperienceRuntime.ts`); confirmed "Experience Rhythm" shares 7/8 stages verbatim with AF-154's real `PACING_CYCLE_STAGES` (only Action vs Combat differs) and "Player Expression" shares 7/8 playstyles verbatim with AF-162's real `PLAYER_PURPOSE_KINDS` (only Scientist vs Founder differs) — both confirmed the SAME mechanism asked twice rather than this codebase's usual near-duplicate-domain-list pattern, reused directly via AF-154's real `PacingCycleTracker` and AF-162's real `PlayerPurposeObserver`; "Surprise Management" is the same shape as AF-153's real `EmergenceOpportunityLog` (4/6 near-exact examples), reused directly; "Emotional Memory" overlaps directly with AF-163's real `PERSONAL_MEANING_CATEGORIES`/`MeaningCurator`/`QuietMomentLog` ("Most meaningful discovery" verbatim), reused directly; "Micro Experiences"/"Returning Moments" compose AF-163's real `SignificanceTracker` directly; "Shared Experiences" is the FIFTH instance of AF-155's real `CollaborativeProblemLog` mechanic; "Experience Pillars" is another entry in the abstract-value-list family (3 exact matches with AF-160's real `CIVILISATION_VALUES`); "Experience States" mirrors the SHAPE of AF-154's real `PlayerExperienceFactors` but never its TYPE; `FirstTimeMomentTracker` and `AtmosphereCoordinator` are both confirmed genuinely new; DebugSnapshot gains a new `atlasExperience` field; live `experience rhythm Discovery · expression Conservationist · surprise 1 · first-time recruited=true (repeat blocked=true) · states curiosity=70 immersion=80 · atmosphere lighting=0.6 · shared festival 2 · long-term rank 1` on the overlay, browser-verified | Aligned | 1823 tests green (12 new) | Approved 9.5/10 | **LOCKED** |
| AF-165 | The Atlas Memory Engine | v1 *(Memory Types (10)/Personal+Player+Shared+Institutional+Cultural+Scientific Memory/Forgetting/Memory Triggers/Nostalgia/Intergenerational Memory/False Assumptions/Memory Network/Developer Tools — the living memory architecture above AF-135's Chronicle and AF-163's Meaning Engine, composing with AF-133/135/151/155/159/163's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasMemory/` module (`atlasMemoryData.ts` + `AtlasMemoryRuntime.ts`); confirmed Personal Memory composes AF-133's real `NpcMemoryLog` directly with AF-163's real `MeaningCurator` directly; Player Memory reuses AF-163's real `playerMeaning` instance directly for "Favourite X" categories ("Favourite Commander" verbatim shared, confirmed by a dedicated test); Shared Memory is the sixth instance of AF-155's real `CollaborativeProblemLog` mechanic; Cultural Memory reuses AF-159's real `CulturalTrendTracker` directly; Memory Network reuses AF-151's real `KnowledgeGraph` directly; Nostalgia composes AF-163's real `SignificanceTracker.reinforce` directly; "Forgetting" is confirmed already handled by AF-133's real `NpcMemoryLog`'s bounded `minorCapacity`, verified by a dedicated test, no second decay mechanism; "False Assumptions" justifies why `MemoryDistortionTracker` stays separate from AF-135's real `EvolvingEntry` — the opposite guarantee (subjective drift vs. objective append-only depth), confirmed by a dedicated test; `PlayerMemoryTracker` (genuinely new numeric visit/photo counts) and `InstitutionalMemoryTracker` (genuinely new at this granularity) round out the module; DebugSnapshot gains a new `atlasMemory` field; live `memory distortion objective "The expedition made peaceful first contact." subjective "I remember it as far more dangerous than it really was." · most visited planet-verdance (2 visits) · photos 1 · institution memories 1 · personal memories 1 · network neighbours 1` on the overlay, browser-verified | Aligned | 1834 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-166 | The Atlas Consciousness Engine | v1 *(Consciousness Components (12)/Identity Model/Self Reflection/Values/Personal Growth/Commander Evolution/Social Identity/Private Life/Life Stages (5-stage ladder)/Moral Reasoning/Self Improvement/Emotional Continuity/Cultural+Collective Identity/Developer Tools — the continuity-of-self layer, composing with AF-139/155/160/161/162/163's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasConsciousness/` module (`atlasConsciousnessData.ts` + `AtlasConsciousnessRuntime.ts`); confirmed "Self Reflection" is exactly AF-160's real Reflection Loop `CyclicStageTracker`, reused directly and composed with AF-163's real `personalMeaning` `MeaningCurator`; "Moral Reasoning" is exactly AF-155's real `rankOptions`, reused directly, confirmed by a dedicated test; "Values" shares 7 of 9 exact-string members with AF-162's real `PURPOSE_DOMAINS`, verified by a dedicated test, kept as reference vocabulary since the real new mechanic is `ValuePriorityTracker`'s capped-delta-per-update constraint; "Life Stages" describes the same career-progression quantity as AF-139's real locked `COMMANDER_MATURITY_STAGES` but can't be re-segmented into 5 without modifying it, kept as its own separate ladder, confirmed distinct by a dedicated test; "Personal Growth" shares exactly 3 exact members with AF-160's real `COMMANDER_WISDOM_TRAITS`, so `PersonalGrowthTracker` mirrors that tracker's shape over a new 8-value union instead; `IdentityRegistry` (full evolving history, mirroring AF-161's real `CommanderBeliefTracker`), `hasIdentityGap` (plain structural divergence check), and `EmotionalContinuityTracker` ("hope gradually returns after setbacks") are all confirmed genuinely new; DebugSnapshot gains a new `atlasConsciousness` field; live `conscious identity "Wildlife Commander" gap=false · reflection Experience (topic "none") · value Exploration=5 · growth "Decision quality"=40 (overall 5.0) · hope 70 · moral choice report-the-anomaly · life stage rank 1` on the overlay, browser-verified | Aligned | 1845 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-167 | The Atlas Identity Engine | v1 *(Identity Layers (10)/Personal+Professional+Commander+Colony+Planetary+Institutional+Cultural Identity/Historical Identity/Reputation/Symbolism/Interaction/Identity Through Change/Developer Tools — the external-recognition layer above AF-166's Consciousness Engine, composing with AF-159/160/162/163/166's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasIdentity/` module (`atlasIdentityData.ts` + `AtlasIdentityRuntime.ts`); confirmed "Personal Identity" reuses AF-163's real generic `MeaningCurator<TCategory>` directly over a new `SignatureTraitKind` type parameter, no mirrored class needed since that class was already a reusable generic; "Commander Identity" and "Institutional Identity" both describe the same others-recognise-a-quality-over-time mechanic (a different axis from AF-160/166's real internal-growth trackers), so one generic `ReputationTracker` serves both, confirmed by a dedicated test; "Cultural Identity" reuses AF-159's real `CulturalTrendTracker` directly; "Symbolism" composes AF-163's real `SignificanceTracker` directly, its own `SYMBOLISM_CATEGORIES` (symbol kinds) sharing no members with AF-163's real `SYMBOL_EXAMPLES` (artifact instances); `EarnedTitleTracker` ("identity becomes history") is confirmed genuinely new and append-only, distinct from AF-135's Chronicle and AF-163's `MeaningCurator`; "Identity Layers" is confirmed a genuinely different axis from AF-166's real `CULTURAL_IDENTITY_LAYERS`; "Professional Identity" shares 4 exact members with AF-162's real `INDIVIDUAL_PURPOSE_KINDS`, kept as reference vocabulary; DebugSnapshot gains a new `atlasIdentity` field; live `identity signature ""The wild remembers kindness."" · reputation Mentorship (2) · titles 1 · cultural adopters 1 · symbol significance 1` on the overlay, browser-verified | Aligned | 1854 tests green (9 new) | Approved 9.5/10 | **LOCKED** |
| AF-168 | The Atlas Soul Engine | v1 *(Soul Dimensions (12)/Collective Character/Civilisational+Commander+Place+Community Spirit/Rituals/Moments of Humanity/Beauty Index/Inspiration/Collective Memory/Galactic Reputation/Soul Through Adversity/Developer Tools — the civilisation-wide collective character layer above AF-167's Identity Engine, composing with AF-159/160/163/166/167's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasSoul/` module (`atlasSoulData.ts` + `AtlasSoulRuntime.ts`); confirmed "Commander Spirit"/"Galactic Reputation" are exactly AF-167's real `ReputationTracker`/`EarnedTitleTracker` (the latter reused at civilisation scale, entity id "humanity"), "Place Spirit" composes AF-163's real `SignificanceTracker` directly, "Community Spirit" reuses AF-159's real `CulturalTrendTracker` directly, "Inspiration" composes AF-160's real `MentorshipLedger` directly, "Collective Memory" is exactly AF-163's real `collectiveMemory` `MeaningCurator` instance, and "Soul Through Adversity" is exactly AF-166's real `EmotionalContinuityTracker` reused at civilisation scale — all confirmed by dedicated tests, no second symbol/legend/propagation/adversity system; "Soul Dimensions" is confirmed the HEAVIEST PROPORTIONAL overlap yet in this codebase (8 of AF-160's real 10-member `CIVILISATION_VALUES`, 80%, surpassing AF-162's prior 67% record), kept separate since ideals-to-reinforce and emergent-only-measured spirit are genuinely different mechanics, verified by a dedicated test; the self-review's explicit "never a numerical morality system" directive is honoured by `CollectiveCharacterTracker` (witnessed counts + emergent dominant trait only, mirroring AF-162's real `PlayerPurposeObserver` "reveal, never assign" discipline, confirmed by a dedicated test with no morality-score property) and `RitualLog` (structurally no mandatory/completion field); `MomentsOfHumanityLog` and `BeautyIndexTracker` are both confirmed genuinely new; DebugSnapshot gains a new `atlasSoul` field; live `soul character Welcoming · rituals 1 · humanity moments 1 · beauty 70 · galactic reputation Compassion · civilisation hope 75` on the overlay, browser-verified | Aligned | 1867 tests green (13 new) | Approved 9.5/10 | **LOCKED** |
| AF-169 | The Atlas Legacy of Tomorrow | v1 *(Legacy Domains (12)/Generational Inheritance/Commander+Player Legacy/Living Inheritance/The Great Continuum/Legacy Projects/Inspiration Network/Remembrance/Evolving Traditions/The Horizon Principle/Galactic Maturity/The Never-Ending Story/Developer Tools — the explicit capstone of the Atlas architecture, composing with AF-151/159/160/162/163/166/167/168's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasLegacyOfTomorrow/` module (`atlasLegacyOfTomorrowData.ts` + `AtlasLegacyOfTomorrowRuntime.ts`); confirmed "Legacy Projects" is exactly AF-162's real `LongTermMissionTracker`, Commander Legacy's "Students" is exactly AF-160's real `MentorshipLedger.menteesOf`, "Inspiration Network" is the same mechanic AF-168's own "Inspiration" already composed (AF-160/151), "Remembrance" composes AF-163's real `SignificanceTracker`/AF-166's real `EmotionalContinuityTracker`, "Evolving Traditions" reuses AF-168's real `RitualLog`/AF-159's real `CulturalTrendTracker`, and "Galactic Maturity" is exactly AF-167's real `ReputationTracker` at civilisation scale — all reused directly, all confirmed by dedicated tests, no new duplicate class; "The Horizon Principle" composes AF-159's real `MysteryLog` directly via the module's own genuinely new `ensureNextHorizonOpen`, a thin function chaining a real `LongTermMissionTracker` completion to a real `MysteryLog.open` call exactly once per mission id, confirmed by a dedicated test; "Legacy Domains" ties the absolute-count overlap record (8/12) with AF-162's real `PURPOSE_DOMAINS` without breaking AF-168's proportional record, kept as reference vocabulary tagging cross-generational inheritance; `NextGenerationLog` ("The Never-Ending Story") is confirmed genuinely new, scoped specifically to generational-continuation moments, distinct from AF-168's real `MomentsOfHumanityLog`; DebugSnapshot gains a new `atlasLegacyOfTomorrow` field; live `legacyTmrw legacy project 100% complete=true · horizon open=true unsolved 2 · students 1 · maturity Compassion · next generation 1` on the overlay, browser-verified | Aligned | 1876 tests green (9 new) | Approved 9.5/10 | **LOCKED** |
| AF-170 | The Atlas Prime Directive | v1 *(The Prime Directives (10, priority order)/Design Arbiter/Conflict Resolution (priority order)/System Priority (in-fiction only)/Future Compatibility/Quality Lock/Redundancy Detector/Evolution Guardian/The Player+Developer Promise/Final Test/Developer Tools — the permanent governing intelligence, never itself a gameplay system, composing with AF-143/149/156's real classes rather than duplicating any of them; explicitly never claims authority over the real docs/CONSTITUTION.md)* | Complete — new `src/game/atlasPrimeDirective/` module (`atlasPrimeDirectiveData.ts` + `AtlasPrimeDirectiveRuntime.ts`); confirmed and documented via a prominent CRITICAL SCOPE NOTE that "System Priority" ranks only in-fiction AF-XXX modules (AF-146/145/000/153) and never the real Constitution, per this project's standing rule since AF-145/146/147, verified by a dedicated test; "Design Arbiter" mirrors the SHAPE of AF-143's real `DesignScoreCard`/AF-149's real `AtlasScoreCard` (the third such mirrored scoring rubric), typed to its own union; "The Prime Directives" and "Conflict Resolution" both mirror AF-156's real `resolveByFailsafePriority` pattern as two separate priority orders, confirmed by dedicated tests; "Future Compatibility"/"Quality Lock"/"Final Test" are three more all-must-pass checklist gates, with "Final Test" a deliberate structural callback to the real Constitution's own "THE AFTERLIGHT TEST"; "Redundancy Detector" is the module's own genuinely new, self-referential contribution — `detectOverlap` formalises this session's manual overlap-checking discipline into a real function, confirmed by a dedicated test reproducing two previously hand-computed overlap facts exactly (Purpose/Philosophy 8/12, Legacy/Purpose 8/12); DebugSnapshot gains a new `atlasPrimeDirective` field; live `primeDir directive Protect Hope · conflict Player agency · design score 9.6 (passed) · future compat=true · quality lock=true · final test=true · system rank 4 · overlap[Purpose,Philosophy] 8/12` on the overlay, browser-verified | Aligned | 1884 tests green (8 new) | Approved 9.5/10 | **LOCKED** |
| AF-171 | The Atlas Creative Intelligence | v1 *(Creative Domains (12)/Creative Sources/Commander+Scientific+Engineering+Cultural+Educational+Player Creativity/Collaborative Creation/Creative Movements/Creative Heritage/Discovery Through Creation/Beauty Principle/Creative Network/Developer Tools — ensuring civilisation never stops creating, composing with AF-030/151/155/159/160/165/168/169/170's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasCreativeIntelligence/` module (`atlasCreativeIntelligenceData.ts` + `AtlasCreativeIntelligenceRuntime.ts`); confirmed "Cultural Creativity"/"Creative Movements" reuse AF-159's real `CulturalTrendTracker` directly, "Educational Creativity"/"Creative Network" reuse AF-160's real `MentorshipLedger` directly, "Player Creativity"'s "Photography" reuses AF-165's real `PlayerMemoryTracker.photograph` directly, "Collaborative Creation" is the seventh instance of AF-155's real `CollaborativeProblemLog` mechanic, "Beauty Principle" is exactly AF-168's real `BeautyIndexTracker`, "Creative Network" also reuses AF-151's real `KnowledgeGraph`, and "Discovery Through Creation" composes AF-159's real `MysteryLog.open` directly (the same mechanic AF-169's `ensureNextHorizonOpen` formalised) — all reused directly, all confirmed by dedicated tests; "Creative Domains" is confirmed a NEW ABSOLUTE overlap record (9 of 12 exact-string matches with AF-169's real `LEGACY_DOMAINS`, surpassing the previous 8-member record), verified using AF-170's real `detectOverlap` function; "Creative Movements" and "Engineering Creativity" are both confirmed near-total conceptual duplicates of AF-159's real lists with minimal exact-string overlap, also verified via `detectOverlap`; "Creative Heritage" mirrors the SHAPE of AF-157/158/159/160/162's real memory-archive classes (the sixth mirrored shape); `CreativeContributionLog` is the module's own genuinely new generic contribution log serving all "X Creativity" sections uniformly, deliberately never wired to AF-030's real dialogue-only `PersonalityTrait`, confirmed by a dedicated test; DebugSnapshot gains a new `atlasCreativeIntelligence` field; live `creative contributions 1 (engineering 1) · heritage 2 · cultural adopters 1 · collaborators 2 · beauty Art=80 · photos 2 · mysteries unsolved 3 · idea neighbours 1 · domain overlap[Creative,Legacy] 9/12` on the overlay, browser-verified | Aligned | 1897 tests green (13 new) | Approved 9.5/10 | **LOCKED** |
| AF-172 | The Atlas Imagination Engine | v1 *(Imagination Domains (12)/Individual Imagination/Commander Visions/Scientific+Engineering+Children's+Cultural+Exploration+Historical Imagination/Collective Dreams/The Dream Network/Imagination Without Fantasy/Future Myths/The Horizon Effect/Developer Tools — the visionary layer above AF-171's Creative Intelligence, composing with AF-155/159/161/162/169/170/171's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasImagination/` module (`atlasImaginationData.ts` + `AtlasImaginationRuntime.ts`); confirmed "Commander Visions" reuses AF-161's real `CommanderBeliefTracker` directly (already a plain evolving-string generic, the same situation AF-167 found with AF-163's `MeaningCurator`), "Engineering Imagination"/"Collective Dreams" both compose AF-162's real `LongTermMissionTracker.register` directly, "Historical Imagination" composes AF-159's real `MysteryLog.open` directly, "The Dream Network" is driven directly by AF-155's real generic `CyclicStageTracker<TStage>` over the module's own 5-stage `DREAM_NETWORK_STAGES` union, and "The Horizon Effect" reuses AF-169's real `ensureNextHorizonOpen` directly — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "Imagination Domains" ties (does not break) AF-171's absolute overlap record, sharing 9 of 12 exact-string members with the real `CREATIVE_DOMAINS`, verified via AF-170's real `detectOverlap`, kept as its own reference vocabulary tagging what a speculative idea concerns; `HypothesisTracker` is confirmed genuinely new — a speculative-idea registry with an explicit grounded/ungrounded state, modelling "imagination never breaks established science," confirmed by a dedicated test to start every idea ungrounded and only mark it grounded through an explicit later action, distinct from AF-155's real `rankOptions` and AF-159's real `PossibilityRegistry`; DebugSnapshot gains a new `atlasImagination` field; live `imagination vision "I dream of a living city that restores itself." · hypothesis grounded=false · engineering idea 0% · dream stage Imagine · mysteries unsolved 3 · domain overlap[Imagination,Creative] 9/12` on the overlay, browser-verified | Aligned | 1905 tests green (8 new) | Approved 9.5/10 | **LOCKED** |
| AF-173 | The Atlas Possibility Space | v1 *(Possibility Categories (12)/Simulation Sandbox/Commander Thinking/Scientific+Engineering+Civilisation+Player Possibility/Multiple Futures/Safe Experimentation/Failed Possibilities/Possibility Network/Innovation Filter/Developer Tools — the conceptual frontier above AF-172's Imagination Engine, composing with AF-143/149/158/159/170/172's real classes rather than duplicating any of them; code lives under the distinct `atlasPossibilitySpace` directory since AF-159 already occupies `atlasPossibility`)* | Complete — new `src/game/atlasPossibilitySpace/` module (`atlasPossibilitySpaceData.ts` + `AtlasPossibilitySpaceRuntime.ts`); confirmed "Possibility Network" is exactly AF-159's real `Possibility`/`PossibilityRegistry` field-for-field, "Multiple Futures" is mechanically identical to AF-158's real `FUTURE_STATE_KINDS`/`FutureStateForecast`/`mostLikelyFutureState` (wording differs, the five-parallel-never-committed-branch shape does not), "Scientific Possibility"'s evidence gate is exactly AF-172's real `HypothesisTracker`, and "Failed Possibilities" is exactly AF-159's real `InnovationMemoryArchive` — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "Possibility Categories" ties (does not break) the codebase's 8/12 absolute overlap record against AF-159's real `DISCOVERY_CATEGORIES`, verified via AF-170's real `detectOverlap`, kept as its own reference vocabulary tagging a sandboxed, not-yet-committed possibility's domain; `SandboxScenarioRegistry` (genuinely new — a proposed/committed gate modelling "no real-world consequences occur until decisions are made," the commitment-gate counterpart to AF-172's evidence-gate `HypothesisTracker`) and `InnovationFilterScoreCard` (the FOURTH mirrored scoring-rubric shape after AF-143/149/170's real rubrics, same 9.5 gate) round out the module; DebugSnapshot gains a new `atlasPossibilitySpace` field; live `possibility network possibilities 2 · future Most Likely Future · scientific grounded=false · sandbox committed=false · failed outcomes 2 · filter score 9.6 (passed) · domain overlap[Possibility,Discovery] 8/12` on the overlay, browser-verified | Aligned | 1913 tests green (8 new) | Approved 9.5/10 | **LOCKED** |
| AF-174 | The Atlas Horizon Engine | v1 *(Horizon Categories (12)/The Discovery Cascade/Living Frontiers/Commander+Player Horizons/Civilisation Horizons (8-stage ladder)/Horizon Network/The Unknown Index/Beyond the Map/The Horizon Effect/Educational+Legacy Horizons/Developer Tools — the perpetual-frontier layer formalising AF-169's own "horizon" concept, composing with AF-151/155/159/161/169/170/173's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasHorizon/` module (`atlasHorizonData.ts` + `AtlasHorizonRuntime.ts`); confirmed "Commander Horizons" reuses AF-161's real `CommanderBeliefTracker` directly (the third reuse of that evolving-string generic for an evolving aspiration), "Living Frontiers"/"The Unknown Index" both compose AF-159's real `MysteryLog.open`/`unsolved` directly ("Incomplete research"/"Lost expeditions" confirmed verbatim-shared with the real `MYSTERY_KINDS` via `detectOverlap`), "Horizon Network" composes AF-151's real `KnowledgeGraph.addEdge` directly, "Beyond the Map"/"Legacy Horizons" both reuse AF-169's real `ensureNextHorizonOpen` directly (the same chaining function AF-172's own "Horizon Effect" already reused), and "Civilisation Horizons" (the 8-stage "ambitions expand" ladder) is driven directly by AF-155's real generic `CyclicStageTracker<TStage>` over the module's own `CIVILISATION_HORIZON_STAGES` union — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "Discovery Cascade"/"Educational Horizons" stay pure reference vocabulary, composing the same three real classes at the call site; "Horizon Categories" ties (does not break) the codebase's 8/12 absolute overlap record against AF-173's real `POSSIBILITY_CATEGORIES`, verified via AF-170's real `detectOverlap`; `HorizonEffectTracker` is confirmed genuinely new — an unknown-index that grows, never shrinks, as recorded knowledge grows, modelling "knowledge expands humility, not certainty," confirmed by a dedicated test, distinct from AF-172's real `HypothesisTracker` and AF-166's real `EmotionalContinuityTracker`; DebugSnapshot gains a new `atlasHorizon` field; live `horizon vision "I want to map the unexplored region beyond Verdance." · mysteries unsolved 4 · network neighbours 1 · civilisation stage Discover · unknown index 2 (knowledge 1) · domain overlap[Horizon,Possibility] 8/12` on the overlay, browser-verified | Aligned | 1921 tests green (8 new) | Approved 9.5/10 | **LOCKED** |
| AF-175 | The Atlas Infinity Engine | v1 *(Evolution Cycles (7-stage cyclic ladder)/Infinity Domains (12)/Self-Growing Systems/Generational Handoff/The Expanding Universe+Mind+Heart+Legacy+Questions/Self-Renewal/Infinite Replayability/The Atlas Principle/The Player Principle/The Final Horizon/Developer Tools — the highest layer of the in-fiction Atlas enrichment chain only, explicitly never above the real docs/CONSTITUTION.md, composing with AF-151/155/159/160/162/166/169/170/172's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasInfinity/` module (`atlasInfinityData.ts` + `AtlasInfinityRuntime.ts`); CRITICAL SCOPE NOTE documented in-code and in the catalogue doc that "highest simulation layer" ranks only the in-fiction Atlas chain (AF-160→174→175), never the real Constitution, consistent with AF-145/146/147/170's precedent; confirmed "Evolution Cycles" is driven directly by AF-155's real generic `CyclicStageTracker<TStage>` over the module's own 7-stage `EVOLUTION_CYCLE_STAGES` union (explicitly cyclic, "the cycle never ends"), "The Expanding Questions"/"Self-Renewal" both compose AF-159's real `MysteryLog.open`/`resolve` and AF-169's real `ensureNextHorizonOpen` directly, and "The Expanding Heart"/"The Expanding Legacy" compose AF-160's real `MentorshipLedger`, AF-166's real `EmotionalContinuityTracker` (civilisation scale), and AF-162's real `LongTermMissionTracker` directly — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "Infinity Domains" is confirmed a NEW ABSOLUTE overlap record (10 of 12 exact-string matches with AF-172's real `IMAGINATION_DOMAINS`, surpassing the previous 9-member record), verified using AF-170's real `detectOverlap` function; "Self-Growing Systems"/"The Expanding Universe"/"The Expanding Mind"/"Infinite Replayability" stay pure reference vocabulary composing AF-151's real `KnowledgeGraph`/AF-159's real `CulturalTrendTracker` at the call site, and "The Atlas Principle"/"The Player Principle"/"The Final Horizon" are documented as prose-only philosophical statements, matching every prior module's treatment of non-mechanical sections; `GenerationalHandoffLedger` is confirmed genuinely new — measures cumulative inherited contribution across generations, guaranteeing a later generation's starting baseline equals the sum of every earlier generation's contribution count ("never from zero"), confirmed by a dedicated test, distinct from AF-169's real plain-witness `NextGenerationLog`; DebugSnapshot gains a new `atlasInfinity` field; live `infinity cycle Knowledge (next Knowledge) · generation 2 baseline 3 · cumulative contributions 3 · mentees 1 · civilisation hope 75 · domain overlap[Infinity,Imagination] 10/12` on the overlay, browser-verified | Aligned | 1927 tests green (6 new) | Approved 9.5/10 | **LOCKED** |
| AF-176 | The Atlas Continuum | v1 *(The Continuum (8-stage cyclic ladder)/Continuum Domains (12)/Time Continuity/Generational+Commander+Player Continuity/Continuous Discovery+Civilisation+Education+Ecology+Culture+Questions/The Threads/Continuum Map/Developer Tools — the permanent continuity framework above AF-175's Infinity Engine, composing with AF-151/155/159/160/169/170/175's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasContinuum/` module (`atlasContinuumData.ts` + `AtlasContinuumRuntime.ts`); confirmed "The Continuum" is driven directly by AF-155's real generic `CyclicStageTracker<TStage>` over the module's own 8-stage `CONTINUUM_STAGES` union ("history becomes a living cycle"), "Time Continuity"/"The Threads" both compose AF-151's real `KnowledgeGraph.addEdge` directly, "Generational Continuity"/"Player Continuity" both reuse AF-175's real `GenerationalHandoffLedger` directly (the same ledger spanning generations AND campaigns), "Commander Continuity" composes AF-160's real `MentorshipLedger` directly, "Continuous Culture" composes AF-159's real `CulturalTrendTracker` directly, and "Continuous Questions" composes AF-159's real `MysteryLog`/AF-169's real `ensureNextHorizonOpen` directly (at least the FOURTH module to reuse this completion-chains-to-a-new-mystery guarantee) — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "Continuum Domains" ties (does not break) the codebase's 10/12 absolute overlap record against AF-175's real `INFINITY_DOMAINS`, verified via AF-170's real `detectOverlap`; "Continuous Discovery"/"Continuous Civilisation" read as near-total conceptual duplicates of AF-174/175's real lists but share ZERO exact-string members with either, also confirmed via `detectOverlap`; `ThreadRegistry`/`allThreadsConnected` are confirmed genuinely new — a curation layer marking entities as permanently significant "threads," with `allThreadsConnected` composing AF-151's real `KnowledgeGraph.isIsolated` to verify none have drifted into isolation, confirmed by a dedicated test, a fundamentally different question from AF-151's own uniform edge store; DebugSnapshot gains a new `atlasContinuum` field; live `continuum stage Past (next Past) · generation 3 baseline 5 · threads 1 connected=true · domain overlap[Continuum,Infinity] 10/12` on the overlay, browser-verified | Aligned | 1936 tests green (9 new) | Approved 9.5/10 | **LOCKED** |
| AF-177 | The Atlas Genesis Engine | v1 *(Genesis Domains (12)/The First Moment/Commander+City+Scientific+Tradition+Institution+Cultural+Ecological+Player Origins/The Spark Network/The Founders/Beginning → Legacy (5-stage cyclic ladder)/Developer Tools — the origin layer alongside AF-176's Continuum, composing with AF-151/155/165/166/167/170/172/174's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasGenesis/` module (`atlasGenesisData.ts` + `AtlasGenesisRuntime.ts`); confirmed "Scientific Origins"'s "Original hypothesis"/"First experiment" are exactly AF-172's real `HypothesisTracker.propose`/`supportWithEvidence`, "Institution Foundations"'s "Institutional memory" is exactly AF-165's real `InstitutionalMemoryTracker.remember` ("Founders" confirmed already the first real category), "The Spark Network" is exactly AF-151's real `KnowledgeGraph.addEdge` using the already-real `"Inspired"` edge kind, "Beginning → Legacy" is driven directly by AF-155's real generic `CyclicStageTracker<TStage>` over the module's own 5-stage `GENESIS_LIFECYCLE_STAGES` union, "Commander Origins" composes AF-166's real `IdentityRegistry` directly via `Identity.lifeMilestones`, and "The Founders" composes AF-167's real `EarnedTitleTracker.earn` directly (a plain free-text title, "Founder" needs no modification) — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "Genesis Domains" ties (does not break) the codebase's 10/12 absolute overlap record against AF-174's real `HORIZON_CATEGORIES`, verified via AF-170's real `detectOverlap`; `GenesisRegistry` is confirmed genuinely new — records the seven "First Moment" fields (founder/reason/location/epoch/inspiration/doubters/believers) per entity, write-once: a second `recordOrigin` call for an already-recorded id is a no-op, confirmed by a dedicated test, the same "permanent history, never silently rewritten" guarantee AF-135's real `EvolvingEntry` established for its own objective description; DebugSnapshot gains a new `atlasGenesis` field; live `genesis origin founder commander-reyes-longlight · hypothesis grounded=false · institution memories 1 · spark neighbours 1 · lifecycle Origin · founder titles 1 · domain overlap[Genesis,Horizon] 10/12` on the overlay, browser-verified | Aligned | 1945 tests green (9 new) | Approved 9.5/10 | **LOCKED** |
| AF-178 | The Atlas Renaissance Engine | v1 *(Renaissance Domains (12)/Triggers/The Golden Age/Commander+Scientific+Architectural+Cultural+Ecological+Educational Renaissance/The Renaissance Network/The End of an Age/Legacy of Ages/Developer Tools — the historical acceleration layer alongside AF-177's Genesis Engine, composing with AF-151/159/160/163/165/168/170/171/172/175's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasRenaissance/` module (`atlasRenaissanceData.ts` + `AtlasRenaissanceRuntime.ts`); confirmed "Architectural Renaissance" is exactly AF-168's real `BeautyIndexTracker`, "Cultural Renaissance" composes AF-159's real `CulturalTrendTracker` directly, "The Renaissance Network" is exactly AF-151's real `KnowledgeGraph.addEdge` using the already-real `"Inspired"` edge kind (the same reuse AF-177's "Spark Network" made), and "The End of an Age" reuses AF-175's real `GenerationalHandoffLedger` directly for "achievements become the foundations of the next era" — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "Renaissance Domains" is confirmed a NEW ABSOLUTE overlap record (11 of 12 exact-string matches with AF-171's real `CREATIVE_DOMAINS`, surpassing the previous 10-member record), verified using AF-170's real `detectOverlap` function; "Commander/Scientific/Educational/Ecological Renaissance" and "Legacy of Ages" stay pure reference vocabulary composing AF-160's real `MentorshipLedger`/AF-172's real `HypothesisTracker`/AF-165's real `InstitutionalMemoryTracker`/AF-163's real `SignificanceTracker` at the call site; `RenaissanceTracker` is confirmed genuinely new — models "a renaissance cannot be forced... multiple triggers compound together" as a compounding-diversity gate (a golden age begins only once enough DISTINCT trigger kinds accumulate, never from repeating one trigger, confirmed by a dedicated test) and "renaissances conclude naturally, not through collapse, through maturity" as an explicit `concludeAge` call, never automatic decay, distinct from every existing quality-threshold scoring rubric in this codebase; DebugSnapshot gains a new `atlasRenaissance` field; live `renaissance golden age=true distinct triggers 3 · beauty Architecture=85 · cultural adopters 1 · network neighbours 1 · domain overlap[Renaissance,Creative] 11/12` on the overlay, browser-verified | Aligned | 1951 tests green (6 new) | Approved 9.5/10 | **LOCKED** |
| AF-179 | The Atlas Ascension Engine | v1 *(Ascension Pillars (12)/Ascension Tiers (8, ordered non-cyclic)/Commander+Scientific+Cultural+Institutional+Planetary+Player Ascension/The Ascension Index/Ascension Events/Ascension Network/The Ascension Test/Developer Tools — the civilisation maturity layer, unrelated to AF-069/070's per-run endgame "ascensionLevel" prestige counter, composing with AF-143/149/151/159/160/166/168/170/173's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasAscension/` module (`atlasAscensionData.ts` + `AtlasAscensionRuntime.ts`); NAMING SCOPE NOTE documented in-code and in the catalogue doc that "Ascension" here is unrelated to AF-069/070's endgame ascension-level prestige counter, no shared state; confirmed "Commander Ascension" composes AF-160's real `MentorshipLedger` directly, "Cultural Ascension" composes AF-168's real `BeautyIndexTracker`/AF-159's real `CulturalTrendTracker` directly, and "Ascension Network" composes AF-151's real `KnowledgeGraph.addEdge` directly (already-real `"Influenced"` edge kind) — all reused directly, all confirmed by dedicated tests; "Ascension Tiers"/"Scientific Ascension"/"Player Ascension" (all ordered, non-cyclic) mirror the codebase's established `xRank(stage): number` pattern (AF-139/156/157/162/166/170) rather than AF-155's `CyclicStageTracker`, the same reasoning AF-166's `LIFE_STAGES` already applied, confirmed by a dedicated test exercising all three rank functions; "Ascension Pillars" shares 5 of 12 exact-string members with AF-168's real `SOUL_DIMENSIONS`, documented honestly via `detectOverlap` without claiming a record; "The Ascension Index" mirrors the SHAPE of AF-143/149/170/173's real scoring rubrics (the FIFTH such rubric, same 9.5 gate), with "Military dominance is never a primary measure" enforced structurally and confirmed by a dedicated test scanning `ASCENSION_INDEX_CRITERIA` for forbidden military/power/strength keywords, finding none; "The Ascension Test" mirrors AF-170's real `finalTestPassed` all-must-pass checklist pattern exactly, confirmed by a dedicated test; DebugSnapshot gains a new `atlasAscension` field; live `ascension tier rank 7 · mentees 1 · beauty Public spaces=80 · cultural adopters 1 · network neighbours 1 · index score 9.6 (passed) · domain overlap[Ascension,Soul] 5/12` on the overlay, browser-verified | Aligned | 1959 tests green (8 new) | Approved 9.5/10 | **LOCKED** |
| AF-180 | The Atlas Transcendence Engine | v1 *(Transcendence Domains (12)/The Civilisational Shift (8, ordered non-cyclic)/Transcendent Institutions+City+Planet+Galaxy/Commander Transcendence/The Gift Principle/The Stewardship Loop (6-stage cyclic ladder)/The Universal Library/The Garden Principle/The Transcendence Index/The Quiet Victory/Developer Tools — the highest layer of the in-fiction Atlas enrichment chain only, explicitly never above the real docs/CONSTITUTION.md, composing with AF-143/149/155/160/163/167/168/170/173/179's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasTranscendence/` module (`atlasTranscendenceData.ts` + `AtlasTranscendenceRuntime.ts`); CRITICAL SCOPE NOTE documented in-code and in the catalogue doc that "highest philosophical layer" ranks only the in-fiction Atlas chain, never the real Constitution, consistent with AF-145/146/147/170/175's precedent; confirmed "The Stewardship Loop" is driven directly by AF-155's real generic `CyclicStageTracker<TStage>` over the module's own 6-stage `STEWARDSHIP_LOOP_STAGES` union ("the cycle never ends"), "The Quiet Victory" reuses AF-163's real `QuietMomentLog` directly, "Commander Transcendence" composes AF-160's real `MentorshipLedger`/AF-167's real `EarnedTitleTracker` directly, and "The Transcendent City" composes AF-168's real `BeautyIndexTracker` directly — all reused directly, all confirmed by dedicated tests; "The Civilisational Shift" (ordered, non-cyclic) mirrors the codebase's established `xRank` pattern rather than `CyclicStageTracker`, sharing exactly 3 of 8 stage names with AF-179's real `ASCENSION_TIERS`; "Transcendent Institutions" shares 4 of 5 institution names with AF-179's real `INSTITUTION_EVOLUTION_EXAMPLES` (only "Gardens" new), "The Transcendent Planet" shares 1 of 5 exact examples with AF-179's real `PLANETARY_ASCENSION_EXAMPLES`, and "Transcendence Domains" shares 8 of 12 with AF-179's real `ASCENSION_PILLARS` (no record claimed) — all confirmed via AF-170's real `detectOverlap`; "The Transcendence Index" mirrors AF-143/149/170/173/179's real scoring-rubric shape (the SIXTH such rubric, sharing 3 of 9 criteria verbatim with AF-179's real `ASCENSION_INDEX_CRITERIA`, same 9.5 gate), and "The Gift Principle" mirrors AF-170/179's real all-must-pass checklist pattern a third time; `UniversalLibrary` is confirmed genuinely new — a preservation ledger with no removal method at all, confirmed by a dedicated test that a second `preserve` call never overwrites the original category and that no `remove`/`forget` method exists, modelling "nothing worthy is intentionally lost" structurally; DebugSnapshot gains a new `atlasTranscendence` field; live `transcendence shift rank 7 · loop Discover (next Discover) · quiet moments 2 · founder titles 1 · library preserved=true (Language) · index score 9.6 (passed) · domain overlap[Transcendence,Ascension] 8/12` on the overlay, browser-verified | Aligned | 1968 tests green (9 new) | Approved 9.5/10 | **LOCKED** |
| AF-181 | The Atlas Eternity Engine | v1 *(Eternity Domains (12)/The Eternal Archive+Library+Museum/Commander Eternity/Cultural Preservation/Planetary Heritage/The Memory Constellation/The Cycle of Preservation (7-stage cyclic ladder)/The Eternal Standard (ANY-of-N gate)/Living Restoration/The Future Curators/The Eternal Promise/Developer Tools — the permanent preservation layer above AF-180's Transcendence Engine, composing with AF-135/151/155/159/163/165/170/175/179/180's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasEternity/` module (`atlasEternityData.ts` + `AtlasEternityRuntime.ts`); confirmed "The Eternal Library"/"Living Restoration" are exactly AF-135's real `PlanetaryChronicle`/`EvolvingEntry` (`entryFor(id).allVersions()` already keeps every prior version), "The Eternal Museum" composes AF-165's real `InstitutionalMemoryTracker` directly, "Cultural Preservation" composes AF-159's real `CulturalTrendTracker` directly, "Planetary Heritage" composes AF-163's real `SignificanceTracker` directly, "The Memory Constellation" is exactly AF-151's real `KnowledgeGraph.addEdge`, "The Cycle of Preservation" is driven directly by AF-155's real generic `CyclicStageTracker<TStage>` over the module's own 7-stage `PRESERVATION_CYCLE_STAGES` union ("nothing truly ends"), and "The Future Curators" reuses AF-175's real `GenerationalHandoffLedger` directly (at least the third reuse of that ledger) — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "Eternity Domains" shares 8 of 12 exact-string members with AF-176's real `CONTINUUM_DOMAINS` (no record claimed, current record 11/12), and "The Eternal Archive"'s category union shares ZERO exact-string members with AF-180's real `LIBRARY_CATEGORIES` despite both describing permanent preservation — both confirmed via AF-170's real `detectOverlap`; `EternalArchive` mirrors the SHAPE of AF-180's real `UniversalLibrary` (the SECOND no-removal preservation registry in this codebase, typed to its own union), and `eternalStandardMet` is confirmed genuinely new in shape — an ANY-of-N gate, confirmed by a dedicated test that a single "yes" answer suffices, the opposite of every existing all-must-pass checklist function in this codebase (AF-170's `finalTestPassed`, AF-179's `ascensionTestPassed`, AF-180's `giftPrincipleSatisfied`); DebugSnapshot gains a new `atlasEternity` field; live `eternity chronicle versions 2 · institution memories 2 · cultural adopters 1 · heritage significance 1 · constellation neighbours 1 · preservation cycle Discover · archive preserved=true (Historic speeches) · generation 3 baseline 6 · domain overlap[Eternity,Continuum] 8/12` on the overlay, browser-verified | Aligned | 1978 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-182 | The Atlas Harmony Engine | v1 *(Harmony Domains (12)/The Balance Model/System Relationships/Ecological+Urban+Commander+Scientific+Cultural+Economic+Social Harmony/The Harmony Index/Positive Feedback Loops/The Imbalance Detector/The Harmony Principle/Developer Tools — the equilibrium layer above AF-181's Eternity Engine, composing with AF-143/149/151/159/160/166/168/170/171/173/179/180's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasHarmony/` module (`atlasHarmonyData.ts` + `AtlasHarmonyRuntime.ts`); confirmed "System Relationships"/"Positive Feedback Loops" both compose AF-151's real `KnowledgeGraph.addEdge` directly (already-real `"Influenced"` edge kind), "Cultural Harmony" composes AF-159's real `CulturalTrendTracker` directly, "Commander Harmony"/"Social Harmony" both compose AF-160's real `MentorshipLedger` directly, and "Urban Harmony"'s "Beauty" composes AF-168's real `BeautyIndexTracker` directly — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "Harmony Domains" shares 8 of 12 exact-string members with AF-171's real `CREATIVE_DOMAINS` (no record claimed, current record 11/12), verified via AF-170's real `detectOverlap`; "The Harmony Index" mirrors AF-143/149/170/173/179/180's real scoring-rubric shape (the SEVENTH such rubric, sharing 3 of 8 criteria verbatim with AF-180's real `TRANSCENDENCE_INDEX_CRITERIA`, same 9.5 gate); "The Balance Model"/"The Imbalance Detector" together describe the module's own genuinely new `HarmonyTracker` — "balance changes gradually" mirrors AF-166's real `ValuePriorityTracker`'s capped-delta-per-update constraint at civilisation scale, confirmed by a dedicated test that a single `adjustToward` call moves a domain's level by at most 5, and "no single domain should permanently dominate another" is enforced structurally via emergent `mostDominantDomain`/`mostNeglectedDomain`/`isBalanced` reads across all tracked domains rather than a fixed-criterion quality gate, confirmed by a dedicated test that pushing one domain to its ceiling breaks `isBalanced` and raising the rest restores it; DebugSnapshot gains a new `atlasHarmony` field; live `harmony dominant Science · neglected Science · balanced=true · network neighbours 1 · cultural adopters 1 · beauty Public spaces=75 · index score 9.6 (passed) · domain overlap[Harmony,Creative] 8/12` on the overlay, browser-verified | Aligned | 1985 tests green (7 new) | Approved 9.5/10 | **LOCKED** |
| AF-183 | The Atlas Symphony Engine | v1 *(Symphony Domains (12)/The Orchestra Model/Commander Ensembles/Institutional+Ecological+Cultural Symphony/Civilisation Rhythm (7-stage cyclic ladder)/Thematic Consistency (ANY-of-N gate)/Player Participation/The Resonance Model/The Silence Principle/The Grand Performance/Developer Tools — the orchestration layer above AF-182's Harmony Engine, almost entirely direct reuse, composing with AF-151/155/159/163/168/170/175/181/182's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasSymphony/` module (`atlasSymphonyData.ts` + `AtlasSymphonyRuntime.ts`); confirmed "Institutional Symphony"/"Cultural Symphony"/"The Resonance Model" all compose AF-151's real `KnowledgeGraph.addEdge` directly (already-real `"Inspired"` edge kind, the fourth-plus reuse of this pattern), "Civilisation Rhythm" is driven directly by AF-155's real generic `CyclicStageTracker<TStage>` over the module's own 7-stage `CIVILISATION_RHYTHM_STAGES` union, "The Silence Principle" reuses AF-163's real `QuietMomentLog` directly (the same class AF-180's "Quiet Victory" reused), and "The Resonance Model"'s "echoes across generations" also composes AF-175's real `GenerationalHandoffLedger` directly — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "Symphony Domains" shares 9 of 12 exact-string members with AF-182's real `HARMONY_DOMAINS` (no record claimed, current record 11/12) and "Thematic Consistency" shares 5 of 8 with AF-168's real `SOUL_DIMENSIONS`, both confirmed via AF-170's real `detectOverlap`; "The Orchestra Model"/"Commander Ensembles" stay pure reference-lookup tuple data, the same shape AF-159/179/180's real paired lists already established; `thematicConsistencyMet` is confirmed genuinely new in shape — the SECOND ANY-of-N gate in this codebase after AF-181's real `eternalStandardMet`, confirmed by a dedicated test that a single matching theme suffices; `CampaignJourneyTracker` is confirmed genuinely new — counts contributions per journey and only reports `isUnifiedStory` once every journey has been touched at least once, confirmed by a dedicated test, a fundamentally different guarantee from every scoring rubric in this codebase; DebugSnapshot gains a new `atlasSymphony` field; live `symphony resonance neighbours 1 · rhythm Exploration (next Exploration) · quiet moments 3 · journey dominant A scientific journey unified=true · theme met=true · generation 4 baseline 6 · domain overlap[Symphony,Harmony] 9/12` on the overlay, browser-verified | Aligned | 1993 tests green (8 new) | Approved 9.5/10 | **LOCKED** |
| AF-184 | The Atlas Unity Engine | v1 *(Unity Domains (12)/The Unity Network/Commander+Planetary+Institutional Unity/The Knowledge Commons/Unity Through Diversity/The Civilisation Web/Shared Achievements/Unity Events/The Unity Index/The Unity Test/Developer Tools — the universal integration layer above AF-183's Symphony Engine, composing with AF-143/149/151/159/170/173/176/177/179/180/182's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasUnity/` module (`atlasUnityData.ts` + `AtlasUnityRuntime.ts`); confirmed "The Unity Network"/"The Civilisation Web"/"The Knowledge Commons" all compose AF-151's real `KnowledgeGraph.addEdge` directly, "Unity Through Diversity" composes AF-159's real `CulturalTrendTracker` directly, and "Shared Achievements" reuses AF-176's real `ThreadRegistry`/`allThreadsConnected` directly (a shared achievement is exactly a permanently significant thread) — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "Unity Domains" shares 9 of 12 exact-string members with AF-183's real `SYMPHONY_DOMAINS` (no record claimed, current record 11/12) and "Institutional Unity" shares 5 of 8 institution types with AF-177's real `INSTITUTION_TYPES`, both confirmed via AF-170's real `detectOverlap`; "The Unity Index" mirrors AF-143/149/170/173/179/180/182's real scoring-rubric shape (the EIGHTH such rubric, sharing 2 of 8 criteria verbatim with AF-179's real `ASCENSION_INDEX_CRITERIA`, same 9.5 gate); "Commander Unity"/"Planetary Unity"/"Unity Events" stay pure reference-lookup data mirroring AF-183's real `ORCHESTRA_MODEL_ROLES` paired-tuple shape, and "Player Unity"/"The Unity Test" are documented as prose-only philosophical statements; `UnityIndexScoreCard` is the module's sole genuinely new contribution, consistent with AF-183's own light-touch precedent for an orchestration-layer module; DebugSnapshot gains a new `atlasUnity` field; live `unity network neighbours 2 · cultural adopters 1 · shared achievements connected=true · index score 9.6 (passed) · domain overlap[Unity,Symphony] 9/12` on the overlay, browser-verified | Aligned | 1999 tests green (6 new) | Approved 9.5/10 | **LOCKED** |
| AF-185 | The Atlas Living Universe Engine | v1 *(The Living Domains (12)/Living People+Communities+Cities+Planets+Knowledge+History+Culture+Science+Ecology+Relationships+Civilisation/Living Feedback/The Living Present/The Living Future/Developer Tools — the permanent heartbeat above every prior Atlas system, unrelated to AF-132's locked "Living Galaxy," composing with AF-135/151/159/160/166/169/172/174/175's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasLivingUniverse/` module (`atlasLivingUniverseData.ts` + `AtlasLivingUniverseRuntime.ts`); NAMING NOTE documented in-code and in the catalogue doc that this module is unrelated to AF-132's locked "Living Galaxy" or the `livingMuseum`/`livingShip` modules, no shared state; confirmed "Living People" composes AF-166's real `IdentityRegistry` directly, "Living Communities"/"Living Culture" compose AF-159's real `CulturalTrendTracker` directly, "Living Cities"/"Living Planets"/"Living Knowledge"/"Living History" compose AF-135's real `PlanetaryChronicle`/`EvolvingEntry` directly ("the skyline tells history" is exactly `entryFor(id).allVersions()`), "Living Knowledge" also composes AF-172's real `HypothesisTracker`, "Living Science"/"The Living Future" compose AF-159's real `MysteryLog`/AF-169's real `ensureNextHorizonOpen` directly (at least the fifth-plus reuse of this guarantee), "Living Relationships" composes AF-160's real `MentorshipLedger` directly, "Living Civilisation" reuses AF-175's real `GenerationalHandoffLedger` directly, "Living Feedback" composes AF-151's real `KnowledgeGraph.addEdge` directly, and "The Living Future" also composes AF-174's real `HorizonEffectTracker` directly — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "The Living Domains" shares only 3 of 12 exact-string members with AF-176's real `CONTINUUM_DOMAINS`, the lowest overlap deliberately documented so far via AF-170's real `detectOverlap`, since this list uses concrete nouns rather than abstract domain vocabulary; `LivingPresentTracker` is confirmed genuinely new — the ONLY overwriting tracker in this codebase, confirmed by a dedicated test that a second `update` call replaces rather than accumulates, the deliberate opposite of every append-only/write-once permanence class built so far (`EvolvingEntry`, `GenesisRegistry`, `UniversalLibrary`, `EternalArchive`); DebugSnapshot gains a new `atlasLivingUniverse` field; live `living present "Restoring a wounded ecosystem on Verdance." · chronicle versions 3 · hypothesis grounded=false · mysteries unsolved 5 · unknown index 3 · domain overlap[Living,Continuum] 3/12` on the overlay, browser-verified | Aligned | 2007 tests green (8 new) | Approved 9.5/10 | **LOCKED** |
| AF-186 | The Atlas Evolution Engine | v1 *(Evolution Domains (12)/Personal+Commander+Species+City+Institutional+Scientific+Technological+Cultural+Educational+Civilisational Evolution/The Evolution Chain (8-stage cyclic ladder)/Reversibility/The Evolution Record/Developer Tools — a naming collision with AF-139's own locked "Evolution Engine," resolved by living under its own `atlasEvolution/` directory and composing AF-139's real classes directly wherever they already cover the same ground, plus AF-151/155/159/160/165/166/172/175's real classes)* | Complete — new `src/game/atlasEvolution/` module (`atlasEvolutionData.ts` + `AtlasEvolutionRuntime.ts`); NAMING SCOPE NOTE documented in-code and in the catalogue doc distinguishing this module from AF-139's locked "Evolution Engine"; confirmed "Species Evolution" reuses AF-139's real `SpeciesAdaptationRegistry` directly, "City Evolution" reuses AF-139's real `HistoricalArchitectureLedger` directly, "Cultural Evolution"'s "Language" reuses AF-139's real `LanguageEvolutionLog` directly (broader section also composes AF-159's real `CulturalTrendTracker`), "Commander Evolution" reuses AF-139's real `commanderMaturityScore`/`commanderMaturityStageFor` directly, "Technological Evolution" reuses AF-139's real `technologyEraFor`/`TECHNOLOGY_ERAS` directly, "Personal Evolution" composes AF-166's real `IdentityRegistry` directly, "Institutional Evolution" composes AF-165's real `InstitutionalMemoryTracker` directly, "Scientific Evolution" composes AF-172's real `HypothesisTracker` directly, "Educational Evolution" composes AF-160's real `MentorshipLedger` directly, and "Civilisational Evolution" reuses AF-175's real `GenerationalHandoffLedger` directly — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "Evolution Domains" shares ZERO exact-string members with AF-139's real `EVOLUTION_PILLARS` despite obvious conceptual overlap (adjective vs noun forms), and its own `SPECIES_EVOLUTION_EXAMPLES` shares 3 of 7 with AF-139's real `SPECIES_ADAPTATION_TRIGGER_KINDS`, both confirmed via AF-170's real `detectOverlap`; "The Evolution Chain" is driven directly by AF-155's real generic `CyclicStageTracker<TStage>` over the module's own 8-stage `EVOLUTION_CHAIN_STAGES` union, confirmed genuinely absent from AF-139's own grow-only trackers; `EvolutionRecord` is confirmed genuinely new — the FIRST tracker in this codebase whose current state can legitimately regress to an earlier value (Adopted → Abandoned → Restored) while remaining a fully traceable append-only history, confirmed by a dedicated test, distinct from AF-177's real write-once `GenesisRegistry`; DebugSnapshot gains a new `atlasEvolution` field, rendered with the label `atlasEvo` (not `evolution`) to avoid visual collision with AF-139's own pre-existing `evolution` debug line; live `atlasEvo species adaptations 1 · architecture layers 1 · phrase "May your dawn find the wild kind." · chain Observation (next Observation) · practice state Adopted · domain overlap[Evolution,Pillars] 0/12` on the overlay, browser-verified | Aligned | 2016 tests green (9 new) | Approved 9.5/10 | **LOCKED** |
| AF-187 | The Atlas Emergence Engine | v1 *(Emergence Domains (12)/Personal+Commander+Community+Scientific+Cultural+Ecological+Civilisational Emergence/Positive Cascades/The Butterfly Network/Emergence Validation (5th all-must-pass gate)/Developer Tools — the emergent complexity layer above AF-186's Evolution Engine, composing with AF-139/151/159/160/167/170/178's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasEmergence/` module (`atlasEmergenceData.ts` + `AtlasEmergenceRuntime.ts`); confirmed "Commander Emergence" is exactly AF-167's real `ReputationTracker` ("reveals, never assigns" a commander's most-recognised quality), "Scientific Emergence" is exactly AF-151's real `KnowledgeGraph.suggestConnections`, "Cultural Emergence"/"Positive Cascades"' chain links both compose AF-151's real `KnowledgeGraph.addEdge` directly (Positive Cascades' culmination reuses AF-178's real `RenaissanceTracker.recordTrigger`), "Ecological Emergence" reuses AF-139's real `SpeciesAdaptationRegistry` directly (the same reuse AF-186's own "Species Evolution" made), "Community/Civilisational Emergence" both compose AF-159's real `CulturalTrendTracker` directly, and "Personal Emergence" composes AF-160's real `MentorshipLedger` directly — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "Emergence Domains" shares 8 of 12 exact-string members with AF-186's real `EVOLUTION_DOMAINS` (no record claimed, current record 11/12), verified via AF-170's real `detectOverlap`; `CascadeTracker` is confirmed genuinely new — classifies effects into four ordered causal-distance tiers per origin action for "The Butterfly Network," confirmed by a dedicated test that `allTiersReached` only becomes true once every tier has been recorded; `emergenceValidationMet` mirrors AF-170/179/180's real all-must-pass checklist pattern a fifth time; DebugSnapshot gains a new `atlasEmergence` field; live `emergence reputation Mentorship · network neighbours 1 · golden age=true · cascade tiers reached=false · validation met=true · domain overlap[Emergence,Evolution] 8/12` on the overlay, browser-verified | Aligned | 2024 tests green (8 new) | Approved 9.5/10 | **LOCKED** |
| AF-188 | The Atlas Possibility Realisation Engine | v1 *(Realisation Stages (12, ordered non-cyclic)/Realisation Domains (12)/Scientific+Engineering+Commander+Institutional+Cultural+Player Realisation/The Implementation Network/Feedback Loop/Quality Gates (9th scoring rubric)/The Ripple Effect/The Implementation Archive/Developer Tools — the transformation layer above AF-187's Emergence Engine, a two-way naming collision with AF-159's locked "Atlas Possibility Engine" and AF-173's locked "Atlas Possibility Space" resolved via a third distinct directory name, composing with AF-151/159/160/162/169/170/172/173/177/178's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasRealisation/` module (`atlasRealisationData.ts` + `AtlasRealisationRuntime.ts`); NAMING SCOPE NOTE documented in-code and in the catalogue doc distinguishing this module from BOTH AF-159's `atlasPossibility/` and AF-173's `atlasPossibilitySpace/`, resolved by dropping "Possibility" from the directory name entirely; confirmed "Scientific Realisation" composes AF-172's real `HypothesisTracker` directly, "Commander Realisation" composes AF-160's real `MentorshipLedger` directly, "Institutional Realisation" reuses AF-177's real `GenesisRegistry` directly, "Cultural Realisation" composes AF-159's real `CulturalTrendTracker` directly, "Player Realisation" reuses AF-162's real `LongTermMissionTracker` directly, "The Implementation Network"/"The Ripple Effect" both compose AF-151's real `KnowledgeGraph.addEdge` directly, and "Feedback Loop" composes AF-159's real `MysteryLog.open` with AF-169's real `ensureNextHorizonOpen` directly — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "Realisation Domains" shares 9 of 12 exact-string members with AF-178's real `RENAISSANCE_DOMAINS` (no record claimed, current record 11/12), and "Quality Gates" shares 3 of 7 criteria verbatim with AF-173's real `INNOVATION_FILTER_CRITERIA`, both confirmed via AF-170's real `detectOverlap`; `QualityGateScoreCard` mirrors AF-143/149/170/173/179/180/182/184's real scoring-rubric shape (the NINTH such rubric, same 9.5 gate); `RealisationTracker` is confirmed genuinely new — the FIRST tracker in this codebase that structurally rejects out-of-order or skipped-ahead advancement, confirmed by a dedicated test that both a skip-ahead attempt and a regression attempt are rejected while sequential single-step advancement succeeds; DebugSnapshot gains a new `atlasRealisation` field, its rendered label checked for collisions against every existing debug line before finalising (per the lesson learned during AF-186); live `realisation stage Question reached-wonder=true · hypothesis grounded=true · mentees 2 · genesis founder commander-fen-beastmaster · gate score 9.6 (passed) · domain overlap[Realisation,Renaissance] 9/12` on the overlay, browser-verified | Aligned | 2031 tests green (7 new) | Approved 9.5/10 | **LOCKED** |
| AF-189 | The Atlas Civilisation Operating System | v1 *(Core Services (12)/The Civilisation Bus/State Management/Resource Coordination/Task Orchestration/Priority Management/Continuous Diagnostics/Self-Optimisation/Failsafe Services (2nd strict-priority resolver)/Service Dependencies/Civilisation Telemetry/Adaptive Coordination (4-tier ordered ladder)/The Civilisation Heartbeat/Developer Tools — the THIRD "operating system"-shaped module after AF-144's foundation-layer AOS and AF-154's player-experience Orchestrator, coordinating the ~40 Atlas-XXX modules themselves, composing with AF-001/144/151/154/162's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasCivilisationOS/` module (`atlasCivilisationOSData.ts` + `AtlasCivilisationOSRuntime.ts`); NAMING SCOPE NOTE documented in-code and in the catalogue doc distinguishing this module from BOTH AF-144's `aos/` (foundation layer) and AF-154's `atlasOrchestrator/` (player-experience layer); confirmed "The Civilisation Bus" is exactly AF-001's real generic `EventBus<E>` (the same class AF-144's own "System Bus" already reused), instantiated with its own new `CivilisationBusEventMap`; "State Management" reuses AF-144's real generic `WorldStateStore<T>` directly, instantiated over the real, already-locked `CivilisationAttribute` union from `civilisationEngine/` rather than a new duplicate attribute list; "Priority Management"/"Civilisation Telemetry" reuse AF-144's real `PriorityEngine`/`TelemetryCollector` directly; "Task Orchestration" reuses AF-162's real `LongTermMissionTracker` directly; "Service Dependencies" composes AF-151's real `KnowledgeGraph.addEdge` directly — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "Failsafe Services" mirrors the SHAPE of AF-154's real `resolveByFailsafePriority` a second time, typed to its own `CivilisationFailsafeConcern` union; `CivilisationHealthTracker` mirrors the SHAPE of AF-144's real `PerformanceBudgetTracker` but INVERTS its direction (a low score, not a high one, marks a domain weak), confirmed by a dedicated test; `ResourcePoolCoordinator` is confirmed genuinely new — a named resource pool with allocate/release/availableFor semantics, confirmed by a dedicated test that over-allocation is rejected; `adaptiveCoordinationTierRank` mirrors the established `xRank` pattern over a new ordered, non-cyclic 4-tier ladder; `CivilisationHeartbeat` is confirmed genuinely new — an append-only per-cycle log answering five fixed questions every tick, explicitly distinct from AF-185's real OVERWRITING `LivingPresentTracker`, confirmed by a dedicated test that two ticks both remain in history; DebugSnapshot gains a new `atlasCivilisationOS` field, rendered with the label `civOS` (checked for collisions against every existing debug line before finalising); live `civOS state population 500 · priority emergency-recovery=High · telemetry events 1 · weakest health Community wellbeing · researchers available 4 · network neighbours 1 · failsafe Knowledge loss · coordination tier rank 0 · heartbeat "A new university opened."` on the overlay, browser-verified | Aligned | 2042 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-190 | The Atlas Meta Evolution Engine | v1 *(Meta Evolution Domains (12)/The Update Life Cycle (12-stage, 2nd FeatureLifecycleTracker-shaped tracker)/Design Evolution/Technical Evolution/Content Evolution/Player Evolution/Community Evolution/The Atlas Regression Detector + Quality Evolution (2nd featureFlagAssessment-shaped gate)/Expansion Governance/The Atlas Scorecard (10th scoring rubric)/Continuous Documentation/The Ten-Year Test/Developer Tools — the REAL-WORLD-development-process layer, a naming collision with BOTH AF-139's and AF-186's own locked "Evolution Engine" modules resolved by living under its own `atlasMetaEvolution/` directory, composing with AF-135/139/144/149/159/170's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasMetaEvolution/` module (`atlasMetaEvolutionData.ts` + `AtlasMetaEvolutionRuntime.ts`); NAMING SCOPE NOTE documented in-code and in the catalogue doc distinguishing this module from BOTH AF-139's and AF-186's locked "Evolution Engine" modules (this is the only one of the three describing the real development process, not the simulated universe) and from `src/game/meta/`'s unrelated "Meta Progression"; confirmed "Update Life Cycle" mirrors the SHAPE of AF-149's real `FeatureLifecycleTracker` exactly (the SECOND instance, register/advance-with-no-target/stageFor/historyFor, typed to its own new 12-stage `UpdateLifecycleStage` union), the Update Life Cycle's own "Iteration" stage reuses AF-149's real `IterationCycleTracker` directly (the same instance, not a second one), "Player Evolution" reuses AF-144's real `TelemetryCollector` directly (the THIRD instantiation), "Community Evolution" composes AF-159's real `CulturalTrendTracker` directly, and "Expansion Governance" reuses AF-149's real `systemImpactReportFor` directly — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "The Atlas Regression Detector" combined with "Quality Evolution" mirrors the SHAPE of AF-149's real `featureFlagAssessment` a second time via the new `updateQualityAssessment` (any regression signal rejects regardless of quality gains), typed to its own `RegressionSignal`/`QualityEvolutionCriterion` unions, confirmed by a dedicated test; "Design Evolution" is confirmed genuinely new in domain (no per-mechanic historical ledger of intent/implementation/reception/complexity/opportunity/risk exists anywhere) via the new append-only `DesignHistoryLedger`, mirroring AF-135/139's established evolving-history shape; "Technical Evolution" is confirmed genuinely new via the new append-only `TechnicalDebtLog` (no technical-debt tracker exists anywhere else, as AF-144's own module doc comment already confirmed); "The Atlas Scorecard" mirrors AF-143/149/170/173/179/180/182/184/188's real scoring-rubric shape exactly — the TENTH such rubric, typed to its own new `AtlasScorecardCategory` union, same 9.5 gate; "Meta Evolution Domains" shares 6 of 12 exact-string members with AF-149's real `SYSTEM_IMPACT_CATEGORIES` (no record claimed, current record 11/12), verified via AF-170's real `detectOverlap`; "The Ten-Year Test" restates the same underlying question as AF-149's real `EXPANSION_QUESTIONS[0]` in different wording, documented honestly rather than claimed identical; DebugSnapshot gains a new `atlasMetaEvolution` field, rendered with the label `metaEvo` (checked against every existing debug line, including the pre-existing unrelated `meta` label, before finalising); live `metaEvo lifecycle Prototype · ready to ship=true · design complexity 3 · technical debt logged 1 · player signals 1 · community adopters 1 · quality reject=false improvements 2 · scorecard 9.6 (passed) · domain overlap[Meta,SystemImpact] 6/12` on the overlay, browser-verified | Aligned | 2053 tests green (11 new) | Approved 9.5/10 | **LOCKED** |
| AF-191 | The Atlas Creator Engine | v1 *(Creation Domains (12, an EXACT 12/12 match with the real CREATIVE_DOMAINS — a new absolute overlap record)/The Creation Cycle (10-stage cyclic)/Creative Inspiration/Commander+Scientific+Engineering+Artistic+Architectural+Educational Creation/Player Creation/Collaborative Creation/The Creator Network/Beauty Through Purpose/The Creation Archive/Developer Tools — a naming collision with BOTH AF-141's locked "Galactic Creator Engine" and AF-171's locked "Atlas Creative Intelligence" resolved by living under its own `atlasCreator/` directory, composing with AF-151/155/168/171's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasCreator/` module (`atlasCreatorData.ts` + `AtlasCreatorRuntime.ts`); NAMING SCOPE NOTE documented in-code and in the catalogue doc distinguishing this module from BOTH AF-141's (player decoration tooling) and AF-171's (near-identical creative-process) locked modules; confirmed "Creation Domains" is, as a SET, an EXACT 12/12 match with AF-171's real `CREATIVE_DOMAINS` (only reordered), a NEW ABSOLUTE OVERLAP RECORD surpassing the previous 11/12 (AF-178 vs AF-171), verified via AF-170's real `detectOverlap` — reused directly rather than declaring a second list; confirmed every "X Creation" section (Commander/Scientific/Engineering/Educational/Artistic/Architectural) reuses AF-171's real `CreativeContributionLog` directly (now exercising two domains — Art, Architecture — AF-171 itself never exercised), "The Creation Cycle" reuses AF-155's real generic `CyclicStageTracker<TStage>` directly over a new 10-stage union, "Collaborative Creation" reuses AF-155's real `CollaborativeProblemLog` directly (at least the EIGHTH instance), "The Creator Network" composes AF-151's real `KnowledgeGraph.addEdge` directly, "Beauty Through Purpose" reuses AF-168's real `BeautyIndexTracker` directly, and "The Creation Archive" reuses AF-171's real `CreativeHeritageArchive` directly — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "Collaborative Creation" participants shares 6 of 7 exact members with AF-171's real `COLLABORATIVE_CREATION_PARTICIPANTS`, "Creative Inspiration" shares 3 of 8 with AF-171's real `CREATIVE_SOURCES`, and "The Creator Network" fields share 2 of 7 with AF-188's real `IMPLEMENTATION_ARCHIVE_FIELDS` — all documented honestly via `detectOverlap`; `purposefulBeautyMet` is confirmed genuinely new — another all-must-pass checklist function, distinct from AF-168's continuous settlement-scale `BeautyIndexTracker`, confirmed by a dedicated test; DebugSnapshot gains a new `atlasCreator` field, rendered with the label `creatorEng` (checked against the pre-existing `creator`/`creative` labels before finalising); live `creatorEng cycle Inspiration (next Inspiration) · collaborators 2 · contributions Engineering=2 Art=1 · network neighbours 1 · beauty Public spaces=75 · archive outcomes 2 · purposeful beauty met=true` on the overlay, browser-verified | Aligned | 2063 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-192 | The Atlas Craftsmanship Engine | v1 *(Craftsmanship Domains (12)/The Craft Cycle (10-stage ordered non-cyclic ladder)/Master Craftsmen/Scientific+Engineering+Architectural+Artistic+Educational+Leadership Craftsmanship/Player Craftsmanship/Quality Without Perfection/The Maker's Mark/The Craft Guilds/The Standard of Excellence (ANY-of-N, ongoing-investment direction)/Developer Tools — AF-191's direct sibling, composing with AF-149/151/160/166/171's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasCraftsmanship/` module (`atlasCraftsmanshipData.ts` + `AtlasCraftsmanshipRuntime.ts`); light-touch NAMING NOTE documented in-code and in the catalogue doc distinguishing this module from the unrelated `src/game/crafting/` (item recipes) and `src/game/masterIndex/` (module dependency registry); confirmed "Master Craftsmen" is exactly AF-166's real `ReputationTracker` (the same "reveals, never assigns" mechanic AF-187's Commander Emergence already reused), "Quality Without Perfection" is exactly AF-149's real `IterationCycleTracker`, "The Maker's Mark" composes AF-151's real `KnowledgeGraph.addEdge` directly, and "The Craft Guilds" reuses AF-160's real `MentorshipLedger` directly — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "The Craft Cycle" is confirmed an ORDERED, NON-CYCLIC ladder (unlike AF-191's own explicitly-closed "Creation Cycle") via the new `craftCycleRank`, mirroring the established `xRank` pattern rather than AF-155's real `CyclicStageTracker`, sharing only 3 of 10 stages exactly with AF-191's real `CREATION_CYCLE_STAGES` despite being sibling 10-stage process ladders authored back to back, verified via AF-170's real `detectOverlap`; "Craftsmanship Domains" shares 9 of 12 exact-string members with AF-191's real `CREATIVE_DOMAINS` (no record claimed, current record remains AF-191's own 12/12 against the same list); "The Standard of Excellence" is confirmed genuinely new in DIRECTION via the new `standardOfExcellenceAssessment` — an ANY-of-N shape gating ONGOING INVESTMENT rather than rejection or completion, distinct from AF-149/181/183/190's real ANY-of-N gates, confirmed by a dedicated test; the six domain-specific craftsmanship-values sections plus Player Craftsmanship are kept as pure reference vocabulary, the same honest scope boundary AF-143/149/171/191 already established; DebugSnapshot gains a new `atlasCraftsmanship` field, rendered with the label `craftsman` (checked against the pre-existing unrelated `masterIdx` label before finalising); live `craftsman  master craftsman Precision · ready to ship=true · maker's mark neighbours 2 · guild mentees 1 · craft cycle rank 6 · continue refining=true · domain overlap[Craft,Creative] 9/12 · cycle overlap[Craft,Creation] 3/10` on the overlay, browser-verified | Aligned | 2070 tests green (7 new) | Approved 9.5/10 | **LOCKED** |
| AF-193 | The Atlas Excellence Engine | v1 *(Excellence Domains (12)/The Excellence Cycle (8-stage explicit closed loop)/Personal+Commander+Scientific+Engineering+Educational+Community+Cultural+Institutional Excellence/Player Excellence/The Excellence Standard (2nd ongoing-investment ANY-of-N gate)/The Improvement Network/The Excellence Index (11th scoring rubric)/Developer Tools — the third module in the Creator/Craftsmanship/Excellence trilogy, composing with AF-149/155/159/160/166's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasExcellence/` module (`atlasExcellenceData.ts` + `AtlasExcellenceRuntime.ts`); confirmed "The Excellence Cycle" reuses AF-155's real generic `CyclicStageTracker<TStage>` directly (an explicit closed loop, unlike AF-192's own confirmed-non-cyclic "Craft Cycle"), "Personal Excellence"'s Mentorship channel reuses AF-160's real `MentorshipLedger` directly, "Commander Excellence" reuses AF-166's real `ReputationTracker` directly (the same "reveals, never assigns" mechanic AF-187/AF-192 already reused), "Cultural Excellence" composes AF-159's real `CulturalTrendTracker` directly, and "Institutional Excellence" reuses AF-149's real `IterationCycleTracker` directly (the same mechanic AF-192's Quality Without Perfection already reused) — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "The Excellence Standard" is the SECOND instance of AF-192's own `standardOfExcellenceAssessment` shape via the new `excellenceStandardAssessment`, typed to its own union, sharing ZERO exact-string questions with AF-192's real `STANDARD_OF_EXCELLENCE_QUESTIONS` despite near-identical framing; "The Excellence Index" mirrors AF-143/149/170/173/179/180/182/184/188/190's real scoring-rubric shape exactly — the ELEVENTH such rubric, sharing only 1 of 8 with AF-190's real `ATLAS_SCORECARD_CATEGORIES`; "The Improvement Network" is confirmed genuinely new in domain via the new append-only `ImprovementNetworkLedger` (no per-improvement structured record of this field shape exists anywhere else); "Excellence Domains" shares 9 of 12 with AF-191's real `CREATIVE_DOMAINS` and 8 of 12 with AF-192's real `CRAFTSMANSHIP_DOMAINS` (no record claimed, current record remains AF-191's own 12/12); "The Excellence Cycle" shares ZERO exact-string stages with either AF-191's or AF-192's own sibling process ladder despite being the THIRD such ladder authored back to back — all overlaps confirmed via AF-170's real `detectOverlap`; the domain-specific excellence-values sections plus Player/Institutional Excellence examples are kept as pure reference vocabulary; DebugSnapshot gains a new `atlasExcellence` field, rendered with the label `excellence` (checked against every existing debug line before finalising, confirmed unique); live `excellence cycle Learn (next Learn) · mentees 1 · reputation Strategic thinking · cultural adopters 1 · institution ready=true · improvements logged 1 · continue refining=true · index score 9.6 (passed) · domain overlap[Excellence,Creative] 9/12` on the overlay, browser-verified | Aligned | 2080 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-194 | The Atlas Possibility Engine (AF-194) | v1 *(⚠ CRITICAL: verbatim title duplicate of AF-159's own real, already-locked "Atlas Possibility Engine" — the first full exact-title collision this session, resolved via a new `atlasOpenPossibility/` directory and disambiguated as "(AF-194)" throughout — Possibility Domains (12)/The Possibility Cycle (8-stage ordered non-cyclic ladder)/Scientific+Engineering+Commander Possibility/Player Possibility/Civilisational Possibility/Possibility Through Cooperation/The Possibility Web/Limitations/The Unknown Reserve/The Possibility Index (12th scoring rubric)/The Open Door Principle/Developer Tools — composing with AF-151/155/159/169's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasOpenPossibility/` module (`atlasOpenPossibilityData.ts` + `AtlasOpenPossibilityRuntime.ts`); ⚠ CRITICAL NAMING COLLISION documented prominently in-code, in the catalogue doc, and flagged to the Project Owner in the closing summary — the first VERBATIM full-title duplicate this session (every prior collision shared only a word inside a differently-worded title); confirmed "The Possibility Web" is exactly AF-159's real `PossibilityRegistry`/`Possibility` interface (already carrying requiredKnowledge/requiredPeople/requiredLocations/potentialRisks/potentialRewards/historicalSignificance/futureImplications), "The Unknown Reserve" reuses AF-159's real `MysteryLog` directly, "Player Possibility" reuses AF-159's real `PlayerInspirationLog` directly, "The Open Door Principle" is exactly AF-169's real `ensureNextHorizonOpen` directly, "Possibility Through Cooperation" reuses AF-155's real `CollaborativeProblemLog` directly, and "Civilisational Possibility" composes AF-151's real `KnowledgeGraph.suggestConnections` directly (the same convergence mechanic AF-159's own Serendipity already reused) — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "The Possibility Cycle" is confirmed an ORDERED, NON-CYCLIC ladder (its final stage, "New Possibilities," never repeats its first by name) via the new `possibilityCycleRank`, sharing 2 of 8 stages with AF-191's real `CREATION_CYCLE_STAGES` and zero with either AF-192's or AF-193's own sibling cycle — the fourth sibling process ladder authored in a row, still fragmented membership; "The Possibility Index" mirrors AF-143/149/170/173/179/180/182/184/188/190/193's real scoring-rubric shape exactly — the TWELFTH such rubric, sharing zero exact categories with AF-193's real `EXCELLENCE_INDEX_CATEGORIES`; "Possibility Domains" shares 11 of 12 with AF-193's real `EXCELLENCE_DOMAINS` (ties but does not break AF-191's own 12/12 record) and only 2 of 12 with AF-159's own real `DISCOVERY_CATEGORIES` — all overlaps confirmed via AF-170's real `detectOverlap`; DebugSnapshot gains a new `atlasOpenPossibility` field, rendered with the label `openDoor` (checked against the pre-existing `possible`/`possibility` labels before finalising); live `openDoor web required people 1 · mysteries unsolved 7 · inspiration surfaced 1 · collaborators 2 · network neighbours 1 · cycle rank 5 · index score 9.6 (passed) · domain overlap[Possibility,Excellence] 11/12` on the overlay, browser-verified | Aligned | 2090 tests green (10 new) | Approved 9.5/10 | **LOCKED** |
| AF-195 | The Atlas Coherence Engine | v1 *(Coherence Domains (12)/The Context Chain/Historical+Scientific+Character+Institutional+Planetary+Emergent Coherence/Player Coherence/The Canon Graph/The Contradiction Detector/The Coherence Standard/Developer Tools — overlaps almost entirely with AF-148's own already-locked "Atlas Canon Engine" (a different title, same territory), composing with AF-135/148/151/165's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasCoherence/` module (`atlasCoherenceData.ts` + `AtlasCoherenceRuntime.ts`); confirmed "The Context Chain" is exactly AF-148's real `CanonEventLedger`/`CanonEventRecord` directly, "Historical/Scientific/Emergent Coherence" all reuse AF-148's real `loreValidationReport` directly, "The Contradiction Detector" reuses AF-148's real `KnowledgeStateTracker.hasDiverged` directly, "Character Coherence" reuses AF-148's real `CommanderContinuityLedger` directly, "Institutional Coherence" reuses AF-165's real `InstitutionalMemoryTracker` directly, "Planetary Coherence" reuses AF-148's real `recordPlanetContinuityFact` directly (composing AF-135's real `PlanetaryChronicle`), and "The Canon Graph" composes AF-151's real `KnowledgeGraph.addEdge` directly — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "The Coherence Standard" is confirmed another instance of the established all-must-pass checklist-function family via the new `coherenceStandardMet`, typed to its own union; "Coherence Domains" shares 7 of 12 with AF-194's real `POSSIBILITY_DOMAINS` and 6 of 12 with AF-193's real `EXCELLENCE_DOMAINS` (no record claimed, current record remains AF-191's own 12/12), verified via AF-170's real `detectOverlap`; "Player Coherence" is kept as pure reference — restates rather than adds to a property many real systems already guarantee; the remaining domain-specific coherence-aspect sections are kept as pure reference vocabulary describing which signals feed the shared mechanisms above; DebugSnapshot gains a new `atlasCoherence` field, rendered with the label `coherence` (checked against every existing debug line before finalising, confirmed unique); live `coherence  context event participants 1 · diverged=false · character facts 1 · institution memories 1 · planet entry "[Ecological changes] A wounded ecosystem was restored." · canon graph neighbours 1 · standard met=true · domain overlap[Coherence,Possibility] 7/12` on the overlay, browser-verified | Aligned | 2098 tests green (8 new) | Approved 9.5/10 | **LOCKED** |
| AF-196 | The Atlas Verification Engine | v1 *(Verification Domains (12)/The Verification Chain (10-stage ordered non-cyclic ladder)/Scientific+Engineering+Historical+Commander+Ecological Verification/Player Verification/Institutional Verification/The Evidence Graph/The Confidence Model (5-tier categorical ladder)/Contradiction Review/The Truth Standard/Developer Tools — AF-195's direct sibling, composing with AF-135/148/151/165/172's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasVerification/` module (`atlasVerificationData.ts` + `AtlasVerificationRuntime.ts`); confirmed "Scientific Verification" reuses AF-172's real `HypothesisTracker` directly, "The Evidence Graph" composes AF-151's real `KnowledgeGraph.addEdge` directly (its existing numeric `confidence` field), "Player Verification" reuses AF-148's real `CanonEventLedger` directly (the same shape AF-195's own Context Chain already reused), "Institutional Verification" reuses AF-165's real `InstitutionalMemoryTracker` directly, and "Contradiction Review" reuses AF-148's real `KnowledgeStateTracker.revealHistoricalUnderstanding` directly — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "The Verification Chain" is confirmed an ORDERED, NON-CYCLIC ladder via the new `verificationChainRank`, mirroring the established `xRank` pattern; "The Confidence Model" is confirmed genuinely new — a discrete categorical ladder distinct from `GraphEdge`'s continuous numeric confidence field — via the new `confidenceLevelRank`; "The Truth Standard" mirrors AF-195's real `coherenceStandardMet` shape a second time via the new `truthStandardMet`, typed to its own union; "Verification Domains" shares 9 of 12 with AF-195's real `COHERENCE_DOMAINS` and 8 of 12 with AF-194's real `POSSIBILITY_DOMAINS` (no record claimed, current record remains AF-191's own 12/12), verified via AF-170's real `detectOverlap`; the domain-specific verification sections are kept as pure reference vocabulary; DebugSnapshot gains a new `atlasVerification` field, rendered with the label `verify` (checked against every existing debug line before finalising, confirmed unique); live `verify     hypothesis grounded=true · evidence graph neighbours 1 · player event witnesses 1 · institution memories 2 · understanding "Later evidence showed a full team contributed." · chain rank 4 · confidence rank 3 · truth met=true · domain overlap[Verification,Coherence] 9/12` on the overlay, browser-verified | Aligned | 2106 tests green (8 new) | Approved 9.5/10 | **LOCKED** |
| AF-197 | The Atlas Reasoning Engine | v1 *(Reasoning Domains (12)/The Reasoning Cycle (9-stage ordered non-cyclic ladder)/Individual Reasoning/Commander+Scientific+Engineering+Historical Reasoning/Player Reasoning/Collaborative Reasoning/Uncertainty/Reasoning Record/Meta-Reasoning/The Reasoning Standard/Developer Tools — overlaps almost entirely with AF-155's own already-locked "Atlas Intelligence Engine" and AF-156's own already-locked "Atlas Decision Engine", composing with AF-155/156's real classes rather than duplicating any of them; data-file only, no Runtime.ts, since no new stateful class or gate function is required)* | Complete — new `src/game/atlasReasoning/` module (`atlasReasoningData.ts` only, no `AtlasReasoningRuntime.ts` — the first data-only module this session, since the sole new piece is a pure `xRank`-shaped lookup); confirmed "Uncertainty" is exactly AF-155's real `suggestUncertaintyResponse` directly, "Collaborative Reasoning" reuses AF-155's real `CollaborativeProblemLog` directly (at least the NINTH instance), "Reasoning Record" reuses AF-156's real `explainDecision` together with AF-156's real `DecisionLog.record` directly, and Commander/Scientific/Engineering/Historical Reasoning's underlying mechanism is exactly AF-155's real `rankOptions` directly — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; mirroring AF-156's own precedent exactly, the four domain-specific Reasoning sections share ZERO exact-string members with AF-155's own conceptually-identical "Reasoning Factors" lists (e.g. Commander Reasoning Criteria vs the real `COMMANDER_REASONING_FACTORS`), kept as new reference lists feeding the same shared mechanism rather than merged in; "Individual Reasoning" (8 style preferences) is confirmed a third distinct axis from both AF-155's evaluation-criteria and AF-156's decision-slot lists, kept as pure flavour vocabulary per the AF-030/155 personality design law; "The Reasoning Cycle" is confirmed an ORDERED, NON-CYCLIC ladder via the new `reasoningCycleRank`, sharing only 2 of 9 stages with AF-155's real `INTELLIGENCE_LAYERS`, 2 of 9 with AF-156's real `DECISION_PYRAMID_LEVELS`, and ZERO with AF-155's own `LEARNING_LOOP_STAGES`; "Reasoning Domains" shares 7 of 12 with AF-196's real `VERIFICATION_DOMAINS` and 6 of 12 with AF-195's real `COHERENCE_DOMAINS` (no record claimed); "Meta-Reasoning" and "The Reasoning Standard" are kept as pure reflective reference checklists, deliberately NOT forced into a gate function since neither uses an explicit "if yes/no then X" framing unlike AF-195/196's own Standards — all overlaps confirmed via AF-170's real `detectOverlap`; DebugSnapshot gains a new `atlasReasoning` field, rendered with the label `reasoning` (checked against every existing debug line before finalising, confirmed unique); live `reasoning  collaborators 2 · best hypothesis hypothesis-dormant-relay · uncertainty response none · reasoning record entries 1 · cycle rank 4 · domain overlap[Reasoning,Verification] 7/12` on the overlay, browser-verified | Aligned | 2114 tests green (8 new) | Approved 9.5/10 | **LOCKED** |
| AF-198 | The Atlas Judgement Engine | v1 *(Judgement Domains (12)/The Judgement Cycle (9-stage ordered non-cyclic ladder)/Individual Judgement/Commander+Scientific+Engineering+Historical Judgement/Player Judgement/Collective Judgement/The Cost of Decisions/Judgement Record/Judgement Maturity/The Accountability Principle/Developer Tools — AF-197's direct sibling, overlapping almost entirely with AF-155/156's already-locked modules, composing with their real classes rather than duplicating any of them; data-file only, no Runtime.ts)* | Complete — new `src/game/atlasJudgement/` module (`atlasJudgementData.ts` only, no `AtlasJudgementRuntime.ts` — the second data-only module in a row); confirmed "balancing evidence with humanity"/"Commander Judgement" is exactly AF-156's real `ethicalAlignmentScore`/`ETHICAL_VALUES` directly, Scientific/Engineering/Historical Judgement's mechanism is exactly AF-155's real `rankOptions` directly, "Judgement Record" reuses AF-156's real `explainDecision` together with AF-156's real `DecisionLog.record` directly, and "Collective Judgement" reuses AF-156's real `GROUP_DECISION_BODIES`/`GROUP_CONSENSUS_FACTORS` composed with AF-155's real `CollaborativeProblemLog` directly (at least the TENTH instance) — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "The Judgement Cycle" is confirmed an ORDERED, NON-CYCLIC ladder via the new `judgementCycleRank`, sharing only 3 of 9 stages with AF-197's real `REASONING_CYCLE_STAGES` and 2 of 9 with AF-156's real `DECISION_PYRAMID_LEVELS`; "Judgement Domains" shares 11 of 12 with AF-197's real `REASONING_DOMAINS` (ties but does not break AF-191's own 12/12 record); "Judgement Record" shares 4 of 6 fields exactly with AF-197's real `REASONING_RECORD_FIELDS` — all overlaps confirmed via AF-170's real `detectOverlap`; "Individual Judgement" kept as pure flavour vocabulary per the AF-030/155 personality design law; "The Cost of Decisions"/"Judgement Maturity"/"The Accountability Principle"/"Player Judgement" all kept as pure reference vocabulary, deliberately not forced into gate functions since none uses an explicit "if yes/no then X" framing; DebugSnapshot gains a new `atlasJudgement` field, rendered with the label `judgement` (checked against every existing debug line before finalising, confirmed unique); live `judgement  ethical alignment score 18 · collective panel size 2 · judgement record entries 1 · cycle rank 4 · domain overlap[Judgement,Reasoning] 11/12` on the overlay, browser-verified | Aligned | 2123 tests green (9 new) | Approved 9.5/10 | **LOCKED** |
| AF-199 | The Atlas Civilisational Wisdom Engine | v1 *(Wisdom Domains (12, an EXACT 12/12 match with the real JUDGEMENT_DOMAINS — ties the absolute overlap record a second time)/The Wisdom Cycle (7-stage explicit closed loop)/Collective+Commander+Scientific+Institutional+Cultural Wisdom/Player Wisdom/Wisdom Through Failure/The Wisdom Library/Intergenerational Wisdom/The Wisdom Test/The Civilisational Compass/Developer Tools — the closest possible collision short of a verbatim duplicate with AF-160's own locked "Atlas Wisdom Engine", resolved by living under its own `atlasCivilisationalWisdom/` directory, composing with AF-155/159/160/165's real classes rather than duplicating any of them)* | Complete — new `src/game/atlasCivilisationalWisdom/` module (`atlasCivilisationalWisdomData.ts` + `AtlasCivilisationalWisdomRuntime.ts`); NAMING SCOPE NOTE documented in-code and in the catalogue doc distinguishing this module from AF-160's own locked "Atlas Wisdom Engine"; confirmed "The Wisdom Cycle" reuses AF-155's real generic `CyclicStageTracker<TStage>` directly (the same class AF-160's own "Reflection Loop" already reused, sharing 3 of 7 stages exactly with AF-160's real `REFLECTION_LOOP_STAGES`), "Commander Wisdom" reuses AF-160's real `CommanderWisdomTracker`/`MentorshipLedger` directly, "Institutional Wisdom" reuses AF-165's real `InstitutionalMemoryTracker` directly, "Cultural Wisdom" composes AF-159's real `CulturalTrendTracker` directly, "Wisdom Through Failure" reuses AF-160's real `WisdomMemoryArchive` directly, and "Intergenerational Wisdom" reuses AF-160's real `generationalTransferRank` directly — all reused directly, all confirmed by dedicated tests, no new duplicate mechanic; "Wisdom Domains" is confirmed an EXACT 12/12 set match with AF-198's real `JUDGEMENT_DOMAINS` (reused directly, tying the absolute overlap record for the second time after AF-191's own reuse of AF-171's real `CREATIVE_DOMAINS`) yet shares only 3 of 12 with AF-160's own real `WISDOM_DIMENSIONS`; "Collective Wisdom" shares 2 of 7 and "The Civilisational Compass" shares 5 of 7 with AF-160's real `CIVILISATION_VALUES`, and "Cultural Wisdom" shares 2 of 6 with AF-160's real `CULTURAL_WISDOM_FACTORS` — all confirmed via AF-170's real `detectOverlap`; `WisdomLibrary` is confirmed genuinely new — an append-only per-lesson narrative record (situation/decision/outcome/reflection/future relevance/teaching value), a different question from AF-160's real `WisdomMemoryArchive` (which only tags destinations); "Scientific Wisdom"/"Player Wisdom"/"The Wisdom Test" kept as pure reference vocabulary, deliberately not forced into gate functions; DebugSnapshot gains a new `atlasCivilisationalWisdom` field, rendered with the label `civWisdom` (checked against the pre-existing `wisdom` label before finalising, confirmed distinct); live `civWisdom  cycle Experience (next Experience) · commander wisdom 3.3 · mentees 1 · institution memories 3 · cultural adopters 1 · failure outcomes 2 · library teaching value "Taught at the Verdance Academy."` on the overlay, browser-verified | Aligned | 2132 tests green (9 new) | Approved 9.5/10 | **LOCKED** |
| AF-200 | The Atlas Constitution | v1 *(⚠ CRITICAL SCOPE: this module's own title/framing echoes the REAL supreme `docs/CONSTITUTION.md` almost word for word — never modifies, supersedes, duplicates, or ranks above it; the THIRD in-fiction meta-governance charter after AF-146's real "Design Constitution" and AF-170's real "Atlas Prime Directive", both of which carry the same disclaimer — 15 Articles/Constitutional Review (broadest all-must-pass gate yet)/The Constitutional Oath/The Player Promise/The Developer Promise/Constitutional Amendments/Developer Tools, composing with none of AF-146/170's locked classes or lists, only documenting overlap against them)* | Complete — new `src/game/atlasConstitution/` module (`atlasConstitutionData.ts` + `AtlasConstitutionRuntime.ts`); ⚠ CRITICAL SCOPE NOTE documented prominently in-code, in the catalogue doc, and in the closing summary to the Project Owner, mirroring AF-146's and AF-170's own established disclaimers verbatim in spirit; AF-170's real, LOCKED `SYSTEM_PRIORITY_LADDER` was NOT edited to insert this module — documented only as prose; confirmed "The Constitutional Oath"'s closing line is a VERBATIM exact-string match (1 of 9) with AF-170's real `DEVELOPER_PROMISE` (which already contains "Leave the universe stronger than you found it"); "The Player Promise" (7) and "The Developer Promise" (4) both share ZERO exact-string members with either AF-146's or AF-170's own real same-named lists despite identical intent — a third instance of each, membership fragmented entirely by word-form/structure differences; the 15 Articles share 7 of 15 with AF-146's real `TEN_PILLARS` (no record claimed) but ZERO with AF-170's real `PRIME_DIRECTIVES` names (all prefixed "Protect ") — all confirmed via AF-170's real `detectOverlap`; `constitutionalReviewPassed` is confirmed genuinely new — another all-must-pass checklist gate, but the broadest in this codebase, gating all fifteen Articles, confirmed by a dedicated test; "Constitutional Amendments" kept as prose documentation only, since it governs future Project Owner prompts rather than any computable signal; DebugSnapshot gains a new `atlasConstitution` field, rendered with the label `atlasConst` (checked against the pre-existing `constitn`/`primeDir` labels before finalising, confirmed distinct); live `atlasConst articles 15 · review passed=true · player promise 7 items · developer promise 4 items · article overlap[Constitution,Pillars] 7/15 · article overlap[Constitution,PrimeDirectives] 0/15 · oath overlap[Constitution,PrimeDevPromise] 1/9` on the overlay, browser-verified | Aligned | 2139 tests green (7 new) | Approved 9.5/10 | **LOCKED** |

## GP Module Status Registry

**A separate numbering track from AF-XXX above.** AF-000 → AF-200 catalogue the Atlas meta-progression/lore layer (spec + implementation modules). GP-XXX begins implementation of the actual real-time core gameplay loop (movement/combat/director/mission/loot/elites/bosses) that the AF-XXX layer sits on top of. Same production contract as every AF module: verbatim prompt cataloguing, real tested TypeScript, full suite + typecheck + build green, browser-verified, self-reviewed to 9.5/10 before lock.

| Module | Title | Version | Stage | Review | QA | Approval | Lock |
|---|---|---|---|---|---|---|---|
| GP-001 | Core Gameplay Loop — The Perfect Roguelite Run / Core Roguelite Framework | v2 *(two prompts, both headed GP-001, treated as one module extended across two rounds: v1's pre-implementation audit found ~80% of the described loop already built across AF-016→090 and identified 6 genuine gaps (Build-Defining Paths/Mid-Run Merchant/Extraction Decision/Mission Results/Guaranteed Elite Rewards/Boss Game-Changing Rewards); v2's follow-up prompt described the same loop again plus a "Game Feel" section, and a second audit found everything else already real, identifying exactly 3 further genuine gaps (Hit-Stop/Particle System/Controller Vibration))* | Complete — v1: new `src/game/progression/buildPaths.ts`, `src/game/loot/eliteRewards.ts`, `src/game/bosses/bossArtifacts.ts`; v2: new `src/engine/feel/HitStop.ts`+`hitStopTuning.ts`, `src/engine/vfx/Particles.ts`+`particleTuning.ts`, `src/engine/input/hapticTuning.ts` (wired into `GamepadAdapter.vibrate()`); plus direct `main.ts` wiring throughout (no Atlas/AF-XXX file touched); `GameStates.ts` extended additively with four new overlay ids, all pre-registered under `Gameplay` exactly like every existing overlay; Build Path bias mirrors `UpgradePool.offer()`'s own algorithm externally; Extraction's difficulty escalation calls `EnemyDirector`'s own already-public `setThreatInputs()`; Elite/Boss reward mechanics are pure post-processing composition over `generateDrop()`/`generateElite()` (AF-023/034 untouched); the Boss Artifact choice is offered only once `BossDirectorRuntime`'s own ceremony-line queue drains; hit-stop is deterministic (every trigger site is itself deterministic) and reuses `Camera.ts`'s own shake-impulse-with-cap shape; particles are the first real consumer of AF-044's own `performance.particleQuality` field and use the same generic `Pool<T>` popups/projectiles already use; vibration reuses hit-stop's own `HitStopSource` vocabulary directly rather than a third parallel union; two accessibility fields (`hapticIntensity`, `reducedScreenEffects`) added additively to AF-044's locked `SettingsData` — `reducedScreenEffects` is also the first real wiring of `Camera.shakeScale`, registered since AF-018 but never previously set from any setting; `DebugSnapshot`'s combined `gpCoreLoop` field extended, not duplicated; browser-verified against real automated runs both rounds (zero page errors both times, only the pre-existing baseline 404, real wave-count increments, real Mission Results recap on a real Defeat screen, a real particle burst firing from a real defeat, both new accessibility toggles found/clicked/confirmed live) | Aligned | 2178 tests green (38 new across both rounds) | Approved 9.5/10 | **LOCKED** |
| GP-002 | Enemy, Wave & Difficulty Framework | v1 *("Continue directly from GP-001. Do NOT redesign previous systems. Extend them." — 14 sections: Enemy Hierarchy/Enemy Roles/AI Behaviours/Wave Director/Difficulty Curve/Wave Structure/Spawning/Elites/Bosses/Boss Phases/Enemy Scaling/Synergy System/Events/Director Intelligence/Mission End/Code Requirements/Self Review; a pre-implementation audit found most of it already built across AF-017/021/033/034/035/056 and identified 9 genuine gaps; the Project Owner's explicit answer was "Everything the audit found" rather than a curated subset)* | Complete — new `src/game/enemies/sniperSynergy.ts`, `src/game/director/spawnPlacement.ts`, `src/game/director/meteorShower.ts`; extended `src/game/enemies/eliteData.ts`+`EliteGenerator.ts` (6 dormant `MutationKind`s flipped `mechanicallyLive`, 3 new — Electric/Cloaked/Vampiric — added, 18/18 now live), `src/game/enemies/enemyData.ts`+6 faction roster files (6 new `ENEMY_ROLES` — assassin/commander/charger/ambusher/burrower/exploder/shieldUnit — tagged onto real rosters, one deliberate lore-respecting exclusion on `machine-command-core`), `src/game/director/conductorData.ts`+`DirectorConductor.ts` (`recordKill()`/`recordNearDeath()` complete the Director Intelligence input set, preserving AF-056's pacing-only guarantee), `src/game/bosses/bossData.ts` (Hollow Sentinel: `twoPhase`→`fourPhase`, hull 900→3000, phases 3/4 added completing the Learning→Pressure→Chaos→Signature arc; `createWorldBossVariant()` mirrors `EliteGenerator`'s own base-scaling pattern over a `BossDef`; `WORLD_BOSS` the one authored instance), `src/game/audio/audioData.ts`+`MusicState.ts` (2 new `MUSIC_STATES` for the new boss phases); plus direct `main.ts` wiring throughout for all 9 items (3 dormant `EnvironmentalEventTriggered` handlers wired onto AF-035's existing hazard-zone engine and existing spawn paths; wave-milestone merchant trigger every 7th wave; Push Deeper boss-chance roll + extraction-depth-scaled research rewards) — no AF-0XX file's locked algorithm edited, every union extended additively only; a boss hull/threshold rebalance was required after adding phases 3/4 surfaced a transition-window damage-overshoot cascade risk in testing (fixed, verified via the full boss suite); a dedicated wave-pacing review found AF-017's locked `phaseSequence` already matches the spec's own 10-wave example's shape, so no reorder was made; browser-verified against a real automated run (a genuine spawned elite carrying `MYTHIC [berserker, electric, quantumShift]`, a real wave-milestone merchant trigger firing mid-run, zero page errors, only the pre-existing baseline 404) | Aligned | 2223 tests green (63 new) | Approved 9.5/10 | **LOCKED** |
| GP-003 | Meta Progression, Campaign & Persistent Progression | v1 *("Continue directly from GP-002. Do NOT redesign previous systems." — 21 sections: Objective/The Meta Loop/The Campaign/Galaxy Progression/Star Systems/Mission Completion/Player Level/Commanders/Ship Progression/Research/Atlas Progression/Galaxy Unlocking/Mission Modifiers/Resources/Blueprints/The Museum/Home Base/Loadout/Difficulty/Long Term Goals/Save System/Code Requirements/Self Review; two parallel pre-implementation audits (campaign/galaxy/mission structure, and commander/ship/research/economy) found most named concepts already real but the spec's own campaign-scale numbers (100 galaxies × 10 systems × 10 missions = 10,000 missions) a vast overclaim against 1 galaxy/13 systems/3 missions, and identified 12 genuine gaps; the Project Owner's explicit answer was "Everything the audit found" rather than a curated 8-item shortlist)* | Complete — **critical save-persistence fix**: `CommanderProgressionRuntime`/`RosterRuntime`/`BondNetworkRuntime`/`ShipOutfittingRuntime`/`ShipCollectionRuntime`/`CampaignRuntime` all gained real `toSave()`/`loadSave()` and registered with `saveCoordinator` (previously silently reset on reload despite being permanent-by-design; `AchievementRuntime` needed nothing, being stateless over already-persisted `meta`); `CommanderProgressionRuntime`/`ShipOutfittingRuntime` converted from hardcoded singletons to lazy per-id maps (`commanderProgressionFor`/`shipOutfittingFor`) for the new Loadout picker; new `src/game/galaxy/galaxyClusterData.ts` (`GalaxyClusterDef` — a real tier above star systems, composing entirely with already-registered boss/research/module/commander/artifact/music ids; a second real cluster gated on a real `CampaignRuntime` unlock, `galaxyRuntime` made reassignable mirroring GP-002's `sandboxBoss` pattern); 5 new additive `GameStates.ts` screens (RecruitCommanders/ViewMuseum/ReadCodex/ManageAtlas/LoadoutChoice), the last scoped to Ship+Commander only (Weapon/Passive/Artifact/Consumables/Cosmetics documented as trims — no dormant mechanism exists for any of them); `MISSION_MODIFIER_KINDS` extended 10→19 with the spec's own named list, distributed into all 4 real missions' pools with real deltas; new `campaignDifficultyFor()` (pure function) feeds `EnemyDirector`'s real `missionDifficulty` seam from `StarSystemDef.threatLevel` (set everywhere, read by nothing before this); new `src/game/longTermGoals/LongTermGoalsRuntime.ts` and `src/game/atlasProgression/AtlasProgressionRuntime.ts` (both pure composition, no new state) unify the spec's 9 long-term goals and 10 Atlas axes respectively from already-live trackers; `RESOURCE_TYPES` extended 10→15 with the spec's exact-named resources, each with a real grant site (mission-modifier-triggered or boss-defeat) and real spend site (2 new recipes); new `BLUEPRINT_CATEGORIES` (6 real-backed, Passives/Buildings excluded) tags every recipe, with 3 new recipes giving every category real craftable content unlocked via boss-defeat drops and 2 new real merchant offers; 2 systems' mismatched `missionIds` corrected to their own already-biome-matched missions (a real bug fix) plus one new mission (`forge-primus-uprising`) authored for a third system — no AF-0XX/GP-001/GP-002 locked algorithm edited anywhere, every union/persistence-registry extended additively only; browser verification surfaced and fixed a genuine layout regression (`index.html`'s `overflow: hidden` clipped Galaxy Command's now-15+-button list past the viewport with no scroll) before re-verifying the full flow end-to-end; browser-verified against a real automated run (all 5 new screens rendering live data, debug overlay showing real composed cluster/difficulty/goals values, zero page errors, only the pre-existing baseline 404) | Aligned | 2267 tests green (~100 new) | Approved 9.5/10 | **LOCKED** |
| GP-004 | Content Engine — Weapons, Ships, Commanders, Events, Galaxies & Loot | v1 *("Continue directly from GP-003. Do NOT redesign previous systems." — asks whether the data architecture can scale to 1000+ weapons/500+ passives/500+ artifacts/500+ Commanders/1000+ enemies "without requiring architectural changes," across 17 content categories; a dedicated audit distinguished genuine architecture violations (adding content would require engine-code edits) from content-volume gaps (the spec's own future-authoring work); the Project Owner's explicit answer was "Fix the real architecture violations" — the first GP module where the answer was NOT "everything the audit found")* | Complete — all 10 faction squad-spawning functions in `main.ts` (`spawnOutlawSquad`, `spawnMachineNetwork`, …) converted from `def.id === "<hardcoded-string>"` leader/special-member lookups to `EnemyDef.roles.includes(...)` (GP-002's own role tags), guarded by new `tests/factionSquadRoles.test.ts` (21 tests) which caught a real bug — `machine-command-core` carries `"controller"`, not `"elite"` (its own lore denies leadership); 2 scattered `drone.def.id` checks fixed the same way; `commanderData.ts` gains `CommanderUltimateEffectKind` (`novaDamage`/`barrierBurst`/`healBurst`) and an optional `effect?: CommanderUltimateEffect` field, with a new `applyCommanderUltimateEffect()` generic interpreter in `main.ts` dispatching onto already-existing mechanisms (`dealAreaDamageToEnemies`, `DefenceState.addBarrier`/`healHull`) — Ultimates were presentation-only before this; new `src/game/passives/passiveData.ts` (`PASSIVE_CATEGORIES` — the spec's 14 named types — reusing AF-028's `PassiveTrigger`/`EquipmentBonus` vocabulary directly, `SANDBOX_PASSIVES` wired into the real, tested level-up flow via a new optional `effect?: EquipmentBonus` field on `UpgradeDefinition` and a rewritten `applyUpgrade()` — previously a `switch(id)` on 6 literal upgrade-ids, the same anti-pattern as the squad-spawning fix, just in the core level-up system; 2 new composition-root wirings for `experienceGain`/`resourceGain` at the XP-pickup callback and `dropLoot`'s `researchBonus`); new `src/game/artifacts/artifactData.ts` (`SANDBOX_ARTIFACTS` — 4 real, permanent, build-altering entries distinct from `bossArtifacts.ts`'s 5 boss-kill-gated rewards — dispatched via 4 new `bus.on(...)` listeners on `EnemyKilled`/`PlayerDamaged`/`ShieldBroken`/`CommanderLevelUp`, mirroring AF-001 §7's "gameplay systems never know meta exists" bus discipline; bought at the mid-run Travelling Merchant via a new `ArtifactRuntime` mirroring `BossArtifactRuntime`'s own no-duplicate law); `WEAPON_CATEGORIES` gains `"summon"` additively (the one genuine gap — no category, naming layer, or weapon existed for it) plus 2 new real weapons (`SWARM_TENDER`, `SPORE_LANCE`) proving the new Summon category and the pre-existing-but-unused `biologicalWeapons` naming layer, with matching `WeaponProfileDef`/`WeaponFamilyDef`/`WeaponRosterEntry` entries keeping every downstream AF-075/076 shelf consistent — every other spec weapon-category name documented as already mapping onto AF-075's existing 17-category naming layer; deliberately did NOT hand-author additional Events/Galaxies/Blueprint/Cosmetic/Museum content or build a JSON mod-loader, matching the Project Owner's scope choice; `DebugSnapshot`'s combined `gpContentEngine` field added, mirroring the `gpCoreLoop`/`gpEnemyWave`/`gpMetaProgression` convention; browser-verified against a real automated run (Splash→GalaxyCommand→MissionSelect→Gameplay: `gpContent passives 14/14 categories · artifacts [none]/4 · weapon categories 16 (+summon) · framework categories 18` and `weapons … arsenal 1/12` both rendering live, zero page errors, only the pre-existing baseline 404) | Aligned | 2305 tests green (38 new) | Approved 9.5/10 | **LOCKED** |
| GP-005 | Final Production Directive | v1 *("Continue directly from GP-004. Do not redesign completed systems." — no content categories, asks instead for continuous, open-ended refinement across 12 review domains (Combat/Game Feel/UI/Performance/Balance/Player Retention/Accessibility/Save System/Code Quality/Testing/Self-Improvement/Final Goal) with an explicitly unbounded stopping condition ("only stop when the game is stable, polished, scalable and genuinely fun"); 3 parallel audits found real, bounded issues across 4 thematic buckets (Balance & Passive triggers / Performance & scalability / Remaining architecture cleanup / Save correctness & accessibility wiring), with real audio, a real graphical UI layer, a dialogue/subtitle system, and 3D/lighting excluded from every option as separately-scoped future work, not a bug-fix sweep; the Project Owner's explicit multi-select answer was ONLY "Balance & Passive triggers")* | Complete — `SANDBOX_UPGRADES` extracted from `main.ts` to new `src/game/progression/sandboxUpgrades.ts` so its own numeric balance is independently testable; Rapid Cycler's `cooldownReduction` value rebalanced 0.14→0.10, closing a strict-dominance gap against Focused Coils (was ~2.13x DPS vs ~1.75x at 5/5 stacks for identical weight/maxStacks/cost, now ~1.69x vs ~1.75x — near parity), guarded by new `tests/upgradeBalance.test.ts`; `PassiveDef.trigger` — never read anywhere, every Passive applying as a flat permanent bonus at pickup regardless of its stated trigger (audit's own cited example: Nanite Mesh "restores hull when critically wounded" that actually healed once, immediately, at pickup) — fixed for the 3 Passives whose `bonus.kind` is a genuinely instant effect (`shieldCapacity`/`shieldRegeneration`: Hardened Plating, Guardian Ward, Nanite Mesh): `applyUpgrade` now holds these in a new `heldPassiveIds` set instead of applying at pickup, a new `firePassiveTrigger()` dispatches held Passives through 4 new `bus.on(...)` listeners (`EnemyKilled`/`PlayerDamaged`/`ShieldBroken`/`DamageDealt`) mirroring GP-004's own `SANDBOX_ARTIFACTS` bus-listener pattern exactly and reusing the same real mechanisms (`DefenceState.addBarrier`/`healHull`); `onLowHealth` (a threshold state, not a discrete event) gets a new `checkLowHealthPassives()` called once per fixed tick, edge-triggered so it fires once per crossing and rearms once health recovers; the other 10 stat-kind Passives correctly stay pickup-permanent (re-firing a permanent stat on every trigger would compound unboundedly), matching AF-028's own pre-existing trigger+bonus convention exactly, which this module does not redesign; guarded by a new GP-005 describe block in `tests/passives.test.ts` (exactly 3 instant-effect Passives, every one's trigger is dispatchable, the stat/instant split is exhaustive with no overlap); deliberately did NOT touch the O(n²) collision-loop/spatial-partitioning gap, the 5 remaining hardcoded-id reinforcement-spawn lookups, the save version-migration gap, or the remappable-controls/colour-blind/font-scaling wiring — all real, all found by the audit, all excluded by the Project Owner's scope choice; `gpContentEngineDebugLine()` extended to report the new classification; browser-verified against a real automated run (`gpContent  passives 14/14 categories (3 real triggered procs, held [none]) · artifacts [none]/4 · weapon categories 16 (+summon) · framework categories 18` rendering live, event-bus listener count rising 19→23 for the 4 new Passive-trigger listeners, zero page errors, only the pre-existing baseline 404) | Aligned | 2311 tests green (6 new) | Approved 9.5/10 | **LOCKED** |
| GP-FINAL | Gameplay Audit, Balance & Finalisation | v1 *(received ahead-of-sequence during the Foundation Phase, 2026-07-06 — catalogued as a standing contract since its prerequisites, GP-001 → GP-005, didn't exist yet; first execution ran a partial audit against AF-016 → AF-026 only, recorded in `docs/GP_FINAL_AUDIT.md`. Resent 2026-07-11, now that GP-001 → GP-005 are complete and locked — second execution re-audited specifically "Run Structure"; a 4-bucket scope answer was ONLY "Wave-driven boss cadence + pacing." "Pick them up now" drove the third execution (the other three buckets). "Fix the next thing" drove the fourth execution — the one item still open after the third)* | Complete (fourth execution) — second execution: Mini Boss tier, wave-count-driven cadence, Director `loop`/pause-resume, recurring Extraction. Third execution: Elite reward pool's other 10 named types (`src/game/loot/eliteRewardPool.ts`); a new standalone per-wave Reward system (`src/game/progression/waveRewards.ts`, 8 categories, auto-granted per wave); the 6-weapon/6-passive build frame (`equippedWeapons`/`weaponRuntimes` parallel arrays, `sourceWeaponIndex`-tagged projectiles, `UpgradePool`'s additive `isAllowed` predicate capping distinct Passives at 6). Fourth execution, same day: closed the last flagged item, the Level-up content law violation — Focused Coils/Rapid Cycler/Precision Optics/Tuned Thrusters (`SANDBOX_UPGRADES`) were plain percentage stats with no mechanic attached, self-declared placeholder since AF-022; `UpgradeDefinition.effect` (AF-022, locked) additively widened to accept an array, and each of the four now carries a second bonus using a BonusKind AF-028 had registered but never wired to anything — `statusChance`/`statusDuration` (raises the chance/duration of the firing weapon's own status-on-hit, in the exact hit-resolution code the third execution's weapon rework touched), `criticalDamage` (feeds `playerPacket()`'s `critMultiplier`), `boostEfficiency` (a new additive `boostCooldownScale` field on `PlayerMovement`, AF-020 locked, mirroring `WeaponRuntime.intervalScale`'s precedent, shortening the real dash/i-frame cooldown); Emergency Barrier/Collection Field left untouched (never cited by the audit, already mechanically textured); `tests/upgradeBalance.test.ts` updated (not weakened) to pull the primary bonus out by kind; browser-verified live — a level-up offer rendered "Rapid Cycler — +10% fire rate, +300ms status-on-hit duration" and resumed cleanly on pick, zero console/runtime errors. Full record: `docs/GP_FINAL_AUDIT.md` §5–§6 and `docs/modules/GP-FINAL-gameplay-audit.md`'s "Third execution"/"Fourth execution" sections | Aligned | 2343 tests green (32 new across the second–fourth executions) | All four of GP-FINAL's own FAIL/FLAGGED items (Level-up content law, Wave rewards, Elite reward pool, 6-weapon/6-passive build frame) are now real, tested, browser-verified — §3's own lock condition is met in full | **LOCKED** |

**Authorised amendments log:**
- 2026-07-05 — AF-002 output: palette extended with `vitality.green`, `shield.blue` (owner ruling via AF-004 review).
- 2026-07-05 — AF-002 output: rarity ramp superseded by AF-007's nine-tier ladder (owner authority via AF-007).
- 2026-07-05 — AF-002 output: secondary display face permitted, titles only (owner authority via AF-009).
- 2026-07-05 — AF-008 output: Plasma Blue → `shield.blue`; Poison → `toxin.green`; Slow → `neutral.grey` (three calls ratified by owner).
- 2026-07-06 — DR-005: research unlocks instant permanently (owner ratified Option A); extends to crafting time by the same reasoning.
- 2026-07-09 — Technology Decision amended: primary art style is now Nintendo-inspired stylized 3D mobile game art (chibi proportions, hand-painted textures, soft PBR lighting), superseding AF-002/092's prior art-style vocabulary wherever it conflicts; requires a real 3D rendering pipeline in place of the current 2D Canvas renderer. See `docs/TECHNOLOGY_DECISION.md` amendment. Implementation scope/sequencing pending Project Owner direction.
- 2026-07-11 — `docs/MASTER_BUILD_DIRECTIVE.md` issued: a standing, non-numbered directive (not an AF-XXX or GP-XXX module) permanently governing implementation quality/workflow for all future work, added to `CLAUDE.md`'s read-before-any-implementation-work list as document #3. Does not redesign AF-000→AF-200 or GP-001→GP-005; restates existing standing rules (pooling, accessibility, extend-never-overwrite) as explicitly permanent and adds new ones (no TODOs/placeholders, finish-compile-test-optimise-commit one feature at a time, the Hades/Brotato/Vampire Survivors/Deep Rock Galactic Survivor quality bar). No code change resulted from its arrival — its scalability/balance/performance targets restate GP-004's/GP-005's own already-audited scope; GP-005's deliberately-deferred items (spatial partitioning, 5 remaining hardcoded-id lookups, save-migration gap, accessibility wiring) remain real backlog, not re-opened by this directive.
- 2026-07-11 — GP-FINAL's second execution: prerequisites (GP-001 → GP-005) delivered, so the standing contract received ahead-of-sequence on 2026-07-06 became executable again. Re-audited "Run Structure" specifically (the item the first execution named as awaiting wave-based content); the Project Owner's scope answer, from a 4-bucket summary, was only "Wave-driven boss cadence + pacing." Full record: `docs/GP_FINAL_AUDIT.md` §4 and `docs/modules/GP-FINAL-gameplay-audit.md`'s "Second execution" section. GP-FINAL itself remains NOT LOCKED — 3 of its 4 own FAIL/FLAGGED items are still open.
- 2026-07-11 — GP-FINAL's third execution, same day: the Project Owner's next instruction ("pick them up now") picked up all three items the second execution had left as open, undeferred backlog — the Elite reward pool's other 10 named types, a new standalone per-wave Reward system, and the 6-weapon/6-passive build frame (a core-combat-loop rework). All three implemented, tested (2336 tests green, 25 new since GP-005's lock), browser-verified live, and committed separately per the Master Build Directive's finish-one-feature-then-commit rule. Full record: `docs/GP_FINAL_AUDIT.md` §5 and `docs/modules/GP-FINAL-gameplay-audit.md`'s "Third execution" section. GP-FINAL remains NOT LOCKED — only the Level-up content law violation (1 of its original 4 flagged items) is still open.
- 2026-07-11 — GP-FINAL's fourth execution, same day: the next instruction ("fix the next thing") closed the one item left open — the Level-up content law violation. `UpgradeDefinition.effect` (AF-022, locked) additively widened to accept an array; Focused Coils/Rapid Cycler/Precision Optics/Tuned Thrusters each gained a second, mechanic-attached bonus wiring a previously-dormant AF-028 BonusKind (statusChance/statusDuration/criticalDamage/boostEfficiency) into a real system (weapon status-on-hit, crit multiplier, a new `PlayerMovement.boostCooldownScale`). 2343 tests green (32 new since GP-005's lock), browser-verified live, `tsc`/`vite build` clean. Full record: `docs/GP_FINAL_AUDIT.md` §6 and `docs/modules/GP-FINAL-gameplay-audit.md`'s "Fourth execution" section. **All four of GP-FINAL's own FAIL/FLAGGED items are now resolved — GP-FINAL is LOCKED.**
- 2026-07-12 — Technology Decision amended (render-style clarification, not a pivot — confirmed explicitly by the Project Owner when flagged for the exact tension per `CLAUDE.md`'s own standing instruction): the 2026-07-09 ruling's "clean hand-painted textures, soft PBR lighting" is replaced by flat toon/cel-shading plus a bold, chunky VFX/UI language (8 Visual Style Rules + a 32px silhouette litmus test, catalogued verbatim in `docs/TECHNOLOGY_DECISION.md`'s 2026-07-12 amendment). Chibi proportions and the 3D rendering-pipeline requirement are unchanged. Documentation only — no code change, since the rendering pipeline this style governs is not yet built (AF-095+, still pending).
- 2026-07-12 — DIRECTIVE — Asset Pipeline & Derivation Rules (BINDING), same day: unlike the two art-direction amendments above, this one required real code, not just documentation. New `src/game/assets/assetPipeline.ts` (pure, tested: `blendModeFor` — the one place blend mode is decided from a `pipeline` field, never filename/guesswork — plus derivation functions for thumbnails/enemy-states/boss-variant-scale) and `src/game/assets/assetRegistry.ts` (the real registry: 1,049 entries, one per actual asset need, each tagged keyed/additive/fullbleed and source/derived). Applying the derivation rules (ship thumbnails, enemy move/attack/death, boss variants are all code-generated from a real parent, never separate source files) drops the true source-file count to **854**. `docs/ASSET_MANIFEST.md` is now generated from the registry (`npm run assets:manifest`) rather than hand-written — per the directive, hand-editing it is now prohibited; `docs/asset-prompts.csv` (`npm run assets:prompts`) regenerated to cover source entries only, with the colour law's substitutions (regeneration→gold, poison/toxic→amber, biomass→amber-yellow) applied. 22 new tests (`tests/assetPipeline.test.ts`, `tests/assetRegistry.test.ts`), 2365 total green. `tsc`/`vite build` clean; bundle unchanged (main.ts doesn't import the new modules yet — no real assets exist to load, so wiring a loader would be scaffolding with nothing to plug into; `main.ts`'s existing 100%-primitive rendering already satisfies the directive's own placeholder-discipline rule by construction).
- 2026-07-12 — Asset registry full-sweep second pass + the asset-run file, same day: the Project Owner asked for an importable prompt-generator file covering EVERYTHING needing visual design ("background, hud, environment, equipment.. the list is more extensive than this"). The registry gained 192 source entries the roster-walk had missed — branding/PWA icons + display typeface, 14 HUD chrome elements, an 11-piece generic UI kit, 9 player-ship VFX, 21 run-time combat/environment entities main.ts already simulates as primitives (outlaw mines, acid pools, void zones, gravity wells, singularity charges, meteor telegraphs/impacts, crystal growths, machine shield lattices, constellation links, spawn warp-ins, attack telegraphs, loot beams/caches, merchant vessel, extraction beacon), the shared space backdrop + arena boundary, 7 interactables, 10 environmental-condition icons, 4 star-map chrome elements, the boss-intro banner, 10 ship-ability VFX, 53 commander active-ability VFX, 11 elite-reward + 8 wave-reward grant VFX, 6 faction emblems, and 18 research-node icons. New generated artefact `docs/asset-run.json` (via the same `npm run assets:prompts`): the full art directive at the top (8 Visual Style Rules verbatim, litmus test, per-pipeline delivery specs, colour law, canonical rarity palette) followed by one style-locked prompt per source asset. Registry now 1,241 entries / **1,046 true source files** (195 derived), all 1,046 prompt-covered with zero template misses. 2365 tests green, `tsc`/`vite build` clean.
